# 01.status — Lopez-Juarez Mouse-Colony Automation

> Completed items live in `_docs/archive/` (cold storage — do not read unless
> investigating history). This doc holds active + planned work only.

2026-09-04→09-07 DB schema + ETL scope era (P0.2 schema, R1–R25 fold, migrations 0001-0020 shipped) ROLLED OFF → _docs/archive/status.2026-09-04_07-schema.md  <!-- ARCHIVE: history-only -->
2026-09-07→09-09 client mock-UI era (Cage-grid → full hierarchy, shadcn/Tailwind, Task bin, Mouse drawer, Upcoming, colour channels) ROLLED OFF → _docs/archive/status.2026-09-07_09-client-mock.md  <!-- ARCHIVE: history-only -->
2026-09-12 client(ui): multi-channel identity colours — always-on Excel colour-cell mouse row (fixed columns: id·genotype·DOB·mate); sex ♂sky/♀pink id-cell bg + age DOB-cell amber (♂>1y/♀>10mo via isOldMouse computed-at-read, not stored) = RULE channels; genotype own-colour cell tint (WT/'?'=null=default bg so only mutants pop; differs-from-line = visible transfer) + mate filled chip per father-fanout group (mateColors is a LIST) = ASSIGNED; signal(state) moved to row left-spine (signalSpineClass)+id-text, off identity cells; lib/colors.ts cut to rule channels (SEX_TINT/isOldMouse), genotype hash + ColorBy toggle removed; DTO MouseCell+=dob/genotypeColor/mateColors, GridLine+=lineColor/nominalGenotypeColor; mock seeds resolved hex (GENO same-genotype-same-hex + MATE groups) = mock of future color_palette/color_assignments (UNIQUE hex, channel text+CHECK, keyed on category not per-mouse); SUPERSEDES Color-by toggle (professor reads all colours at once); also folded: line/cage panes stacked+narrowed→wider mice col, dynamic breadcrumb+Legend one row, per-mouse task colour tags (filled squares), pane totals; check-types 0, page hot-reloaded w/ dob+amber
2026-09-12 client(ui): mouse-row colour REFINED — workflow-state(signal) colour removed from the row (no spine, no row-fill, no id-text tint; signalRowClass/signalSpineClass/signalCellClass deleted as dead code); LEFT COLUMN now stacks the mouse's active-task badges collapsed BY SIGNAL with a count (2 plan tasks = one blue badge "2"), separate task-badge sub-row removed, row min-height fits 3 badges; DEAD = whole row dark (bg-neutral-700) + id strikethrough, identity tints suppressed; cage AND slot both rendered as LEFT-RAIL COLUMNS (cage | slots | mice fully COLUMN-based, NOT nested headers — hard user directive): 1 cage rail (number+line dot+name, active tint, filter count) on the left, its slot rails to the right, mice to the right of each slot; slot labels GLOBALLY unique per domain rule — mock re-seeded A8/B8/C8/D8/E8/F8 (was 4×A8); cage cards on white bg (bg-background), grey only in gutters; check-types+lint 0 errors, page hot-reloaded
2026-09-12 client(ui): 3-pane (Lines|Cages|Mice) RETIRED → single UNIFIED BODY — line|cage|slot|mice all as nested LEFT-RAIL COLUMNS in one full-width scroll Card; header gains SEARCHABLE jump dropdowns (Lines/Cages/Mice → scrollIntoView via line-/cage-/slot-/mouse- ids); NEW scope-highlight on click = ancestor path + subtree (pure lib/gridSelection resolvePath + isOnSelection, agree-up-to-min(elemLevel,selLevel) rule), selection=tint / filter=opacity kept ORTHOGONAL, default selection null + click-again clears; breadcrumb now DERIVED from selection path (descends to slot/mouse); removed Pane/PaneRow/GroupHead/Count/Dot as dead code; check-types+lint 0 errors, served 200
2026-09-12 docs: ratified 2 data-model decisions (plan §4, consistent w/ shipped schema) — breeding-cycle dates (mated=mates.occurred_at, ~DELIV=expected_delivery_on) on VERSIONED mates not mice/mouse_meta (edit=appendVersion; NEW cycle same couple=new origin_mate_id=re-mating, NOT a version row; PLUG stays notes-only Q28); tissue/genotyping lifecycle=tasks rows (task_type, origin_task_id version rows, request count=COUNT DISTINCT origin_task_id) ratifying 0016 mouse_events drop, structured RESULTS in P1 genotyping_results FK'd to mouse+originating task not tasks.direction; MouseCell.dates=view field resolved server-side from mates/litters/genotyping/tasks; folded into 00.plan §4 + 00.tasks P0.7/P1-genotyping + mock comment
2026-09-12 docs: slot adult-density overcrowding warning rule added to plan (§2 new rule-derived channel: live-adult count>capacity → warning colour + N/cap badge, computed-at-read never stored, orthogonal to selection/dead/filter, optional Upcoming-task); 3 professor constants Q32–Q34 (adult-age threshold vs weaning 21d, capacity=5?, per-slot vs per-cage) + 1 P0.6 task
2026-09-12 client: DOB life-stage colour channel SHIPPED — lib/colors.ts lifeStage(dob,sex,now) tints DOB cell baby<21d green(bg-emerald-100)/adult no-fill/old(♂>365d ♀>304d) amber, pure computed-at-read never stored; new exports ADULT_MIN_DAYS=21 + DOB_TINT, old isOldMouse replaced; docs flipped 00.tasks [x] + plan §2 age sub-bullet
2026-09-12 client: slot overcrowding warning SHIPPED — live post-weaning mice>SLOT_ADULT_CAP=5 → RED rail + N/5 badge; countSlotAdults() counts alive non-dead non-baby (babies excluded via shared ADULT_MIN_DAYS=21 → DOB channel + warning agree on one adult/baby line); red≠amber-old, orthogonal to selection; Q33 cap/Q34 per-slot-vs-cage open; 00.tasks task flipped [x]
2026-09-12 docs: colour ALLOCATION-uniqueness rule recorded (plan §2) — new key picks next palette token whose hex is unused in scope (SELECT token FROM color_palette WHERE NOT EXISTS ... [AND channel] ORDER BY sort LIMIT 1); invariant no two live assignments share hex in scope; Q35 = scope per-channel vs palette-wide; color_palette/color_assignments still design-stage (not in SCHEMA.md yet), mock seeds hex directly
2026-09-12 client: genotype-first line rail SHIPPED + lineColor DROPPED — the retired 'line' colour channel's rail now takes the NOMINAL genotype colour as its sole hue (WT lines neutral); DTO GridLine.lineColor removed, GridLine.nominalGenotypeColor is the line-rail source; docs folded (plan §2 DTO line + Assigned-channels bullet)
2026-09-12 docs: living-doc fold — plan R11–R25 schema-redesign block → _docs/archive/plan.r11-r25-schema-redesign.md (stub); tasks P0.1 + P0.6 completed mock-UI sub-tasks → _docs/archive/tasks.p0-scaffold-and-mock-ui.md (stubs, P0.6 open tasks + cross-line/Q33/Q34 pointers kept live); status 09-07→09-09 client era rolled off → _docs/archive/status.2026-09-07_09-client-mock.md; lineColor drop reflected in plan §2
2026-09-12 docs: ratified P2 offline-first PWA decision (plan §3) — every mutation→append-only op-log/outbox (NOT on tasks; UNION at read); offline render from IndexedDB snapshot (keep getColonyGrid mock→real seam) + SW shell + outbox optimistic-apply + Background-Sync flush = ONE mechanism; conflict via existing prev_id CAS; audit_logs immutable = outbox-reuse candidate; scope: DO NOT build now, later SWAP after online path; Q7 marked RESOLVED (supersedes read-only rec), added Q36 (audit_logs vs dedicated outbox)/Q37 (server-replay vs CRDT)/Q38 (Dexie vs PowerSync/ElectricSQL/Replicache); 00.tasks P2 line rewritten to design-stage stub
2026-09-12 docs: ratified 3 CRUD/colour-spike decisions (plan, source _docs/design/crud-and-color-palette.md) — (1) slots.label STAYS GLOBALLY unique, R10 composite (cage_id,label) proposal REVERSED; live DB already global (SCHEMA.md slots_label_key via 0013) so NO migration, but import must DEDUPE/RELABEL the real workbook's 'F5'-in-two-cages before insert (§5 Q19 re-ratified global + corrections annotated in-place at R1/R10-AMENDED; NOT struck — both carry other live decisions); (2) colour-allocation uniqueness = PALETTE-WIDE not per-channel, `[AND a.channel]` clause DROPPED, Q35 RESOLVED (§2 allocation bullet + §5 Q35); (3) genotype/mate colour FREES EAGERLY on last-live-user delete (eager-with-count-check, allocation filters deleted_at IS NULL, hex returns to pool — §2); 00.tasks: P0.2-R25 MEMO annotated RATIFIED + P0.4 import-helper slot-scope corrected + ONE new P0.4 dedupe task added
2026-09-13 tasks: adopt CASES+TASK_EVENTS model — cases(stable identity: case_type/signal/subject FKs+NEW subject_mate_id/gen_key/current_status cache/due_date/created_by) has-many append-only task_events(status,actor,note,ts), SUPERSEDES tasks origin_task_id versioning FOR TASKS ONLY (notes/mates/mice keep origin-id+CAS); lifecycle todo→doing→done→verified, cancelled side-exit (doing=staff claim); gen_key partial-unique WHERE deleted_at IS NULL on immutable cases resolves R10 no-UNIQUE-on-versioned-rows (key=ruleKey+origin fact id e.g. plug_check:mate=<id>, never run date; survives cancel = no regen); overdue DERIVED at read (todo/doing && due_date<today → coral thick border); system user for auto-gen created_by; SOP automation = hard-coded plug_check+genotyping generators behind pure TaskGenerator seam + event runner (genotyping reads genes/mice_genes, mouse_events dropped 0016; offsets stay task_offset_rules), user macro engine DEFERRED P1+ (later = one more TaskGenerator); plan §4 Task model v2 + tasks P0.9 (Wave 1 [~] 0021+DTOs, Wave 2 server/client)
2026-09-13 tasks: REFINE case/task model — task_events RENAMED to child `tasks` (IMMUTABLE append-only STATUS RECORDS under a case: status+case_id+actor+note+created_at, status fixed at creation = audit trail), cases keep MUTABLE current_status rollup (advance = INSERT task + UPDATE rollup, ONE service tx), create work = case + first todo task (SOP generators same), batch over N mice = N cases each w/ own first task, subject_kind = mouse/cage/slot/litter/mate/line ('room' REMOVED, slot+line ADDED w/ subject_slot_id/subject_line_id FKs), UI stays TASK-centric (/tasks route + Tasks nav KEPT, "case" internal); plan §4 REFINED bullet + tasks P0.9 reworded
2026-09-14 tasks: batch = ONE case : N mice (SUPERSEDES batch=N-cases 2026-09-13, user-created batches only — SOP generators single-subject, untouched) — single case_id, one current_status, all members advance together (NO per-mouse progress); additive migration 0024_case_mice.sql: NEW subject_kind 'mice' (all six subject FKs NULL) + case_mice(case_id,mouse_id) join (partial-unique WHERE deleted_at IS NULL, case_id/mouse_id indexes), cases CHECKs widened w/ 'mice' branch, single-subject cases UNCHANGED; grid badges now DERIVED from case store (hardcoded MouseCell.activeTasks REMOVED; badge iff member of OPEN todo/doing case via subject_mouse_id or case_mice, grouped by signal, memoized Map<metaId,tags[]> O(mice+members)); mock now schema-faithful + id-keyed by metaId not label (fixes F5AYL dup-rendered-id drawer bug, metaId 102 vs 402); batch renders ONE card w/ N-member list, /tasks needs no grouping; tasks P0.9 Wave 3 [~]
