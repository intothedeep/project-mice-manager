# 01.status — Lopez-Juarez Mouse-Colony Automation

> Completed items live in `_docs/archive/` (cold storage — do not read unless
> investigating history). This doc holds active + planned work only.

2026-09-04→09-07 DB schema + ETL scope era (P0.2 schema, R1–R25 fold, migrations 0001-0020 shipped) ROLLED OFF → _docs/archive/status.2026-09-04_07-schema.md  <!-- ARCHIVE: history-only -->
2026-09-07→09-09 client mock-UI era (Cage-grid → full hierarchy, shadcn/Tailwind, Task bin, Mouse drawer, Upcoming, colour channels) ROLLED OFF → _docs/archive/status.2026-09-07_09-client-mock.md  <!-- ARCHIVE: history-only -->
2026-09-12 client colour + doc-fold era (multi-channel identity colours, DOB life-stage + slot-overcrowding channels, unified nested-column body + scope-highlight, genotype-first line rail; docs: breeding-date/tissue-genotyping placement, P2 offline PWA, CRUD/colour-spike ratifications, R11–R25 fold) ROLLED OFF → _docs/archive/status.2026-09-12_client-colour-and-folds.md  <!-- ARCHIVE: history-only -->
2026-09-13 tasks: adopt CASES+TASK_EVENTS model — cases(stable identity: case_type/signal/subject FKs+NEW subject_mate_id/gen_key/current_status cache/due_date/created_by) has-many append-only task_events(status,actor,note,ts), SUPERSEDES tasks origin_task_id versioning FOR TASKS ONLY (notes/mates/mice keep origin-id+CAS); lifecycle todo→doing→done→verified, cancelled side-exit (doing=staff claim); gen_key partial-unique WHERE deleted_at IS NULL on immutable cases resolves R10 no-UNIQUE-on-versioned-rows (key=ruleKey+origin fact id e.g. plug_check:mate=<id>, never run date; survives cancel = no regen); overdue DERIVED at read (todo/doing && due_date<today → coral thick border); system user for auto-gen created_by; SOP automation = hard-coded plug_check+genotyping generators behind pure TaskGenerator seam + event runner (genotyping reads genes/mice_genes, mouse_events dropped 0016; offsets stay task_offset_rules), user macro engine DEFERRED P1+ (later = one more TaskGenerator); plan §4 Task model v2 + tasks P0.9 (Wave 1 [~] 0021+DTOs, Wave 2 server/client)
2026-09-13 tasks: REFINE case/task model — task_events RENAMED to child `tasks` (IMMUTABLE append-only STATUS RECORDS under a case: status+case_id+actor+note+created_at, status fixed at creation = audit trail), cases keep MUTABLE current_status rollup (advance = INSERT task + UPDATE rollup, ONE service tx), create work = case + first todo task (SOP generators same), batch over N mice = N cases each w/ own first task, subject_kind = mouse/cage/slot/litter/mate/line ('room' REMOVED, slot+line ADDED w/ subject_slot_id/subject_line_id FKs), UI stays TASK-centric (/tasks route + Tasks nav KEPT, "case" internal); plan §4 REFINED bullet + tasks P0.9 reworded
2026-09-14 docs: ratify APPEND-ONLY EVERYWHERE (plan §1) — every update APPENDS a new row, never edits in place → append/version rows ARE the change-tracking audit, NO separate audit_logs write needed (audit_logs table kept only as optional cross-table who-did-what); case status already this (transition = appended immutable `tasks` row; current_status = derived cache); editable case fields (due_date/assignee) will follow the SAME append pattern; mock `setTaskStatus` already appends = the audit
2026-09-14 tasks: batch = ONE case : N mice (SUPERSEDES batch=N-cases 2026-09-13, user-created batches only — SOP generators single-subject, untouched) — single case_id, one current_status, all members advance together (NO per-mouse progress); additive migration 0024_case_mice.sql: NEW subject_kind 'mice' (all six subject FKs NULL) + case_mice(case_id,mouse_id) join (partial-unique WHERE deleted_at IS NULL, case_id/mouse_id indexes), cases CHECKs widened w/ 'mice' branch, single-subject cases UNCHANGED; grid badges now DERIVED from case store (hardcoded MouseCell.activeTasks REMOVED; badge iff member of OPEN todo/doing case via subject_mouse_id or case_mice, grouped by signal, memoized Map<metaId,tags[]> O(mice+members)); mock now schema-faithful + id-keyed by metaId not label (fixes F5AYL dup-rendered-id drawer bug, metaId 102 vs 402); batch renders ONE card w/ N-member list, /tasks needs no grouping; tasks P0.9 Wave 3 [~]
2026-09-14 client: quick-create case from cage grid [~] — right-click MOUSE cell → case-type menu (TASK_TYPES subjectKind='mouse'), one-click case + first todo task (no dialog), signal='instruction', via BATCH addTask path (N=1) so badge lights; v1 single-mouse, required fields skipped; v2 selection-batch / v3 node right-click deferred (SUPERSEDES right-click→SIGNAL-menu framing)
2026-09-14 tasks: genotyping `.N` re-clip label = read-time DERIVED (Option C; NO stored column, R12-safe) — N = count of mouse's done/verified 'Tissue collection' cases (deleted masked, case_mice/mice[] membership counted; physical re-clip only, NOT requests/PCR re-runs); N≤1 bare, N=2 '.2', N=3 '.3'; cancel = no retroactive renumber; mock M4BCW.2 merged to SAME mouse (metaId 101, derived suffix; F5AYL 402 stays distinct); +N vs .N STILL OPEN (professor); mock v1 task added tasks P0.9 [~]
2026-09-14 client: PLUG/DELIV/TISSUE/GENOTYPING date-cell colour = read-time join to case store (lib/dateSignal buildDateCaseIndex + lib/signal dateColorOf) — open+instruction→red (deadline), open+plan→blue (planned), done/verified→normal ink (completion date), cancelled→struck; date = case done-date when closed else dueDate; tissue/genotyping keyed by subjectMouseId/mice, plug/deliv = mate-cycle → seeded onto DAM metaId in mock (TODO real subject_mate_id→dam); taskTypes Plug check/Birth·delivery subjectKind cage→mate; no stored colour/date (compute-at-read)
