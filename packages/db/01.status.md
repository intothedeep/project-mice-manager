2026-09-04 policy: TESTS SUSPENDED until development done (user) — no test files, no jest runs; @repo/jest-config kept deliberately unused, not dead code; verify by deliberate breakage meanwhile (CLAUDE.md §19 overridden, plan §2, P0.3 test tasks marked suspended)
2026-09-04 run: app boots — server /health 200 {"status":"ok"}, client 200 renders "Colony Automation", @repo/db pool sees 20 tables in colony_dev
