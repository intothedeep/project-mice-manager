# P0.7-c — genotype as GENE ROWS — tasks

> Split from `PLAN.md` (rules/docs.md §1), via `TASKS.md` → `docs/phases/p0.7.tasks.md`.
> The stub is the truth if the two disagree; update both in the same edit.
> **SIZE, 2026-09-23 — ~412 lines, past `rules/docs.md` §1's 400 hard line;
> NOT split, and here is why (§1 requires the reason in the doc).** What is
> left after the earlier split is ONE sub-phase: three closed tasks, the two
> prerequisites their closure produced, and the allele-order resolution + the
> `SCHEMA.md` finding that every one of those tasks cites. Cutting anywhere
> separates a task from the evidence that closed it or from the ruling that
> re-aimed it — shared private context, which is exactly the artificial seam
> §1's escape hatch names. The real remedy is §2, not §1: when tasks 5 and 6
> close, the completed detail (tasks 1, 2, 4 and the resolution record) archives
> to `_archive/tasks.p0.7-c.md` and this file collapses to the open items, the
> way P0.7-b's did. Do not archive before then — §2 forbids archiving a set
> with open work.
>
> Split out 2026-09-23, the day this sub-phase opened: adding it inline took
> `docs/phases/p0.7.tasks.md` to ~409 lines, past docs.md §1's 400 HARD line,
> and P0.7-c shares no private context with the six base P0-b tasks or the
> AC-defect PATTERN block it sat between — so the "artificial seam" escape
> hatch does not apply. Destination written and read back before the cut (§2).
> The cut text and this file are NOT byte-identical on purpose: three inherited
> numbers/symbols were re-grepped and corrected in the move (call-site count,
> the `byMetaId` symbol, the ParentCell fixture count) and two AC wordings were
> restated away from bare-identifier greps. Both changes are marked below.

> Same defect as the stored `mouseLabel` that P0.7-b's 9a/9b removed: a
> COMPOSED STRING was stored on `MouseCell` and could drift from the rows it
> claims to summarise. The genotype now lives as `MouseCell.genes: GeneRef[]`
> and is composed at read by `lib/genotype.ts genotypeOf`.
> **VERIFICATION PROVENANCE, updated 2026-09-23 — say which tick has what.**
> `ca216c2` (task 1) now has an INDEPENDENT REVIEWER **PASS**: the reviewer
> re-derived the equality itself rather than trusting the commit (parsed
> `ca216c2^`'s fixture, ran `genotypeOf` over `ca216c2`'s →
> `mice=22 genotype mismatches=0 color mismatches=0 distinct forms=8`) and
> forced UNCACHED gates after spotting that the first run replayed from cache
> (`turbo run --force`: check-types 5/5, lint 0 errors, build 5/5); all 8 claims
> PASS. Verdict file:
> `/private/tmp/claude-501/-Users-tio-Documents-project-lopez-auto/fec5d5ae-bcba-44fd-87dc-38c3ae39a465/scratchpad/verdict.md`.
> `3932157` (task 2) and `11d0322` (task 4 + the allele fix) have MEASURED
> DEVELOPER EVIDENCE but NO independent reviewer pass — recorded that way on
> purpose; do not read their numbers as a verdict. Every criterion marked
> VERIFIED below was checked against the SOURCE by this pass.

- [x] **1 — `MouseCell.genotype` (stored composed string) → `genes: GeneRef[]`,
      composed at read (`ca216c2`).** `lib/genotype.ts` is the sole composer;
      migration `0029_mice_genes_alleles.sql` adds `mice_genes.allele_pat` /
      `allele_mat`.
      AC (1) VERIFIED — `packages/types/src/grid.ts:155` declares
      `genes: GeneRef[]` on `MouseCell` and `MouseCell` carries NO `genotype:`
      key declaration. (Scoped to `MouseCell` ON PURPOSE: the only surviving
      `genotype:` declaration under `packages/types/src` is
      `OutsideParentCell.genotype` (`grid.ts:106`), which is legitimate — an
      outside parent has no `MouseCell` to compose from, plan §5 Q46 option C.
      An unscoped "returns 0" grep would be the FIFTH-occurrence trap in
      `p0.7.tasks.md`'s PATTERN block.)
      AC (2) VERIFIED — every rendered genotype goes through `genotypeOf`
      (`lib/genotype.ts:41`): `gridFilter.ts:26`, EIGHT sites in
      `ColonyGridView.client.tsx` (`:195`, `:234`, `:314`, `:393`, `:567`,
      `:903`, `:1986`, `:1990`), `IdentitySection.client.tsx:203`, and the
      fixture's own colour key `genoOf` (`getColonyGrid.mock.api.ts:86`) — no
      second hand-written composer. (Count RE-GREPPED at the split, 2026-09-23:
      it read "6 sites" when first written, which is this block's own lesson.)
      AC (3) VERIFIED — `0029:35-37` adds BOTH columns TEXT, NULLABLE, with no
      DEFAULT, no CHECK and no enum.
      AC (4) VERIFIED — NULL/NULL and `'+'/'+'` are different facts on screen:
      `genotype.ts:30-31` renders the bare code when both are NULL, and the
      fixture proves both arms live (`G_WT` → `WT`, `G_NF1_PLUS_PLUS` →
      `Nf1 +/+`).
      AC (5) REPORTED, not re-run — exact equality over the 22 fixture mice
      (`pupNumber:` count = 22, VERIFIED), 0 mismatches against the pre-commit
      strings.
      CARRIED FORWARD, but as a MEMO, not a task: the change removed the stored
      composed STRING and left a stored composed COLOUR on the in-grid parent.
      `rules/docs.md` §7 would make that unmet criterion its own `[ ]`; the
      OWNER RULED otherwise on 2026-09-23 — *"whats the problem? leave a
      memo."* — so it is recorded, findable and unscheduled under MEMO below.
      The §7 bend is deliberate and named here so it is not read as an
      oversight; the owner outranks the rule (`rules/core.md` authority order).

- [x] **2 — `WT` is a real litter code; `parseLitterCode` was rejecting it
      (`3932157`).** It broke the Add-mouse litter dropdown AND the validation
      gate.
      AC (1) VERIFIED — `apps/colony_client_web/lib/litterCode.ts:23` is
      `const LITTER_CODE_RE = /^([A-Z]{3,5}|WT)$/`.
      AC (2) VERIFIED — the fixture really does use it as a litter code:
      `litterCode: 'WT'` occurs on 10 of the 22 seed mice
      (`getColonyGrid.mock.api.ts`), so before this commit those 10 contributed
      nothing to `useLitterCodes` / the counter scan.
      AC (3) VERIFIED — the widening is an ALTERNATION, not a length change:
      the regex still rejects any other two-letter code (`AB`, `ZZ`), which is
      the property `litterCode.ts:20` claims.
      AC (4) REPORTED, not re-run — the dropdown lists `WT` and the add-mouse
      validation gate accepts it.

> **Task number 3 is deliberately absent.** The in-grid parent tint drift was
> going to be task 3; the OWNER ruled it a MEMO instead (2026-09-23). The
> number is not reused, so nothing looks lost — see MEMO below.

- [x] **4 — `genes.sort_key` + migration `0030`; `mice_genes.order_index`
      DROPPED. SHIPPED `11d0322`** (developer evidence, no independent reviewer
      pass — see the provenance note above). Closing evidence, all re-checked
      against source by this pass:
      AC (1) MET with a NAME CORRECTION — the file is
      `packages/db/migrations/0030_gene_sort_key.sql` (singular `gene`), not the
      `0030_genes_sort_key.sql` this task predicted; seed values are exactly
      PlpCre 10, Nf1 20, Ai14 30, ccEGFP 40, WT 50 (`:36-42`), gaps of 10, and
      the column is `NOT NULL` with no default (`:44-45`) so a surviving
      0014-era code such as `'Nf1 f/+'` makes the promotion FAIL LOUDLY instead
      of silently nulling.
      AC (2) MET — `GeneRef` (`grid.ts:60-81`) declares `sortKey` and no
      `orderIndex:` key declaration survives under `packages/types/src`.
      AC (3) MET — `mintGeneRefs` copies `sortKey` FROM THE CATALOGUE
      (`genotype.ts:83 geneSortKey(code)`), not from its map index, and
      `inOrder` sorts on `a.sortKey - b.sortKey` (`:46`).
      AC (4) MET by FILE READ, not by a live probe (no Postgres this session —
      recorded the way 8d's migration AC was): `0030:64` drops
      `mice_genes.order_index`, and `:75-77` creates
      `mice_genes_mouse_gene_key ON mice_genes (mouse_id, gene_id)
      WHERE deleted_at IS NULL`. The old `mice_genes_order_key` goes with the
      column.
      AC (5) EXIT BAR MET per developer evidence — 22 rows, 0 mismatches, across
      BOTH the sort_key change and the allele swap. **Keep the trap this AC
      nearly walked into:** a byte-identical bar preserves whatever it is
      anchored to, INCLUDING a defect. Had the allele inversion not been caught
      first, this bar would have certified the paternal-first strings as
      "correct" — which is why the fixture rows were SWAPPED (meaning corrected)
      rather than reordered, leaving the rendered string identical on purpose.
      AC (6) **NOT MET → carried forward as its own `[ ]` (docs.md §7)**, task 5
      below: the three stale `order_index` references are still stale, and the
      scenario is now actively broken.
      AC (7) INAPPLICABLE, not met-or-unmet — there is no database (owner,
      *"no db yet"*, see the NO DATABASE record below), so there is no
      `schema_migrations` table for `0030` to be absent from. What the AC was
      really guarding still holds: `SCHEMA.md` is unchanged by this task.
      ALSO SHIPPED, both predicted by this task's scope and neither in the
      original dispatch: `Gene` + the `GENES` mock gained `sortKey`
      (`getGenes.mock.api.ts:25-34`), and `GENE_CODES` is now DERIVED —
      `GENE_CATALOG_CODES.filter(...)` (`lib/taskTypes.ts:14`) — closing the
      hand-written drift this file recorded below as a hazard. A reviewer found
      that literal had already drifted silently.
      ORIGINAL SCOPE AND RATIONALE, kept because the "why" is not in the diff:
      WHY `sort_key` and not `order_index`: `0007_breeding_and_records.sql:12-14`
      says of `order_index`, in its own words, that it is "the TOTAL ORDER that
      makes rendering deterministic: string_agg(marker_text, ';' ORDER BY
      order_index)" and that "This is NOT a biological claim about marker
      precedence" — VERIFIED against the file. That is exactly the job
      `sort_key` takes over, but per GENE (catalogue-wide) instead of per
      mouse-row.
      WHY `order_index` can go: its second role — position in the imported
      source string — is redundant with `mouse_meta.raw_genotype`, which keeps
      the original verbatim (VERIFIED: `0002_core_tables.sql:297`,
      `SCHEMA.md:294`).
      Scope:
      (a) `0030_genes_sort_key.sql` (`0029` is the current highest — VERIFIED
          by listing `packages/db/migrations/`). `genes` gains `sort_key`,
          seeded WITH GAPS — PlpCre 10, Nf1 20, Ai14 30, ccEGFP 40, WT 50 — so a
          future gene can be inserted between two existing ones without
          renumbering. NOTE: **no migration seeds `genes` today** (grep
          `INSERT INTO genes` hits only `scenarios/flow-weekly-cycle.sql:36`;
          `0014`'s header defers population to "a data migration"), so the seed
          must be written INSERT-or-UPDATE by `code` and must not assume the
          five rows exist.
      (b) `genotypeOf` sorts by `sortKey`, not `orderIndex`
          (`lib/genotype.ts:37-39` is the comparator to change).
      (c) `mice_genes.order_index` DROPPED; `GeneRef` (`grid.ts:70-73`) drops
          `orderIndex` and gains `sortKey`. ENTAILED, and the dispatch is silent
          on it: `mintGeneRefs` (`genotype.ts:64-77`) sets `orderIndex: i` from
          PICK ORDER, so after this change it must read the value from the
          CATALOGUE — which means `Gene` (`packages/types/src/gene.ts`) and the
          `GENES` mock (`apis/getGenes.mock.api.ts:19-25`) gain `sortKey` too.
      (d) the unique index `mice_genes_order_key (mouse_id, order_index)
          WHERE deleted_at IS NULL` (VERIFIED live, `SCHEMA.md:264`) is REPLACED
          by `(mouse_id, gene_id) WHERE deleted_at IS NULL` — the invariant that
          actually matters (a mouse cannot carry the same gene twice), which
          also closes the `[Nf1, Nf1]` hole.
      (e) **WRITTEN, NOT RUN against any database.** This is a real column DROP:
          if `colony_dev` holds imported rows, the source-string POSITION data is
          lost (recoverable from `mouse_meta.raw_genotype`, which is why (c) is
          safe at all).
      (f) FALLOUT the dispatch does not name, found by grep this pass:
          `packages/db/scenarios/flow-weekly-cycle.sql:37,41` INSERTs
          `order_index` and ORDER BYs it, `scenarios/README.md:22` documents
          `string_agg(g.code, ';' ORDER BY mg.order_index)` as the contract, and
          `docs/phases/p0.4.tasks.md:19` carries "order_index preserved" in the
          ETL column map. All three go stale the moment 0030 lands. (Same
          scenario line 36 also still inserts the pre-`0029` baked code
          `'Nf1 f/+'` — recorded, separate defect, not this task's to fix
          unless the same edit touches it.)
      (g) **INAPPLICABLE — corrected 2026-09-23 (owner: *"no db yet"*): there
          is no `colony_dev` and never was, so there is nothing to apply and
          nothing to regenerate. The premise below came from `SCHEMA.md`'s false
          "generated from the live `colony_dev`" banner — see the NO DATABASE
          record further down. Kept as written so the wrong inference stays
          visible.** DEPENDS ON `0029` BEING APPLIED. `SCHEMA.md:251-266` shows `mice_genes`
          with NO `allele_pat`/`allele_mat`, so either 0029 is unapplied to
          `colony_dev` or SCHEMA.md was not regenerated — settle which BEFORE
          running 0030 anywhere.
      ACs AS WRITTEN 2026-09-23 (their closure is recorded at the top of this
      task; kept verbatim so the bar can be re-read against the diff):
      (1) `packages/db/migrations/0030_*.sql` exists and its seed values are
      exactly 10/20/30/40/50 for PlpCre/Nf1/Ai14/ccEGFP/WT, every neighbouring
      pair at least 10 apart (a consecutive 1..5 seeding FAILS this);
      (2) no `orderIndex:` KEY DECLARATION remains under `packages/types/src`
      and `GeneRef` declares `sortKey` — the declaration, not the bare word,
      because a comment explaining that `sort_key` replaced `order_index`
      legitimately re-introduces the word (9d's RESTATED form; the FIFTH
      occurrence in `p0.7.tasks.md`'s PATTERN block is exactly this mistake);
      (3) no `.orderIndex` PROPERTY ACCESS remains under
      `apps/colony_client_web` (excluding `.next/`), and `mintGeneRefs` derives
      no ordering value from its map index — the value comes from the catalogue;
      (4) `0030` contains a `DROP COLUMN order_index` on `mice_genes` and
      creates the `(mouse_id, gene_id) WHERE deleted_at IS NULL` unique index;
      inserting a second live row for the same (mouse, gene) is rejected by that
      index (file-read only if no live Postgres — state which was done, the way
      8d's migration AC was recorded);
      (5) EXIT BAR, same as `ca216c2`'s: the rendered genotype is BYTE-IDENTICAL
      for all 22 fixture mice before and after. Falsifiable, not vacuous: the two
      multi-gene sets (`G_PLPCRE_AI14_PLUS_PLUS`, `G_PLPCRE_NF1_F_PLUS`) both put
      PlpCre first, which `sort_key` 10 reproduces — reorder either fixture and
      the bar breaks;
      (6) `flow-weekly-cycle.sql`, `scenarios/README.md` and
      `docs/phases/p0.4.tasks.md:19` carry no live `mice_genes.order_index`
      reference;
      (7) NOT RUN: no `schema_migrations` row for 0030 and `SCHEMA.md` is
      unchanged by this task (regen belongs to whoever applies it).
      Gate for a task that ships no runnable DB change: `pnpm check-types` +
      `pnpm lint` (0 errors).

### BEFORE THE FIRST `createdb` — tasks 5 and 6

> Both exist because the owner's *"no db yet"* (2026-09-23) makes them
> PREREQUISITES rather than maintenance. **KEPT AS TWO CHECKBOXES, decided
> deliberately:** they share one trigger (the first time anyone stands a
> database up) but not one deliverable — 5 is a mechanical fix with a
> falsifiable AC and no decision in it, 6 is an OWNER DECISION about where the
> catalogue seed lives, which nobody can close by editing a file. Merging them
> would put one checkbox over two deliverables, the defect this phase's own
> history keeps recording. Whoever does the first `createdb` should read them
> together.

- [ ] **5 — `scenarios/flow-weekly-cycle.sql` is a TRAP LAID FOR THE FIRST
      `createdb`** (task 4's unmet AC (6), carried forward per docs.md §7;
      RE-AIMED 2026-09-23 after the owner's *"no db yet"*). It breaks nothing
      today — there is nothing to run it against — which is exactly why it will
      be discovered by the person least able to diagnose it: the one bringing
      the schema up for the first time. What it does when that happens:
      `packages/db/scenarios/flow-weekly-cycle.sql:37` INSERTs
      `mice_genes (mouse_id, order_index, gene_id)` and `:39,:41` aggregate
      `string_agg(g.code, ';' ORDER BY mg.order_index)` — both reference a
      column `0030` drops, so the scenario FAILS the first time it is run
      against a schema built from `migrations/`;
      `packages/db/scenarios/README.md:22` documents the same aggregate
      as THE genotype contract; `docs/phases/p0.4.tasks.md:19` carries
      "order_index preserved" in the ETL column map, which now instructs a
      future importer to populate a dropped column.
      SEPARATE DEFECT IN THE SAME FILE, fold it in only if the edit is already
      there: `flow-weekly-cycle.sql:36` still seeds the pre-`0029` baked code
      `INSERT INTO genes (code) VALUES ('Nf1 f/+')` — a 0014-era code, which is
      exactly the row `0030`'s `SET NOT NULL` is designed to fail loudly on.
      AC: grep `order_index` under `packages/db/scenarios/` returns 0; the
      scenario's genotype aggregate orders by the catalogue's `sort_key`
      (join to `genes`), and its `mice_genes` INSERT names only columns that
      exist after `0030`; `docs/phases/p0.4.tasks.md:19` no longer tells the
      importer to preserve `order_index`; `packages/db/scenarios/README.md`
      states the `sort_key` contract. **The AC is FILE-LEVEL on purpose** —
      there is no database to run the scenario against, and inventing a
      "run it and see" criterion would be unmeetable until the very
      `createdb` this task is protecting.

- [ ] **6 — BEFORE THE FIRST `createdb`, owner-visible, no code decision
      here: nothing seeds `genes`.** Re-aimed 2026-09-23 with task 5 — this is
      not a gap in a running system, it is part of what must exist before a
      database can be stood up at all, because a catalogue with no rows makes
      `genes.sort_key` a column nobody can use.
      `0030`'s own header says it (`:29-30`) — "Nothing in migrations/ seeds
      `genes`, so on a fresh database the UPDATE matches zero rows and the
      constraint holds trivially" — and grep confirms the only
      `INSERT INTO genes` in the repo is the scenario's. CONSEQUENCE: the five
      `sort_key` values (PlpCre 10 … WT 50) live ONLY in the mock catalogue
      (`apis/getGenes.mock.api.ts:25-34`) and in `0030`'s `CASE`; the first
      real database gets the column and no rows to carry it, and any code
      added outside those five FAILS the `NOT NULL` promotion — including the
      `'Nf1 f/+'` that task 5's scenario would insert, which is how the two
      items meet. Not scheduled here because WHERE the catalogue seed belongs (a
      seed migration, the P0.5 seed generator, or the import) is the owner's
      call, not this file's. Closing condition: the owner names the home, or
      rules that the mock catalogue is enough for MVP1.

## MEMO — the in-grid parent cell is HALF-MIGRATED (owner ruling 2026-09-23)

**Not a task. No AC, no date, no target, and nobody is picking it up next** —
the owner's ruling is *"whats the problem? leave a memo."* It is written down
so anyone touching parent cells knows, not to schedule work.

`InGridParentCell.genotypeColor` (`grid.ts:98-101`) is still a STORED copy,
while the genotype STRING on that same cell is now composed at read time
(`ca216c2`). The two halves of one cell have different freshness.

REPRODUCTION, verified by a reviewer against the real `updateMouse`: add a gene
to mouse 101 (the father of 103). Mouse 101's OWN cell correctly goes no-fill —
`lib/updateMouse.ts` nulls its own `genotypeColor` on any gene change. But
nothing syncs `parents[].genotypeColor`, so row 103's father sub-cell keeps
painting the old teal `#0d9488` behind the NEW text `Nf1 f/+;WT`. The same mouse
appears in two colours on one screen, and the tint claims a genotype that no
longer exists.

NOT URGENT: mock-era only. The colour comes from the fixture today and the
server resolves it in the server era; the data is value-correct — only the tint
is stale.

WORTH KEEPING: it is the SAME defect class this whole arc removed — a stored
copy of something derivable. The string half was fixed and the colour half was
not, so the cell is half-migrated.

FILED ALONGSIDE the deferred gene-level colour model (`plan §5 Q47`, DECIDED in
principle and deliberately not implemented): both live in the same design area
and will most likely be settled together.

## RECORDED, NOT SCHEDULED (2026-09-23) — no task, no AC

- **`GeneRef[]` is a MUTABLE array type and `G_WT` is ONE array shared by 12
  fixture mice** (VERIFIED: `getColonyGrid.mock.api.ts:58` + 12 `genes: G_WT`
  sites). Value-correct today — every mutation builds new rows via
  `mintGeneRefs` and nothing splices a `genes` array in place — and it is the
  SAME CLASS as the seed aliasing P0.7-b's 8g examined and ruled SAFE. Cheapest
  future hardening if it ever matters: `readonly GeneRef[]`.
- **`getGenes()` (`apis/getGenes.mock.api.ts:31`) is exported and has ZERO call
  sites.** Deliberate, not dead code: it matches the established seam —
  `getColonyGrid()` (`:940`) and `getTasks()` (`:809`) are likewise defined and
  uncalled, each named as the FUTURE real fetcher so the mock→real swap is a
  single-file change. VERIFIED by grep over source (excluding `.next/`).
- **TWO gene option lists ON PURPOSE:** `GENE_CODES` (`lib/taskTypes.ts:7`,
  FOUR codes, no WT) is the "genes to CHECK" list a genotyping case picks from
  (`NewTaskDialog.client.tsx:296-300` says so in code); `GENE_CATALOG_CODES`
  (`apis/getGenes.mock.api.ts:28`, FIVE codes, WITH WT) is the catalogue used to
  ASSIGN a genotype (`AddMouseDialog:535`, `IdentitySection:175`). The drift
  hazard this bullet named — `GENE_CODES` hand-written rather than derived — is
  **CLOSED as of `11d0322`**: it is now `GENE_CATALOG_CODES.filter(...)`
  (`lib/taskTypes.ts:14`), after a reviewer found the standalone literal had
  already drifted. The two-list DESIGN stands; only the duplication is gone.
- **THERE IS NO DATABASE — owner, 2026-09-23: *"no db yet."*** This CORRECTS
  what this file, a reviewer and the main session all said earlier today.
  `colony_dev` does not exist. Every migration `0001`–`0030` is a PAPER
  CONTRACT: nothing to apply, nothing to regenerate, and no code↔DB drift,
  because there is no DB side. Task 4's scope (g) ("depends on 0029 being
  applied") and its AC (7) are therefore about a database that does not exist —
  they are not unmet, they are INAPPLICABLE, and the task's close is unaffected
  (every criterion it actually rested on was a file read).
- **`SCHEMA.md` IS A FOSSIL AND ITS OWN BANNER IS FALSE — recorded, and NOT
  edited here** (the owner is deciding whether to correct the banner or keep it
  and record the hazard; no answer yet, and this pass does not touch the file).
  `packages/db/SCHEMA.md:3` asserts "GENERATED by
  `packages/db/scripts/schema-doc.sh` from the live `colony_dev`". VERIFIED this
  pass: it carries `pup_number_offsets` (`0027`, `:357`) and ZERO occurrences of
  `untagged` (`0028`), so it stopped at `0027` — it is a snapshot of a database
  that is gone, asserting that it reflects a live one.
  **THE FINDING IS BIGGER THAN THE CORRECTIONS IT CAUSED:** a GENERATED file
  whose PROVENANCE LINE OUTLIVES ITS SOURCE becomes an authoritative-looking
  lie, and this one misled THREE separate readers in ONE SESSION about the exact
  thing it exists to settle — a reviewer read it as "0029 unapplied", this pass
  read it as "either 0029 unapplied or SCHEMA.md not regenerated" (correctly
  uncertain, wrong premise either way), and the main session collapsed that
  disjunction into the wrong branch and told the owner 0029 had not been
  applied. Note the shape: the uncertainty was expressed honestly and STILL
  produced a wrong conclusion downstream, because every branch of the
  disjunction presupposed a database. A generated artefact must either be
  regenerable on demand or say what it is a fossil OF; this one says the
  opposite. It is the DRIFT class from the PATTERN block (`p0.7.tasks.md`) —
  true when written, false once its referent went — at the level of a whole
  file rather than a sentence.

## ALLELE ORDER — RESOLVED 2026-09-23 (owner): MATERNAL FIRST, 09-17 STANDS

**The owner's answer, put to them with both readings: *"어머니 먼저 — 9/17
결정이 살아있다"*** (maternal first; the 2026-09-17 decision is alive). So
`0029` had INVERTED a standing decision; the owner did NOT reverse it on 09-23.
Fixed and committed in **`11d0322`** (developer evidence, no independent
reviewer pass).

WHAT THE HISTORY WAS — kept deliberately, not erased, because the useful part
is how it was caught:
- `STATUS.md` 2026-09-17 recorded the owner deciding the pair is PARENT OF
  ORIGIN ordered, maternal then paternal, with the columns to be named
  `allele_maternal`/`allele_paternal` precisely because "a generic name is how
  someone fills an ordered pair backwards".
- `0029` shipped saying the opposite — "the paternal and maternal allele, in
  that order" — and the DTO and renderer followed it, so every heterozygote in
  the fixture was displayed with its parents swapped.
- It was caught by a REVIEWER comparing the code against
  `docs/phases/p1.genotyping.plan.md:59`, and escalated rather than guessed at.

WHAT LANDED (VERIFIED against source by this pass):
- `0029` corrected IN PLACE, not compensated for by `0030` — it had never
  touched a database, so there is no archaeology to preserve. Columns are now
  declared `allele_mat, allele_pat` (`0029:45-47`) and the header states the
  left allele is MATERNAL, citing 2026-09-17 (`:15-24`).
- `renderGene` emits `mat/pat` (`genotype.ts:37-38`); `GeneRef` declares
  `alleleMat` first (`grid.ts:62-74`).
- Fixture rows were **SWAPPED, not reordered** (`getColonyGrid.mock.api.ts:54-55,
  :70-90`), so the rendered string is unchanged and only the MEANING is
  corrected.
- The rule is now stated at the migration header AND at `renderGene`, not only
  in a phase doc — it had been inverted once already.

**THE GENERAL TRAP, worth keeping:** task 4's byte-identical exit bar would have
certified the paternal-first strings as "correct" if the inversion had not been
caught first. An equality bar preserves whatever it is anchored to, INCLUDING a
defect — which is why the fixture was swapped rather than reordered.

REVIEWER FINDINGS RECORDED WITH IT:
- `geneRefsEqual` compared by ARRAY POSITION while `genotypeOf` sorted — a
  server returning the same rows in another order would have read as a change
  and triggered a spurious write. FIXED in `11d0322`: it now sorts both sides
  through the same `inOrder()` and deliberately does not compare `sortKey`,
  which is a catalogue fact rather than part of the picked set
  (`genotype.ts:88-109`, VERIFIED).
- `CodeBadgeSelect` has no exclusivity logic, so deselect+reselect REORDERS a
  mouse's picks. **Now moot for everything that can be checked in source**: the
  rendered genotype sorts by `sortKey` (`genotype.ts:46`), change detection is
  order-insensitive (above), and `mintGeneRefs` copies `sortKey` from the
  catalogue rather than from pick order — so pick order no longer reaches the
  display or the write path. NOT moot for what the PICKER ITSELF shows, which
  is the open question recorded as `plan §5 Q49`.
