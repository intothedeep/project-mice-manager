# P0.3 Domain parsers — tasks

> ACTIVE detail for P0.3, sharded from `TASKS.md` (rules/docs.md §1).
> The root stub is the truth if the two disagree; update both in one edit.

### P0.3 Domain parsers (`packages/domain` — pure, no I/O) — P0-a

- [ ] Tiered mouse-ID parser (rev. 2026-09-04 R3): canonical
      `[MF]\d+[A-Z]{1,5}(tag-suffix)?(\.N)?` — parser accepts litter codes
      `^[A-Z]{1,5}$` (generator emits 3–5); tag-suffix = OPEN set: TEXT +
      known-mapping lookup (empty→foot/leg, e→ear, (toe)→toe, others
      possible), unknown suffix → warn NOT reject; `.N` = count of
      done/verified Tissue-collection cases (derived, never stored — plan §5
      Q43); bare no-sex NEWBORN-PUP variant (`1BIZ`) → NEW
      `unsexed_pup` disposition (non-warn, round-trips); other bare no-sex
      IDs (`1BIS`) → sex=U + warn, never guess; **`+N` variant — PROTOTYPE
      RULE (user, 2026-09-05): parse ONLY sex + leading pup_number + litter
      code.** `M4+10AZZ` → sex=M, pup_number=4, litter_code=AZZ. The `+N`
      chain (a pup-number offset assigned on transfer, plan §5 Q23/Q45) ~~is
      NOT parsed into a structured column here~~ <!-- SUPERSEDED 2026-09-17, see UPDATE below -->
      — these are ordinary single
      mice for the prototype and DO enter the natural key. The original string
      is preserved twice: byte-exact in `mouse_meta.raw_mouse_id`, and as a
      `notes` row on the mouse. ~~`+N` is left unparsed beyond the leading
      pup_number ONLY because its storage design (plan §5 Q45) is still
      pending — not because the notation itself is unresolved (Q2/Q23 are
      answered);~~ <!-- SUPERSEDED 2026-09-17: Q45 RESOLVED, storage landed --> junk `=>` → UNPARSED→error.
      **UPDATE 2026-09-17 (owner; plan §5 Q45 RESOLVED → `pup_number_offsets`,
      migration 0027; plan §4 storage bullet):** the `+N` chain IS now parsed.
      Grammar: the pup-number position is `<n>(?:\+(\d+))*` — the offset group
      is REPEATING. Output shape: `pup_number` = the LEADING number = the
      immutable BIRTH number (unchanged from the prototype rule, so nothing
      about the natural key moves); NEW `offsets: number[]` = every captured
      `+N`, in LABEL ORDER (left to right), each > 0; effective pup number =
      `pup_number + sum(offsets)` is DERIVED at read, never a parser output
      field and never stored. `M4+10AZZ` → pup_number=4, offsets=[10];
      `F1+6+8CF` → pup_number=1, offsets=[6, 8] (base 1, then +6, then +8 —
      two successive transfer bumps). Magnitudes are arbitrary (multiples of 10
      are convention, not grammar — do NOT validate them). `+` still lives in
      the PUP NUMBER only, never in the litter code. The `mouse_meta.raw_mouse_id`
      byte-exact copy STAYS (provenance of whichever row created the mouse —
      it is NOT a key, see P0.4 upsert task). P0.4 turns each `offsets[i]`
      into one `pup_number_offsets` row, inserted left-to-right.
      AC: 66/66 real-ID fixtures parse to the documented shape or route to a
      disposition listed in the FROZEN disposition table at
      packages/domain/fixtures/mouse-id.dispositions.md; zero silent
      fallbacks; `+N` fixtures (`M4+10AZZ`, `F10+1earAYY`, multi `F1+6+8CF`)
      all yield the LEADING pup_number with the raw label byte-preserved and a
      note carrying the original.
      AC (added 2026-09-17): the same three fixtures ALSO yield
      `offsets` = `[10]`, `[1]`, `[6, 8]` respectively, in label order; a label
      with no `+` yields `offsets = []` (never null); `M1+10A` and `M1A` parse
      to the SAME `(litter_code, pup_number)` = `(A, 1)` and differ ONLY in
      `offsets`; a `+0` or `+` with no digits routes to a disposition, never a
      silent drop.
      VERIFIED 2026-09-05: the leading-number rule yields 0 collisions across
      175 (litter, pup) pairs on Breeders, the only P0 import target. Other
      sheets show 146/3528 (4%) collisions of the form `M1+10A` vs `M1A` —
      they are out of P0 scope; Q2 is answered (sum reading, plan §5) but the
      offset-storage design (Q45) must land before those rows can import.
      UPDATE 2026-09-17: Q45 LANDED (migration 0027) — that blocker is
      cleared. What still gates those non-P0 rows is the question below: (are
      `M1A` and `M1+10A` in ONE sheet snapshot the same animal? — under the
      owner's model they FOLD into one mouse at import and the 146 collisions
      evaporate; not yet confirmed). P0/Breeders is unaffected either way.
      ADDED 2026-09-23 (owner, `check.md` C-4 — the P0.7 pup-ID spec was
      corrected the same day): the GENERATOR always emits a sex letter, so a
      freshly generated pup id is `U1BIZ`, never the bare `1BIZ`
      (`apps/colony_client_web/lib/mouseIdentity.ts:28,32`). This parser's
      canonical form above is `[MF]\d+…` and its bare arm is the
      `unsexed_pup` disposition — **neither covers a leading `U`**, so the
      grammar needs a `U` arm (sex=U, non-warn, round-trips) or generated ids
      will not parse. Recorded here rather than on the P0.7 task because this
      is the parser's contract; the bare `1BIZ` arm STAYS (sheet rows are
      genuinely written bare), and whether `unsexed_pup` should still be its
      disposition once a `U` arm exists rides plan Q4 with the rest.
      AC (added with it): `U1BIZ` parses to sex=U, pup_number=1,
      litter_code=BIZ with no warn; and for a `U`-prefixed fixture the
      round-trip holds against the SHIPPED composer — `parse(buildMouseLabel({
      sex, pupNumber, pupOffsets: [], litterCode, punches: [] }))` returns the
      same `(sex, pupNumber, litterCode)` (`lib/mouseIdentity.ts`; there is no
      `compose()` symbol — the mock-era name is `buildMouseLabel`, wrapped by
      `mouseLabelOf`).
      BLOCKED-BY: professor confirmation of the amended disposition set (plan
      Q4) — the fixture table must be frozen before this task starts.
- [ ] litter-code codec (pure, R9): encodeLitterCode(n)/decodeLitterCode(code) —
      bijective base-26 (A=1..Z=26). Used ONLY to GENERATE the next code
      across AZZ→BAA and ZZZ→AAAA rollovers (P0-b). CHANGED BY R14: the
      ordinal IS now persisted, as `litters.seq`, so issue order is a plain
      ORDER BY seq and the R9 length-first index is retired. The codec computes
      `seq` for IMPORTED codes; newly issued codes take it from
      `litter_code_seq`.
      AC: encode/decode round-trips for all `^[A-Z]{1,5}$` fixtures;
      seq(AZZ)+1 == seq(BAA); seq(ZZZ) < seq(AAAA); for the mixed-length
      fixture set {ZZZ, AAAA, AZZ, BAA, A, AB}, ORDER BY decodeLitterCode(code)
      differs from a plain lexicographic sort (which places 'AAAA' before
      'ZZZ') — that difference is exactly why the ordinal is stored.
      NOTE 2026-09-16: a MOCK-ERA implementation of this codec exists at
      `apps/colony_client_web/lib/litterCode.ts` (pure bijective base-26 +
      store counter; `useLitterCodes` latest-first + `peekNextLitterCode`) —
      shipped with add-mouse v1. FOLD it into `packages/domain` at server era
      (meet the `litters.seq` ACs above); do not rewrite blind.
- [ ] YYMMDD codec: 260405→2026-04-05; pin `20` century; validate MM/DD;
      invalid→raw-store + warn.
      AC: valid/invalid fixture table passes; invalid input never throws, returns
      warn disposition.
- [ ] Forward-fill helper: fill mouse-line/cage/slot down blocks (Breeders cols
      B/D/E inheritance — B resolves to the mouse_line entity, E to slot.label
      per plan §4); section-label rows ("To Breed", "Ps Breed.") →
      section_marker disposition (not mouse, not error).
      AC: fixture reproducing Breeders R22–R25 fills correctly; section rows emit
      section_marker.
- [ ] canTransition(role, from, to) pure fn: role→allowed-transition matrix
      (staff: open→done; professor: open→done, done→verified, done→open,
      open→cancelled, done→cancelled; admin: any→any; nobody exits
      verified/cancelled except admin) — placeholder matrix TBD-with-professor
      (plan Open Question 18). Fixture tests are SUSPENDED (CLAUDE.md §19) —
      build the pure fn now, write its tests in the end-of-development pass.
      AC: matrix defined in exactly one place; deterministic (pure, no I/O);
      every (role, from, to) combination is REPRESENTABLE in the matrix
      structure (the allowed/disallowed fixture table comes with the test pass).
      NOTE 2026-09-13: the state set is now todo/doing/done/verified/cancelled
      (plan §4 Task model v2, Q18 UPDATE) — fold the `doing` claim step into
      the matrix when built; the shape of the fn is unchanged.
- [ ] (OPTIONAL v0.1) Note-date extractor: `\b2[0-9]\d{4}\b` → note_event; keep
      original text, don't fix typos. raw_notes is stored verbatim regardless of
      whether this ships.
      AC: fixtures based on real notes (75/185 contain dates) pass; original text
      byte-identical in output.
- [ ] Parser test suite — **SUSPENDED until development is done (user directive
      2026-09-04; CLAUDE.md §19). Do NOT write this yet.** Deferred to a single
      end-of-development pass, not deleted: the fixture inventory below is the
      spec for that pass. Until then, parsers are validated by deliberate
      breakage against real inputs, not by a suite.
      (**Jest + ts-jest** via the reused `@repo/jest-config`
      — matches CLAUDE.md §19 and the sibling project; the ml brief's pytest
      reference is superseded, plan §2 discrepancy CLOSED): mouse_id (66 real fixtures
      incl. unsexed_pup), litter_code codec, yymmdd,
      forward_fill (+ note_date_extractor if built); mirror src; core parsers
      only, no I/O/SQL.
      AC: full suite green in one command; no test touches DB or filesystem beyond
      fixtures.
