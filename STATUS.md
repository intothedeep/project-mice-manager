# STATUS — Lopez-Juarez Mouse-Colony Automation

> Completed items live in `_archive/` (cold storage — do not read unless
> investigating history). This doc holds active + planned work only.

2026-09-04→09-07 DB schema + ETL scope era (P0.2 schema, R1–R25 fold, migrations 0001-0020 shipped) ROLLED OFF → _archive/status.2026-09-04_07-schema.md  <!-- ARCHIVE: history-only -->
2026-09-07→09-09 client mock-UI era (Cage-grid → full hierarchy, shadcn/Tailwind, Task bin, Mouse drawer, Upcoming, colour channels) ROLLED OFF → _archive/status.2026-09-07_09-client-mock.md  <!-- ARCHIVE: history-only -->
2026-09-12 client colour + doc-fold era (multi-channel identity colours, DOB life-stage + slot-overcrowding channels, unified nested-column body + scope-highlight, genotype-first line rail; docs: breeding-date/tissue-genotyping placement, P2 offline PWA, CRUD/colour-spike ratifications, R11–R25 fold) ROLLED OFF → _archive/status.2026-09-12_client-colour-and-folds.md  <!-- ARCHIVE: history-only -->
2026-09-13 tasks: adopt CASES+TASK_EVENTS model — cases(stable identity: case_type/signal/subject FKs+NEW subject_mate_id/gen_key/current_status cache/due_date/created_by) has-many append-only task_events(status,actor,note,ts), SUPERSEDES tasks origin_task_id versioning FOR TASKS ONLY (notes/mates/mice keep origin-id+CAS); lifecycle todo→doing→done→verified, cancelled side-exit (doing=staff claim); gen_key partial-unique WHERE deleted_at IS NULL on immutable cases resolves R10 no-UNIQUE-on-versioned-rows (key=ruleKey+origin fact id e.g. plug_check:mate=<id>, never run date; survives cancel = no regen); overdue DERIVED at read (todo/doing && due_date<today → coral thick border); system user for auto-gen created_by; SOP automation = hard-coded plug_check+genotyping generators behind pure TaskGenerator seam + event runner (genotyping reads genes/mice_genes, mouse_events dropped 0016; offsets stay task_offset_rules), user macro engine DEFERRED P1+ (later = one more TaskGenerator); plan §4 Task model v2 + tasks P0.9 (Wave 1 [~] 0021+DTOs, Wave 2 server/client)
2026-09-13 tasks: REFINE case/task model — task_events RENAMED to child `tasks` (IMMUTABLE append-only STATUS RECORDS under a case: status+case_id+actor+note+created_at, status fixed at creation = audit trail), cases keep MUTABLE current_status rollup (advance = INSERT task + UPDATE rollup, ONE service tx), create work = case + first todo task (SOP generators same), batch over N mice = N cases each w/ own first task, subject_kind = mouse/cage/slot/litter/mate/line ('room' REMOVED, slot+line ADDED w/ subject_slot_id/subject_line_id FKs), UI stays TASK-centric (/tasks route + Tasks nav KEPT, "case" internal); plan §4 REFINED bullet + tasks P0.9 reworded
2026-09-14 docs: ratify APPEND-ONLY EVERYWHERE (plan §1) — every update APPENDS a new row, never edits in place → append/version rows ARE the change-tracking audit, NO separate audit_logs write needed (audit_logs table kept only as optional cross-table who-did-what); case status already this (transition = appended immutable `tasks` row; current_status = derived cache); editable case fields (due_date/assignee) will follow the SAME append pattern; mock `setTaskStatus` already appends = the audit
2026-09-14 tasks: batch = ONE case : N mice (SUPERSEDES batch=N-cases 2026-09-13, user-created batches only — SOP generators single-subject, untouched) — single case_id, one current_status, all members advance together (NO per-mouse progress); additive migration 0024_case_mice.sql: NEW subject_kind 'mice' (all six subject FKs NULL) + case_mice(case_id,mouse_id) join (partial-unique WHERE deleted_at IS NULL, case_id/mouse_id indexes), cases CHECKs widened w/ 'mice' branch, single-subject cases UNCHANGED; grid badges now DERIVED from case store (hardcoded MouseCell.activeTasks REMOVED; badge iff member of OPEN todo/doing case via subject_mouse_id or case_mice, grouped by signal, memoized Map<metaId,tags[]> O(mice+members)); mock now schema-faithful + id-keyed by metaId not label (fixes F5AYL dup-rendered-id drawer bug, metaId 102 vs 402); batch renders ONE card w/ N-member list, /tasks needs no grouping; tasks P0.9 Wave 3 [~]
2026-09-14 client: quick-create case from cage grid [~] — right-click MOUSE cell → case-type menu (TASK_TYPES subjectKind='mouse'), one-click case + first todo task (no dialog), signal='instruction', via BATCH addTask path (N=1) so badge lights; v1 single-mouse, required fields skipped; v2 selection-batch / v3 node right-click deferred (SUPERSEDES right-click→SIGNAL-menu framing)
2026-09-14 tasks: genotyping `.N` re-clip label = read-time DERIVED (Option C; NO stored column, R12-safe) — N = count of mouse's done/verified 'Tissue collection' cases (deleted masked, case_mice/mice[] membership counted; physical re-clip only, NOT requests/PCR re-runs); N≤1 bare, N=2 '.2', N=3 '.3'; cancel = no retroactive renumber; mock M4BCW.2 merged to SAME mouse (metaId 101, derived suffix; F5AYL 402 stays distinct); +N vs .N STILL OPEN (professor); mock v1 task added tasks P0.9 [~]
2026-09-14 client: PLUG/DELIV/TISSUE/GENOTYPING date-cell colour = read-time join to case store (lib/dateSignal buildDateCaseIndex + lib/signal dateColorOf) — open+instruction→red (deadline), open+plan→blue (planned), done/verified→normal ink (completion date), cancelled→struck; date = case done-date when closed else dueDate; tissue/genotyping keyed by subjectMouseId/mice, plug/deliv = mate-cycle → seeded onto DAM metaId in mock (TODO real subject_mate_id→dam); taskTypes Plug check/Birth·delivery subjectKind cage→mate; no stored colour/date (compute-at-read)
2026-09-14 client: add-mouse v1 (single, user-created) [~] — colony grid migrated STATIC const → subscribable lib/mockColonyStore.ts (mirrors mockStore useSyncExternalStore) so adds appear immediately; addMouse + AddMouseDialog (sex default 'U', litterCode+pupNumber, dob, line, genotype opt→'?', cage+slot), NEW slot-label creation ALLOWED w/ GLOBAL dedupe (slots.label global-unique); = roadmap P0-b Record-Litter (manual/outside mouse = is_from_outside 1-pup litter shell, litter_id NOT NULL, ONE code path; renderedId composed at read); server-era swap = mouse_meta+mice+litter tx, DTOs unchanged; batch record-pups + follow-up tasks deferred v2, pedigree/case-link v3; tasks P0.7 [~]
2026-09-16 client: add-mouse modal overhaul — litter-code AUTO-GEN (pure bijective base-26 lib/litterCode.ts + store counter; useLitterCodes latest-first + peekNextLitterCode sentinel) + searchable combobox components/ui/combobox.tsx for litter/cage/slot (type-to-filter + "+ Add" inline-create, REPLACES select+sentinel+revealed-input trio); NEW cage/slot/line inline-create w/ GLOBAL dedupe (cage_number, slot label, renderedId, lineName all global-unique); add-mouse v1 → DONE mock-era (tasks P0.7 [x], archived)
2026-09-16 client: inline cell edit — updateMouse (renderedId global dedupe, sex-sync from id, genotypeColor NULLED on genotype change) + EditableCell atom (double-click → inline edit; single-click highlight/drawer kept via per-cell gesture + id-cell 200ms click-timer) + Sac context-menu item (signal=dead, isAlive=false)
2026-09-16 client: add affordance (option A) — selection-based tail [+] on each highlighted container (absolute-positioned, zero reflow; append child: slot→mouse, cage→slot, line→cage, grid→line); tail() gates on NODE selection path ONLY (fixes [+] riding genotype/mate washes); structural [+] → AddMouseDialog prefilled or NEW AddLineDialog+addLine; wider inter-line gaps
2026-09-16 client: store split — pure lib/colonyMutations.ts (state,counters,input)→{state,counters,result}; mockColonyStore.ts now thin stateful shell; lib/colonySeed.ts seed-scan helpers (+maxLineId)
2026-09-16 client: per-cell add [+] — REPLACES the column-aligned AddRow strip (c80d903); TailPlus straddles the BOTTOM BORDER of the bottom-most selected cell per column per line (rail widths RAIL_W/SEL_W → 4 icons on a diagonal), absolute z-20 (over HlOverlay z-10, under sticky header z-30), zero reflow, rendered as SIBLING of the rail <button> (nested-button + LineLabel overflow-hidden); gate = NODE selection ONLY — genotype/mate wash still lights cells but carries no [+] (a wash is a query, not a position claim); empty-line standing "+ cage" added (a fresh line has no cages, so every descendant [+] lived inside l.cages.map = dead end); dimmed row: opacity moved off the overlay/icon parent (opacity<1 opens a stacking context that trapped the z-20 icon)
2026-09-16 db+types: punch records foundation — 0026_punches.sql (mouse_id→mouse_meta NOT mice/version rows; punch_location CHECK toe|ear|other; effective_at DISTINCT from created_at=tx-start so an implicit toe punch and a same-tx ear punch stay orderable; actor_id; partial idx WHERE deleted_at IS NULL; deliberately NO UNIQUE(mouse_id,location) — both ears is real and the duplicate rows ARE the information; no DB trigger mints the toe punch, service layer does, so a future import cannot double-write); SCHEMA.md+ERD regen; @repo/types PunchLocation/PunchRef{punchId,location,effectiveAt,note?}/MouseCell.punches? (OPTIONAL until addMouse mints one — step 9 flips it); §19 probes on colony_dev in a ROLLBACK tx: bogus location REJECTED, bad FK REJECTED, two live toe rows BOTH insert, soft-delete drops from partial idx and RETAINS the row, effective_at independent of created_at
2026-09-16 client: litterCode.ts extractLitterCode BLOCKER FIX — was /^[MFU]\d+([A-Z]{3,5})$/, rejecting every punch suffix, so ear-punched mice silently vanished from useLitterCodes AND from advanceLitterCounter → auto litter code could collide with a real one; now /^[MFU]\d+(?:\+\d+)?(?:ear|e)?([A-Z]{3,5})e*$/ — tolerates pooled +N, legacy mid-position e/ear (parse-only), and REPEATED tail e (ee = both ears, plan Q41); suffix alternation is lowercase-only vs [A-Z] capture so it can never steal an uppercase letter (M6EEE→EEE); fixture M4+10AZZ now contributes AZZ to the seed scan (it did not before)
2026-09-16 client: add-flow split into FOUR mutations (plan §3 P0-b v1.1) — addLine(name,cageNumber,slotLabel,mouse?)→addCage(lineId,…)→addSlot(cageId,slotLabel,mouse?)→addMouse(cageId,slotId,mouse); each creates what its name says and fills required descendants DOWN TO SLOT, mouse optional except addMouse; addMouse is now the SOLE mouse-creating primitive (one place for the future toe punch, not two branches); AddMouseInput loses newCageNumber/newSlotLabel; invariants line≥1 cage, cage≥1 slot, slot MAY be empty; suggestNextCageNumber = max+1, editable, '' when any cage number is non-numeric; AddLineDialog gains required cage+slot (empty line is now unreachable); AddMouseDialog line-only prefill opens in NEW-CAGE mode (was silently reusing cages[0]) and cage-only prefill in NEW-SLOT mode (was adding a mouse to slots[0]); colonyMutations.ts 309→386 lines (AC said <300 — step 9 gutting updateMouse should get it there)
2026-09-16 client: gridMove BLOCKER FIX — `cage.slots = cage.slots.filter(s => s.mice.length > 0)` ran on EVERY move for EVERY cage, encoding the pre-split "a slot cannot be empty" world; once addSlot could create an empty slot the first subsequent move DELETED it, leaving the cage slots:[] with no add affordance at all (both the slot [+] and the standing "+ mouse" render inside c.slots.map) — unrecoverable dead end reachable from the main AddLineDialog path; prune removed, verified by probe (A1:1 A2:0 → move → A1:0 A2:1, both slots retained); also addSlot now restores the CALLER's state/counters when its inner addMouse rejects (only level missing the guard)
2026-09-16 docs: plan §4 R1–R10 schema refinements (291 lines, SUPERSEDED by R11–R25) → _archive/plan.r1-r10-schema-refinements.md, byte-exact diff verified before the cut; stub keeps one line per R because §4's preamble, tasks P0.7-b step 9 and P1 genotyping all cite by R-number; 00.plan.md 1414→1148 lines. 00.tasks.md has NOTHING archivable — its 65 KB is the OPEN backlog, not completed detail (that is the case for a docs/phases/ tier, not for archiving)
2026-09-17 client: mate cell — partner id 10px→9px; mate+parent cells flex-col→`grid grid-rows-2` so the two dividers line up at any row height (a flex item's min-height is auto, so the half holding text could not shrink below its content and shoved the divider off the 50% line; 1fr tracks split exactly in half — tradeoff: content now CLIPS instead of pushing, hence min-h-0 + overflow-hidden); empty-mate `—` gains row-span-2 (one child in a 2-row grid would leave a dead half); top half shows the LATEST mate only (ids wrap badly; badges below carry the whole group), badges keep the same order (leftmost = latest) and wrap as the group grows
2026-09-17 types: `MateRef.matedOn` (ISO, nullable) — "latest mate" is matedOn sorted DESCENDING, nulls last, NEVER array order; mock fixtures deliberately put the LATEST mate at array index 0 so an order-based fallback renders visibly wrong; server-swap trap recorded as a task in docs/phases/p0.6.tasks.md (omitting matings.mated_on from the SELECT nulls the field → sort degenerates to join order → wrong partner, no error)
2026-09-17 scope: MVP IS MOCK-ONLY (user) — mock data + mock APIs, no server, no real persistence; server + persistence = Phase 2. Resolves the standing contradiction between plan §3 (listed server endpoints under P0-a) and the "MVP = mock+UI workflow fidelity" directive — the directive wins. P0.6 server-side items (endpoints, MOVE tx, repositories, appendVersion, audit writes) and P0.3/P0.4 (parsers + ETL, which exist to feed a DB) are OFF the MVP path; MVP must prove the WORKFLOW in the UI against mock stores
2026-09-17 plan: genotype allele pair is ORDERED = PARENT OF ORIGIN (user) — first MATERNAL, second PATERNAL; `f/+` and `+/f` are DIFFERENT mice. Canonical-order rule (`f<+<-<G<…<?`, lower rank stored first) DELETED — it would have silently rewritten `+/f` as `f/+` and destroyed parent of origin unrecoverably. Columns/DTO renamed allele_a/b → allele_maternal/allele_paternal (a generic name is how someone fills an ordered pair backwards). GENO colour key preserved by sorting a THROWAWAY COPY at lookup time only, so storage keeps the order while `f/+` and `+/f` still share one colour. Unknown parent-of-origin = staff/professor enters it MANUALLY; picker must start at `?/?` and never pre-fill an order for unknown-parent mice
2026-09-17 types: MouseTaskTag → MouseCaseTag (user) — under Task model v2 the CASE is the primary entity and a task is a child record, so the badge on a mouse cell represents the case; "TaskTag" made it look like it mapped to a child task. 4 live call sites (packages/types src grid+index, ColonyGridView import/badgeIndex/prop); the type's doc-comment was also stale (claimed a server-resolved task_type model — badges are actually derived client-side from open cases at todo/doing) and was corrected in the same edit. Shape `{type, signal}` is a neutral badge projection, unchanged; MouseCell has no field of this type (activeTasks was removed earlier); packages/types/dist is untracked build output, left stale
2026-09-18 client: P0.7-b step 5 DONE — pure lib/mouseIdentity.ts buildMouseLabel (sex·pupNumber·(+offset)*·litterCode·e×earPunchCount; spec name composeMouseIdentity); probe: M6BFA/M6BFAe/M6BFAee/M6+10BFAe byte-exact, toe-only==none, 21/21 mock labels == compose(parts) after fixing metaId 201 F10BEVe (missing ear punch row → punchId 22); unused effectivePupNumber dropped
2026-09-22 docs: P0.7-b step 9 SPLIT into 9a/9b (scope-verify, no code) — step 9's AC covered only the mutation side (drop the patch field, drop the uniqueness check, "call buildMouseLabel at render"), but `MouseCell.mouseLabel` is still a STORED field (grid.ts:100) written at colonyMutations.ts:88 and overwritten at :342, while the architect design requires "NO stored label" — that deletion had NO task and no AC anywhere; 9a = mutation-side (drop `UpdateMousePatch.mouseLabel`, drop the `mouseLabelSet` uniqueness branch, label cell goes read-only, `mouseLabelSet` loses its only consumer), 9b = type + call-site migration (delete the field, flip `punches?`→required per the 2026-09-16 note "step 9 flips it", ~30 mock-fixture lines, gridFilter composes at filter time, mockColonyStore:100 + colonySeed:88 read `m.litterCode` instead of parsing it back out); step 9's AC named `renderedId` but the shipped symbol is `mouseLabel` (renamed at step 5) so "grep renderedId returns 0" was already vacuously true — ACs now name shipped symbols, and the `:202-229`/`:205-209` line refs (now `addSlot` after step 8) replaced by symbol refs; step 10 re-pointed BLOCKED-BY 7,9 → 7,9b (its "ear punch changes the label immediately" AC is impossible while the label is a stored string); OUT OF SCOPE + routed to architect: `ParentCell.mouseLabel` has no compose parts, so it must keep a projected string or gain parts (non-blocking)
2026-09-22 docs: living docs renamed to the canonical `rules/docs.md` §0 names — 00.plan.md→PLAN.md, 00.tasks.md→TASKS.md, 01.status.md→STATUS.md (pure renames, ~30 refs rewritten across docs/phases/, docs/design/, root CLAUDE.md, .prettierignore; _archive/ and .claude/_planning/ keep their historical paths on purpose). WHY: `01.status.md` matched NO glob in docs.md `paths:` — the status log had been loading zero rules (§4 line style, superseded-strike, roll-off thresholds) since it was created; `00.plan/00.tasks` only matched by accident of ending in `.plan.md`/`.tasks.md`, globs that exist for §1 SPLIT SIBLINGS (docs/phases/*), not for root docs. Canonical names close the gap without a naming-variant glob, so `**/*.status.md` was added then removed again. `**/*.plan.md`/`**/*.tasks.md` STAY — they are what loads the 10 split shards in docs/phases/. Orphan `packages/db/01.status.md` (2 lines, no sibling PLAN, project-wide content) MOVED into _archive/status.2026-09-04_07-schema.md — its era was already rolled off — then x_-renamed; destination verified before the cut (§2)
2026-09-22 client: P0.7-b step 9a DONE, reviewer PASS (a4c4089) — `UpdateMousePatch.mouseLabel` dropped with its `sex` back-inference and `extractLitterCode` re-derivation, colony-wide label-uniqueness check removed and `mouseLabelSet` deleted (no unused export), grid label cell now read-only; `MouseCell.mouseLabel` still stored on purpose = 9b, which is unblocked now
2026-09-22 docs: two P0.7-b AC defects recorded (no code) — (a) 9b's "editing `sex` via inline edit changes the rendered label" is IMPOSSIBLE: there is no inline sex editor (sex was only reachable by label back-inference, removed by 9a; app/MouseDetail.client.tsx:125-126 is read-only), so the AC is REWRITTEN as a store-level probe (`updateMouse(603,{sex:'F'})` → seed mouse 603 `U5BFA` renders `F5BFA`, no label write; `UpdateMousePatch.sex` already exists) and the missing control becomes its own NEW-SCOPE task BLOCKED-BY 9b, not silent 9b scope creep; (b) step 8's `colonyMutations.ts < 300 lines` was never met — 386 at close (deferred in the 2026-09-16 STATUS clause "step 9 should get it there", with NO task), 488 today, past code.md's 400 HARD line; 9a is NOT the cause (it removed ~33). Step 8 stays [x] (its four mutations shipped) with the criterion annotated as PARTIALLY MET and carried forward as new task 8b, BLOCKED-BY 9b (9b deletes the label writes living in that file). THIRD AC defect in this family after the vacuous `renderedId` grep and the task-less `MouseCell.mouseLabel` deletion — pattern sentence added to the P0.7-b preamble: an AC named a symbol/control/number never checked against the code when written
2026-09-22 plan: §5 Q46 OPEN — `ParentCell` label, architect recommends option C (discriminated union: in-grid parent carries NO label, composed from the `MouseCell` at its `metaId`; outside/unknown parent keeps a string renamed `snapshotLabel` from `mates.mate_raw_label`), OWNER HAS NOT PICKED; cost = "renderable" becomes tied to "present in the payload" (bites only if ColonyGrid stops being whole-colony); consequence = `mate_raw_label` is FATHER-ONLY (0002_core_tables.sql:459-461) so an outside MOTHER has no snapshotLabel source; drift already documented in the fixture (getColonyGrid.mock.api.ts:254-257); dead `@repo/types MouseDetail.parents` (getMouseDetail has zero callers, drawer reads `MouseCell.parents`) decided with it; 9b's stale "route to system-architect" paragraph repointed to Q46
2026-09-22 client: P0.7-b step 9b DONE, reviewer PASS (8ae117e) — stored `MouseCell.mouseLabel` DELETED and `MouseCell.punches` flipped to REQUIRED (finishing step 2's "step 9 flips it"); ten render sites + `gridFilter` now compose through a NEW `mouseLabelOf(mouse)` helper in `lib/mouseIdentity.ts` (reviewer ruled it in-scope: it puts the active-ear-punch filter in exactly ONE place instead of ten); `colonySeed`/`mockColonyStore` read `m.litterCode` directly instead of parsing it back out; 21 `MouseCell`-level `mouseLabel:` fixture lines deleted, the 8 `ParentCell` ones deliberately kept (Q46 unanswered); verified all 21 seed mice compose byte-identically to their old stored strings (offsets + ear suffix), `updateMouse(603,{sex:'F'})` → `F5BFA`, `peekNextLitterCode` → `BGY` both sides, search still matches `BFA`/`M6`; `colonyMutations.ts` 488→485 lines
2026-09-22 docs: P0.7-b — step 8's SECOND unmet AC recorded and opened as 8c (no code): `addMouse` mints NO implicit `toe` punch and never did — a reviewer ran the real `addMouse(...)` and got `punches: []`, and no `PunchRef`-creating code exists outside the mock fixtures. FOURTH AC defect in this family and a NEW VARIANT — the first where an AC asserted behaviour NEVER IMPLEMENTED at all (the earlier three mis-named a symbol/control/number that did exist). It survived several reviews because step 5's AC requires a toe-only mouse to render IDENTICALLY to a zero-punch mouse, so the label projection masks it BY CONSTRUCTION — no rendering check can ever catch it, only direct `punches` inspection, and 8c's AC is written against the field. Step 8 stays [x] (its four mutations shipped), annotation now lists criteria (i) `<300 lines` → 8b and (ii) toe punch → 8c. 8c's dependency is left OPEN and attributed: minting needs `nextPunchId` (step 7, BLOCKED-BY 6, BLOCKED-BY the GATE), so 8c either waits on 7 or pre-empts it — system-architect owns that, and the 8b↔8c ordering with it. Interim divergence (seed mice 1 toe / user-created 0 / `punches` required) is described by the correct-but-unowned comment at `colonyMutations.ts:109-112`; per docs.md §7 that comment is a deferral in code form, legitimate only now that 8c exists, and 8c's AC requires its deletion. ALSO: NEW 9c — `extractLitterCode` has ZERO consumers after 9b while 9b's AC explicitly forbade deleting it (steps 3+5 need the legacy-form parser), so code.md's no-unused-exports gate is a criterion unmet at 9b's close → its own task; an `x_` rename is NOT a soft delete here because both apps' tsconfig `exclude: ["**/x_*"]` removes it from the BUILD; the intended consumer is OPEN, architect naming it (9b itself is AC MET IN FULL — its own escape hatch said flag, don't delete). Two record corrections: 9b's body named `MouseCaseDrawer.client.tsx` + `TasksView.client.tsx` as migration sites but NEITHER holds a `MouseCell` (both render persisted snapshot strings `target.label`/`task.subjectLabel`) — struck in place, developer correctly left them alone, logged in the PATTERN block as the file-name variant of the same defect; and 8b's `colonyMutations.ts` line count 488→485 after 9b. Recorded not actioned: `NewTaskDialog.client.tsx:35` imports `SEED_COLONY` directly instead of the live store (mock module feeding a real UI list, MVP-ladder invariant L3; pre-existing, not from 9a/9b) — OPEN, architect. 8b + inline sex editor now UNBLOCKED (9b closed), step 10 BLOCKED-BY 7 only
2026-09-22 plan: §5 Q46 DECIDED (owner) — option C, "we do not save label, we need to combine from mouse info, do the same thing": a parent with non-null metaId carries NO label and composes from that parent's MouseCell via buildMouseLabel/mouseLabelOf; only the outside/unknown arm (metaId null) keeps a string, renamed `snapshotLabel`, sourced from FATHER-ONLY `mates.mate_raw_label` (so an outside MOTHER has no server-era source — recorded as a server-era gap, does not block). Opened as P0.7-b task 9d: type change + 2 call sites (ColonyGridView ParentRow, MouseDetail) + 8 fixture lines (6 in-grid deleted, 2 outside renamed) + the dead `@repo/types MouseDetail.parents` deleted/retargeted WITH it (getMouseDetail has zero callers). Falsifiable AC: the parent cell for metaId 101 renders `M4BCW.2`, the composed `.N` form the stored string never had. CORRECTS STATUS 2026-09-22 line "OWNER HAS NOT PICKED" — picked. Blocked by nothing
2026-09-22 plan: §5 Q45 marked RESOLVED 2026-09-18 (doc was stale, not the work) — the `+offset` storage home landed as migration 0027 `pup_number_offsets` + `MouseCell.pupOffsets` with step 5; recorded as a RESOLVED line under the Q per §5's own rule, TASKS P0.7 stub's "Q42 + Q45 OPEN" corrected to Q42 only
2026-09-22 docs: P0.7-b blockers RULED, no code — 8c is BLOCKED-BY NOTHING (Q40 DISSOLVED 2026-09-16, so step 6's `BLOCKED-BY: GATE (Q40 …)` was stale and is corrected; of the still-open gate Qs none touches a punch-id counter: Q42 is the NAME of the third location and creation only mints `toe`, Q43's remainder is a DIFFERENT mint site deferred to step 10). CORRECTS STATUS 2026-09-22 line "8c's dependency is left OPEN and attributed". 8c now owns `Counters.nextPunchId` (creation needs it first) and step 7 no longer claims it — two checkboxes stop owning one deliverable; 6 is sequenced after 8c, 8c runs before 8b (same file: content change first, split second). 8c scope fixed at three pieces: `maxPunchId` in colonySeed (seed max 22 → first minted 23), counter + toe punch in addMouse, counter seed in mockColonyStore; date injection = NEW `MouseSpec.punchEffectiveAt` (ISO, callers pass TODAY from lib/dueDates) so no mutation signature changes — NOT `spec.dob`, effective_at means when the punch physically happened; shipped field is `punchId`, not `id`. AC headline is the OWNER'S formulation — every mouse in the colony has at least one punch, therefore every mouse has a punchId — asserted colony-wide AFTER several store creations, plus pairwise-distinct punchIds (catches a second allocator), toe+ISO effectiveAt, addSlot-without-mouse mints nothing, a one-mint-site grep excluding `apis/*.mock.api.ts`, and a §19 delete-the-mint/watch-it-fail/restore step; every criterion inspects the `punches` FIELD because step 5's AC masks this defect in the render BY CONSTRUCTION
2026-09-22 docs: 9c REWRITTEN as a RETIREMENT task (owner decided) — `extractLitterCode` leaves the code instead of gaining a consumer: the Q39 "PARSE must accept the legacy mid form" premise cited import AND inline edit, but 9a deleted the inline-edit path and there is NO TS import path in this repo (`raw_mouse_id` greps to 0 over *.ts/*.tsx; P0.4 import is server-era), and a parser shared by importer and client belongs in packages/domain, not an app lib. Knowledge MOVES FIRST: the function body + its entire comment block are copied VERBATIM into docs/phases/p0.7.plan.md under "Legacy mouse-label parse contract (retired from code 2026-09-22)", preserving the six tolerated forms (M6BFA/M6BFAe/M6eBFA/M6+10eBFA/F10+1earAYY/M1+10+20AZZ) and the three-instance bug-class history — that block is the acceptance spec the future import parser must satisfy. `parseLitterCode`/`formatLitterCode`/`nextLitterCode` ring-fenced, module stays; the two now-false references in the file's own header comment go too; AC: source-grep 0, no NEW unused export, `peekNextLitterCode` identical on the seed colony before and after. An amendment note added under Q39 so the premise change is visible where the requirement lives
2026-09-22 docs: PLAN.md SPLIT per docs.md §1 (owner-approved) — §2 Architecture decisions (201 lines) → docs/phases/architecture.plan.md and §5 Open Questions (417 lines) → docs/phases/open-questions.plan.md, each destination written and read back BEFORE the cut (§2 move mechanics), each leaving a stub carrying current STATE; section + Q numbering PRESERVED in both files so the ~30 existing `plan §2` / `plan §5 Q19|Q43|Q45` citations across TASKS.md, docs/phases/ and docs/design/ still resolve through the stub — no mass rewrite. PLAN.md 1068→495 lines (still past docs.md §1's 400 hard line; next candidates are §3 Roadmap ~115 and §4 ~143, NOT split this pass — owner approved two). Root CLAUDE.md "Living docs" path list updated to name both split files. NOT FIXED, needs the owner: docs/phases/p0.3.tasks.md:62 cites a `plan §5 Q47` that does not exist, and docs/phases/p0.4.tasks.md:83 cites `plan §5 Q46` for a collision-warn-scope question that is NOT the ParentCell Q46 — pre-existing numbering drift; renumbering or minting a Q47 would give the dangling ref a wrong target
2026-09-22 docs: CORRECTION to the split line above — the post-split section sizes are §3 Roadmap **226** lines (PLAN.md:103-328) and §4 **143** (329-471), not "~115 and ~143"; §3 is the LARGEST remaining unit and is the next split candidate, not §4. Verified by heading grep. Also verified this pass, for two ACs that were inherited rather than re-grepped: `['"]toe['"]` under apps/colony_client_web (excluding .next) still hits ONLY the fixture (21 occurrences, 1 file), so 8c's one-mint-site AC is falsifiable; and `nextLitterCode` ALREADY has zero external consumers (pre-existing, unrelated to 9c) — 9c's AC reworded to "no NEW unused export" with that baseline named, so it cannot be blocked by a defect it did not cause
2026-09-22 client: P0.7-b 9c/8c/9d/8b all DONE, reviewer PASS (15194aa 4d5870e fcece2b da51a75, stale-ref fixes afa8c43) — 9c RETIRED `extractLitterCode` after moving its regex + six tolerated forms + three-instance bug history into docs/phases/p0.7.plan.md and DIFFING the doc block against the deleted lines (not eyeballing it), `peekNextLitterCode` `BGY` both sides; 8c mints the implicit `toe` punch in `addMouse` — colony-wide 25 mice none with zero punches, 26 pairwise-distinct punchIds, first minted id 23 off seed max 22, `addSlot` without a mouse burns none, `'toe'` in exactly one non-fixture file, and reverting the mint reproduced the failure on exactly the four new mice (§19); 9d made `ParentCell` a discriminated union (Q46 option C) — metaId 101's parent renders `M4BCW.2` with BOTH `mouseLabelOf` AND `composeMouseLabel` verified in the chain (`mouseLabelOf` alone gives `M4BCW`, the silent pass), `ParentRef`/`MouseDetail.parents` deleted with nothing dangling; 8b split colonyMutations.ts 485→307 + NEW updateMouse.ts (107) + colonyMutationHelpers.ts (114) — CORRECTS the 2026-09-16 clause "step 9 gutting updateMouse should get it there" and closes step 8's criterion (i) via the AC's SECOND branch (<400 + a written justification: each add mutation's input type is coupled to the function below it, so lifting them out is the artificial seam the task warned against), justification recorded on the closed task so it is not re-litigated at 7 lines
2026-09-22 docs: P0.7-b recording pass, no code — four tasks ticked with their stubs; FIFTH AC defect logged in the PATTERN block, a NEW VARIANT (an AC grepping a BARE IDENTIFIER that legitimately appears in prose and in a module name): 9d's "grep `mouseLabel` under packages/types/src returns 0" returns 2, both legitimate (grid.ts:27 stale prose, since repaired afa8c43; and the surviving `lib/mouseLabel.ts` MODULE, which 9b's AC required keep existing) — intent met, wording unmeetable, RESTATED in place as "no `mouseLabel:` KEY DECLARATION", and the sharp edge is that 9b's own AC carried that warning verbatim one task above. RULED, so it is not re-argued: the three stale refs afa8c43 fixed (plan §7 citation, grid.ts pointing at a retired function, five/six tolerated-forms count) are a DRIFT class — TRUE when written, stale when the referent retired — distinct from PATTERN (false when written); test = "was the sentence false the day it was written?"; shared remedy = grep the referent at write time AND at retirement. No living doc says "five" any more (p0.7.tasks.md:450 fixed). NEW 9e — **owner DECIDED: DELETE the dead code**, superseding the record-only treatment: `nextLitterCode` (re-grepped post-9c, definition only; the live path is counter-based ON PURPOSE because next-from-current can collide, and its `return 'AAA'` fallback is a silent-wrong-answer hazard), `apis/getMouseDetail.mock.api.ts` (133 lines, zero importers) and `MouseDetail`/`GeneCall`/`HistoryEvent` + their re-export — explicitly NOT a 9c-style knowledge move, `GeneCall`'s baked `"f/+"` is the genotype model P1 abolishes (allele_maternal/paternal) and `HistoryEvent` is superseded by `useTaskLog()`; AC greps are WORD-BOUNDED because `MouseDetailDrawer`/`MouseDetail.client.tsx` must survive — an unbounded grep would be the same §7 trap that just bit 9d; SEQUENCED after step 6 (shared packages/types). `x_getLineGrid.mock.api.ts` recorded NOT actioned: already x_-prefixed and tsconfig-excluded, awaiting the OWNER's manual removal. Phase state: 6 and the inline sex editor are [~] IN FLIGHT (sex editor's design question DECIDED by the owner — control in the MOUSE-DETAIL DRAWER, not the grid cell); step 4 GATE holds Q42 + Q43-narrow only; 7 waits on 6, 10 on 7 + Q43-narrow
2026-09-22 plan: punch tombstones DECIDED (owner) — **option B**, "B: correct. because we can update punch record's deleted_at": `MouseCell.punches` stays ACTIVE ROWS ONLY and a removed punch is tombstoned in a separate append-only PUNCH LOG served by its own selector, mirroring how the drawer gets case history via `useTaskLog()`. Step 6's in-flight implementation had flipped `punches` to carry tombstones, contradicting FOUR standing statements at once (p0.7.plan.md:34 "active rows only", 0026's partial index `WHERE deleted_at IS NULL`, grid.ts's "no deleted_at leaks to the client" header, rules/core.md "mask at read"); B RESTORES plan §4 rather than changing it, so §4 is untouched. Step 6 SPLIT: NEW **6a punch history contract** (additive grid.ts header sentence naming history DTOs — SNAKE `deleted_at`, never camel, so 6's grep stays falsifiable; `punches` reverted to active-only citing plan §4; `PunchRef.deletedAt` removed from the grid DTO; NEW `PunchHistoryEntry` in its OWN packages/types file, NOT grid.ts; `{grid, punchLog}` store shape; a `colonySeed.maxPunchId` comment recording that it counts EVERY row INCLUDING tombstones because ids are never reused — a counter seeded from active rows alone re-issues the highest id the moment that punch is removed) BLOCKS 6; step 6 rebases, does not restart — `punchMutations.ts`'s structure, the pure/no-`Date` discipline, the injected `deletedAt` and no-op-on-unknown-id all survive. `mouseIdentity.mouseLabelOf`'s `deletedAt` filter comes OUT as **6a scope (f), not step 6** — it is dead under active-only AND untypeable the moment 6a removes the field, so leaving it to a step 6a blocks would make 6a's own check-types criterion unmeetable. Vocabulary SCOPED, or the rebased step contradicts itself: "never splice" was absolute — remove = tombstone the RECORD in the log, never hard-delete; splicing the row out of the VIEW is what "mask at read" means. Step 7 grows to own the `punchLog` seed (AC asserts `punchLog.length` == sum of seed `punches.length`, equality not the literal 22) + a `usePunchLog(metaId)` selector mirroring `useTaskLog`; 10 adds 6a to its blockers. Baseline named so it is not misread later: `PunchRef.deletedAt` exists only in the UNCOMMITTED working tree
2026-09-22 docs: SIXTH AC defect in the P0.7-b family, a NEW SUB-SHAPE — an AC that silently MANDATES a schema change because the state shape it operates on has no room for what it demands. Step 6's AC said `removePunch` shrinks active punches "while the row survives with deletedAt set", but in a pure function whose entire output is `ColonyGrid` there is nowhere for the row to survive EXCEPT `MouseCell.punches` — so the AC forced the flip. Discriminating test applied: false the day it was written (plan §4 already said active-only on 2026-09-16) → PATTERN, not DRIFT. The developer had no other reading and FLAGGED rather than hid it, correct on both counts. Cross-check that would have caught it: grep the plan for the FIELD'S OWN DEFINITION before writing an AC that constrains it. Step 6's AC REWRITTEN (one mouse +1 and all others reference-equal + one `punchLog` row appended; remove = one fewer in `punches` WHILE the log holds that `punchId` with `deletedAt`; ids never reused; no-op on unknown/already-removed; **grep `deletedAt` under packages/types/src/grid.ts returns 0** as the falsifiable guard that B landed). Step 10's AC is NOT stale and did NOT change — it never named `MouseCell.punches` as the source — only TIGHTENED to say the removed row surfaces via `usePunchLog`. RECORDED OPEN, architect owns it, not designed here: zero-active-punches (floor vs no floor) is STILL UNDECIDED — the owner answered with a PROPOSAL instead of a pick, a new `untagged` punch type for pups that arrive with no tag; three collisions recorded on step 10 (`punch_location` is a CLOSED CHECK in 0026 so a fourth value is a MIGRATION; plan §5 Q42 is open and asks precisely whether that CHECK is closed or the suffix set open; and it may contradict 8c, which SHIPPED today minting `location:'toe'` at creation — 8c is NOT marked defective). 9e re-sequenced after 6a as well as 6 (all three touch packages/types/src/index.ts)
2026-09-22 client+docs: inline `sex` editor reviewed PASS then SUPERSEDED, NOT SHIPPED (b5a990b) — data layer PROVEN (U5BFA→F5BFA→M5BFA→U5BFA all reachable, no `mouseLabel` key in the result, SEED_COLONY unmutated, no sex control in the grid cell, check-types/lint 0), interaction layer explicitly NOT VERIFIED (no browser in the environment) and the reviewer found THREE real problems BY READING: a native `<select>` does not blur on pick so the edit sits uncommitted with no Save affordance and no pending cue; Escape closes the whole Sheet because Radix registers on `ownerDocument` with `capture: true`, ahead of React, making editable-cell.tsx:115-116's "stop propagation" comment FALSE for this consumer; and pick-then-close-via-overlay may SILENTLY LOSE the edit (Radix dismisses on `pointerdown`, before focus moves). Those three are the EVIDENCE BASE for the owner's removal decision, not incidental bugs. KEPT deliberately (task 12 is their consumer, do not delete them with the control): the drawer's live-lookup staleness fix — ruled ENTAILED by the AC, not scope creep, and proven (the click-time snapshot renders U5BFA after the edit and FAILS the AC) — and the store-direct `updateMouse` wiring. Record-only, no task: MouseDetail.client.tsx:73-74 carries a comment claiming a guard that does not exist (task 11 rewords it if it survives), `next as Sex` was an unchecked cast (dies with the control), `findMouseByMetaId` is now the FOURTH colony traversal alongside ColonyGridView's memoized map, updateMouse.ts's inline finder and gridSelection.ts (SURVIVES, architect's to rule on, not fixed here), and editable-cell.tsx:11 contradicted line 99 (DRIFT class, moot once the file goes)
2026-09-22 docs: INLINE CELL EDITING REMOVED — OWNER DECIDED 2026-09-22, "lets simplify we will remove cell click edit feature from now on > work this first no more cell direct update. delete all related code." / "we use a edit modal or mouse drawer to update a mouse data" / "for mouse label we will show all part by part so we can edit each". Owner chose REMOVAL FIRST as its own task and the replacement surface LATER, explicitly accepting a window in which NO mouse field can be edited in the app — that gap is DELIBERATE and must not later be read as a regression. NEW P0.7-b task 11 [~] (developer already implementing): delete components/ui/editable-cell.tsx (181 lines) + its three consumers — ColonyGridView genotype (:1969) and dob (:1992), MouseDetail sex (:163) — cells go read-only, no dead double-click affordance survives, nothing else changes. An `x_` rename is NOT a soft delete here (both apps' tsconfig `exclude: ["**/x_*"]` drops the module from the BUILD), so the file goes as an ordinary reviewable diff, recoverable from git. 11 RING-FENCES `updateMouse`/`UpdateMousePatch.sex`/the drawer live lookup — baseline grepped: `updateMouse` keeps exactly ONE UI caller after the removal, ColonyGridView.client.tsx:1034's Sac context-menu, so it is not a zero-consumer export; ACs are grep-baselined against the UNCOMMITTED tree (EditableCell already 0 in SOURCE, 157 hits remain in `.next/` build output, onDoubleClick 0). NEW task 12 [ ] replacement edit surface, BLOCKED-BY 11 + an architect ruling, NOT designed here: modal-vs-drawer, the shape of the part-by-part label editor, and P0.7-b vs P0.8 allocation are all OPEN and attributed — the earlier "control goes in the mouse-detail drawer" ruling is SUPERSEDED with the mechanism, so 12 does NOT inherit "drawer" as decided. 12 must carry two falsifiable criteria that are 9a/9b closure, not design: no free-text whole-label input and no sex back-inference from a typed string, and "no `mouseLabel:` KEY DECLARATION under packages/types/src" (9d's RESTATED form — the bare-identifier grep returns 2 legitimate hits and is unmeetable). Also noted on closed 8b: its AC clause "still edits genotype + DOB inline" was TRUE at close (da51a75) and is RETIRED by 11 — do not re-run it, 8b stays [x]
2026-09-22 plan: `untagged` punch location DECIDED (owner) — ADOPT it, "add this type into enum for punchs table", OVERRULING the architect's earlier recommendation against it; the architect is now DESIGNING it. CORRECTS the 2026-09-22 line above that recorded it as a PROPOSAL rather than a pick. The three collisions stay recorded on step 10 and are what the design must answer, not dissolved by the decision: `punch_location` is a CLOSED CHECK in 0026 so a fourth value is a MIGRATION; plan §5 Q42 is OPEN and asks precisely whether that CHECK is closed, so proposal and Q42 must be answered together; and it sits against shipped 8c, which mints `location:'toe'` unconditionally at creation — 8c is NOT marked defective. STILL OPEN: the zero-active-punches FLOOR (floor vs no floor) — adopting `untagged` did not pick a branch, and the architect's design may or may not close it
2026-09-22 docs: P0.7-b NEW task 8d — `untagged` punch location, owner decided the VALUE and the DEFAULT (`untagged`, not `toe`: a pup may arrive with no tag and a forgotten field must not assert a mark that does not exist); it is an ORDINARY fourth value of `punch_location`, nothing special. Four pieces: migration `0028_punch_location_untagged.sql` (0027 is the current highest — VERIFY the constraint name via `SELECT conname FROM pg_constraint WHERE conrelid='punches'::regclass AND contype='c'`, expected `punches_punch_location_check`, because SCHEMA.md lists indexes only; then regen SCHEMA.md + ERD); `PunchLocation` gains `'untagged'` (`PunchRef` unchanged); `MouseSpec.initialPunchLocation` riding the existing `mouse?` param exactly as `punchEffectiveAt` does so NO mutation signature changes, `buildMouseCell` mints `spec.initialPunchLocation` and the `'toe'` literal leaves it, AddMouseDialog (the ONLY MouseSpec construction site, `:331`) gains a select defaulting to `untagged`, `addMouse` stays the sole mint site; 1-2 `untagged` fixture mice added beside the 21 existing `toe` rows, which moves the seed `maxPunchId` off 22 — step 7's AC already asserts the EQUALITY, not the literal. `toe` renders nothing, `untagged` renders nothing, `mouseIdentity.ts` unchanged. SEQUENCED AFTER 6a and 6 (shared `packages/types/src/grid.ts`; a third rebase of in-flight work is what this avoids). Step 6's `+1` AC, `0026:24-25` and 6a's `maxPunchId` comment are untouched. CORRECTS the line above: the architect is no longer "DESIGNING it" — 8d is the design. Q42 left exactly as it stands, no amendment. STILL OPEN (unchanged by this): the zero-active-punches FLOOR
2026-09-22 docs: P0.7-b task 12 FILLED IN — architect RULED all three open points. Surface = the DRAWER, not a modal (`MouseDetailDrawer` exists, is store-driven, already carries the live-lookup fix a new modal would re-solve); shape = an EDIT MODE on the existing Identity section (pencil toggle, LOCAL draft, Save/Cancel, ONE atomic `updateMouse(metaId, patch)` on Save — NOT per-field live commit, because several parts compose one label and the composed result must be visible before it is committed); phase = P0.7-b, not P0.8 (9a deleted label-as-input, 9b deleted label-as-storage, "edit the parts, not the string" is the closing move of that arc; every P0.8 deliverable is cage-location/transfers/drag-and-drop, so parking 12 there leaves the app with no edit path for a whole phase). BLOCKED-BY 11 and SEQUENCED AFTER step 10 (12's punch row is a read-only projection pointing at 10's section). EDITABLE: `sex` (M/F/U select), `genotype`, `dob` (both inherited from 11's removed inline edits, already in `UpdateMousePatch`; P1's per-gene allele picker supersedes free-text genotype later — parity, not new design). READ-ONLY with the reason SHOWN in the UI: `pupNumber` (grid.ts "BIRTH number, immutable" — NOT added to `UpdateMousePatch`; typo correction recorded OPEN for the owner), `litterCode` (membership, not a text field — changing it is re-parenting, interacts with `advanceLitterCounter`/`peekNextLitterCode`), `pupOffsets` ("assigned on transfer" — order is significant, typing it fabricates a renumber provenance no transfer produced; P0.8's transfer flow is the only writer), punches (read-only projection "2 active ear punches → ee" linking to step 10's section, NO second add/remove UI), `.N` (derived from done/verified Tissue-collection cases). Live result composes through `composeMouseLabel(buildMouseLabel({...draft, earPunchCount from live punches}), reclipIndex.get(metaId) ?? 0)` — never a template string, never a second builder. ACs written as DATA-FLOW DIRECTION: no `updateMouse(` argument derives from `buildMouseLabel`/`mouseLabelOf`/`composeMouseLabel` (those appear only in JSX render expressions), the composed label lives only in a `<span>` and never in an `<input value=…>`, the only `sex` write is `updateMouse(metaId,{sex:<select value>})` with no back-inference from a string's first character, no `mouseLabel:` KEY DECLARATION under `packages/types/src` (9d's restated wording), the 603 probe (`U5BFA`→`F` renders `F5BFA` in BOTH drawer and grid with no label write), every read-only part renders with no input control AND its reason, Cancel discards and writes nothing, check-types + lint 0. SUPERSEDES the "do not carry 'drawer' forward as decided" note from earlier today
2026-09-22 docs: two decisions recorded as SETTLED — (a) the owner REAFFIRMED removal-first/editing-later, so the no-edit window between 11 and 12 stands and no stopgap control is restored; do not re-raise it; (b) main session's call, deferred not forgotten: splitting `docs/phases/p0.7.tasks.md` (~1140 lines, past docs.md §1's 400 hard line) WAITS until P0.7-b closes — splitting mid-flight moves files under in-progress tasks (6, 6a, 11 live) and §2 forbids archiving a set with open work; noted at the top of the phase file and in the TASKS.md stub
2026-09-22 docs: OWNER DECIDED — P0.7-b step 10 + task 12 MERGED into ONE task 13, the MOUSE-EDITING DRAWER ("merge into one task"): one surface, and 12's punch row was a read-only projection LINKING to 10's section, so the awkward 12-blocked-by-10 dependency dissolves. 13 carries both scopes and every AC of both, nothing dropped. What the merge CHANGED: the Identity section shows the composed `e…` projection while the drawer's punch section is where rows are added/removed — one mutation path, two views, no second add/remove UI. 10 and 12 keep non-checkbox MERGE NOTES at their old positions (step-9 split precedent) naming what each held and where it went; nothing un-ticked; old 10's three `untagged` collisions re-homed on 13 (the owner's verbatim words live on 8d). BLOCKED-BY 7 + 11 (11 landed `28d38df`, close pass is the reviewer's)
2026-09-22 plan: §5 Q43-narrow RESOLVED (owner) — "tissue is independent each other with toe": a Tissue-collection case does NOT mint a `toe` punch row, so `punches` records IDENTIFICATION MARKS only, `.N` and punch rows describe DIFFERENT events (no double-count), and the punch UI shows ONE mint site. Recorded as a RESOLVED line under Q43 in docs/phases/open-questions.plan.md with PLAN.md's §5 stub updated in the same pass (docs.md §1), the question itself not rewritten. It was task 13's last gate — 13 is now BLOCKED-BY step 7 + task 11 only, and the "merging couples the field half to Q43-narrow" consequence was REMOVED as no longer true. Step 4's professor GATE now holds Q42 alone, which by its own text gates nothing on the creation path — so it may hold nothing at all; NOT closed here, owner's call, Q42 left untouched
2026-09-22 docs: recorded OPEN for the owner, no task — the `x_` soft-delete convention is INERT inside `packages/`: apps/colony_server/tsconfig.json:14 + apps/colony_client_web/tsconfig.json:11 exclude `**/x_*`, but packages/{types,domain,db}/tsconfig.json:16 exclude only dist/node_modules, so `packages/types/src/x_mouseDetail.ts` (soft-deleted by 9e, `81f4abc`) still compiles into `@repo/types` and would emit to dist/. Harmless today (dependency-free interface file, zero importers); 9c/11's "both apps' tsconfig exclude" statements stay true — they are about the APPS. No task opened, no rules change proposed
2026-09-22 plan: §5 Q42 CLOSED (owner) — "close this. and delete all related contents. just write down do not consider. it is just a placeholder." `other` is a placeholder, no decision to make; the accumulated analysis (real-name/suffix-letter framing, open-set-vs-closed-CHECK conflict, the "still open / gates nothing" annotation) DELETED on the owner's instruction — a DELIBERATE exception to §5's record-a-RESOLVED-line-never-rewrite rule, flagged in the entry so it is not read as a defect and restored; Q number kept so `plan §5 Q42` citations resolve. PLAN.md §5 stub updated in the same edit (docs.md §1)
2026-09-22 docs: P0.7-b step 4 (professor GATE) CLOSED — it holds nothing: Q39/Q41/Q43-main/Q44 answered 2026-09-16, Q40 dissolved, Q45 resolved 2026-09-18, Q43-narrow resolved 2026-09-22, Q42 closed 2026-09-22. Ticked `[x]` in docs/phases/p0.7.tasks.md with a per-Q closure line and the "may hold nothing / NOT closed here / Q42 left untouched" prose deleted; TASKS.md P0.7 stub updated in the same edit. Task 13's THREE collisions are now TWO (the Q42 one existed only to reason about `other`; 8d answers both survivors); 8c's BLOCKED-BY-nothing rationale KEPT, its "still-open Q42" clause tagged CLOSED. No open question gates P0.7-b any more
2026-09-22 plan: §1 mouse-ID grammar simplified to what ships — tag suffix = one `e` per ACTIVE `ear` punch (Q41), `toe` renders nothing, `untagged` renders nothing (task 8d); the "OPEN tag-suffix set" / `(toe)`-as-rendered-suffix / "TEXT + known-mapping lookup, unknown → warn not reject" clause removed — it existed to hold Q42 open. NOTE: that import-tolerance clause now lives ONLY in docs/phases/p0.7.plan.md's legacy mouse-label parse contract (the six tolerated forms), which is the future importer's acceptance spec. Applied migration `0026_punches.sql` deliberately untouched — its `other`-is-a-placeholder comment is historical record
2026-09-22 docs: CORRECTION to the line above — the "unknown suffix → warn NOT reject" import tolerance does NOT live in p0.7.plan.md's legacy parse contract (that block holds six tolerated FORMS, no unknown-suffix disposition). It lives, unchanged and owned, in `docs/phases/p0.3.tasks.md:10-12` (the tiered mouse-ID parser, still `[ ]`), which still states the OPEN tag-suffix set with `(toe)`. Left as-is on purpose: §1 now describes COMPOSE (what the app renders — narrow), P0.3 describes PARSE (what an importer must tolerate — wide), the same compose-narrow/parse-wide split Q39 already set. Not orphaned, no task opened
2026-09-22 client+docs: P0.7-b **11 and 13 CLOSED**, both reviewer PASS — 13 (`91bb988`) ALL 12 ACs, verified by driving the real store modules under Node (no browser): `U5BFA` → `updateMouse(603,{sex:'F'})` → `F5BFA` with NO `mouseLabel` key on the cell, `addPunch(ear)` → `F5BFAe`, `removePunch` → `F5BFA` with the `punchLog` row carrying `deletedAt` while the active `punches` list no longer holds it; JSX-only criteria (composed label only in a `<span>`, read-only parts with their stated reason, no punch control outside the drawer, Cancel) READ not clicked — caveat stated, not a claimed click-level verification. 11 (`28d38df`) was ISSUE on criterion (5) alone — two stale comment phrases owed a rewording in its own diff — fixed in `5f4586d`, and the SPLIT is the point: `MouseDetail.client.tsx:69` ("the sex editor below never fires for a mouse it can't find") was FALSE and is rewritten (there IS a sex editor again, and on the not-found path Save DOES fire, returning `{ok:false}` and showing the error), while `:24` ("the drawer itself writes a patch via the store") was false when 11 landed and became TRUE AGAIN when 13 shipped, so it was correctly left as written. L4 SEQUENCING SLIP recorded on BOTH tasks: 13 shipped while 11 was still `[~]` although 13's BLOCKED-BY names it — remedied by this close, nothing re-opened. DOC LAGS SOURCE, stated once rather than papered over: 6a, 6, 7 and 8d have LANDED in source (13's reviewer could not have driven the punch store otherwise) but stay `[ ]`/`[~]` — their ticks are owed by the pass carrying each reviewer verdict; every `[x]` in this file carries a hash + PASS and this pass did not break that
2026-09-22 docs: SEVENTH AC defect in the P0.7-b family — **an AC that greps a PHRASE which a LATER TASK MADE TRUE AGAIN**. Task 11's AC (5) ("grep `the sex editor below|drawer itself writes a patch` returns 0") is UNMEETABLE AS LITERALLY WORDED: one phrase was genuinely stale and was rewritten, the other became true again when 13 shipped the drawer that writes exactly that patch, so a zero-hit grep could only pass by DELETING A TRUE COMMENT. Discriminating test applied as the SIXTH did: the FORM was false the day it was written — a phrase grep cannot distinguish "comment removed" from "comment became true again" — so PATTERN, not DRIFT; same class as the FIFTH (bare-identifier grep) one level up, there the identifier was legitimate, here the CLAIM is. AC (5) RESTATED in 9d's form: **no comment in `MouseDetail.client.tsx` makes a FALSE claim**, not a phrase grepping to zero. Remedy generalised in the block: assert the PROPERTY, never the ABSENCE OF A STRING
2026-09-22 plan: **`untagged` ALWAYS EXISTS and is NEVER REMOVED — OWNER DECIDED**, "we never delete a untagged record. we keep together and if we see 1 active tag with untagged then we can target which mouse is not tagged." Creating a mouse ALWAYS mints an `untagged` punch (not a choice); real `toe`/`ear` tags are added BESIDE it; the `untagged` row COEXISTS and is NEVER tombstoned; "needs tagging" = its ONLY active punch is the `untagged` one (a DERIVED definition, recorded — no indicator scoped). **This CLOSES the zero-active-punches FLOOR BY CONSTRUCTION** — a mouse can never reach zero active punches, so the floor is structural rather than a rule someone must enforce at removal time; it was the architect's to rule on and the owner has settled it. Closed at every site it was recorded (8d body, step-10 merge note, 13's OPEN section, both TASKS.md stub mentions). It CHANGES 8d, which made the location a CHOICE defaulting to `untagged`: per this file's precedent 8d STAYS as shipped (migration, enum value, dialog wiring, fixture all stand) and NEW task **8f** carries the change — 8d's AC (3) "creating with `toe` chosen" and AC (6) "`buildMouseCell` contains NO location literal" annotated RETIRED in place (8b's RETIRED-by-11 device), and its "no coexistence rule, no auto-tombstone" prose struck as SUPERSEDED. 8f scope: always-mint; the AddMouseDialog punch-location select GOES; `untagged` rows cannot be removed (HOW the refusal reads is the architect's — the AC only needs the row to SURVIVE); and the SEED FIXTURES gain an `untagged` row per mouse so the invariant holds in seed data, not only for mice created in-app — a fixture that never reaches a path is exactly what hid 8c's defect for three reviews. Every AC inspects the `punches` FIELD, never a render: `untagged` and `toe` BOTH render nothing, so no rendering check can see any of this (8c's lesson). Mock-coverage gaps checked this pass: runtime-only history stands (owner, no seeded tombstones) and the zero-punch case is now IMPOSSIBLE, so the only real gap left is **two `ear` punches on one mouse** (`ee`, Q41) — folded into 8f as a NEW fixture mouse (`untagged` + `ear` + `ear`), never by mutating an existing one, since 9b's close asserts all 21 seed mice compose byte-identically
2026-09-22 plan: **`punchLog` becomes the SINGLE SOURCE for punches — OWNER DECIDED**, "refactor this. I like this simplification." 13's closing review found `punchLog` has TWO WRITERS — `punchMutations.addPunch` and `mockColonyStore.commitIfOk`'s `findMintedPunch` back-fill for creation mints — correct today but resting on a HEURISTIC ("if `nextPunchId` advanced, find the punch at the pre-commit id"), so a future mutation minting two punches in one call would silently lose one. NEW task **8e**, SEQUENCED BEFORE 8f (same files, one developer, refactor first for a cleaner base): `punchLog` holds every row active + tombstoned with `deletedAt`; `grid.punches` becomes a PROJECTION off it (filter `deletedAt`, map to `PunchRef`); the second writer and its heuristic are DELETED; fixtures keep their nested shape as seed INPUT and the store normalises on init. **The DTO contract does NOT change** — `MouseCell.punches` stays active-only with no `deletedAt` exactly as 6a set it, so this is store-internal and 6a's `grep deletedAt packages/types/src/grid.ts` = 0 guard is repeated in 8e's AC. WHY refactor code that landed hours ago: it matches what the real server does — ONE `punches` table, TWO queries, one `WHERE deleted_at IS NULL` (which is what 0026's partial index exists for); the two-array shape is what made the log's completeness depend on a heuristic. AC also: exactly one writer (no second append path, `findMintedPunch` greps 0), a mouse created through all four add rails has its creation punch in the log AND in active `punches`, and the seed equality still holds (active `punchLog` count == summed fixture `punches.length`)
2026-09-22 docs: MEMO recorded, NOT a fix — the OWNER asked for a memo, so NO task was opened and NO rules change proposed. 9a's anti-re-widening guard greps `lib/colonyMutations.ts`, but **8b moved `updateMouse` and `UpdateMousePatch` to `lib/updateMouse.ts`** (`da51a75`) and NO AC anywhere greps that file, so the guard is VACUOUS TODAY: re-adding a label field to `UpdateMousePatch` would trip nothing and 9a's whole point (the label is an OUTPUT, never an input) would silently regress. Same shape as the FIRST PATTERN occurrence (a grep outliving its referent) — there by a RENAME, here by a FILE MOVE. Candidate remedy recorded alongside so it is not re-derived later: a COMPILE-TIME guard in `lib/updateMouse.ts` (`type NoLabelKey<T> = 'mouseLabel' extends keyof T ? never : T;` applied to `UpdateMousePatch`) rather than a grep, which survives a file move in a way a grep AC does not — as 8b just demonstrated. Recorded as a footnote on 9a's AC, where the vacuous guard lives
2026-09-22 docs: record-only findings from 13's closing review (no AC, no task, dispositions named so nobody re-opens them as defects) — `AddMouseDialog.client.tsx` still has an UNCHECKED `e.target.value as Sex` cast on the CREATION path (the cast class that died with the inline sex editor, on a different path); `updateMouse`'s no-op guard MISSES the null-dob case, so Save on a mouse with `dob === null` sends `''`, compares unequal and forces a pointless tree rebuild + subscriber emit; `TODAY` is a MODULE CONSTANT so a tab left open across midnight defaults the punch date to YESTERDAY; `MouseDetail.client.tsx` is 307 lines, past code.md's 300 SOFT line and under the 400 hard one (a soft-line finding, not an unmet criterion — 13 carries no line-count AC); `IdentitySection` RE-IMPLEMENTS `buildMouseLabel`'s `+${offset}` formatting for display (a second formatter for one grammar); and the two drawer sections import shared constants from `AddMouseDialog.client`, making a DIALOG a constants provider for its siblings
2026-09-22 docs: 8e/8f ACs GREP-BASELINED before writing (docs.md §7, read-only greps — no source touched), which caught two things worth the pass. (a) **8e is already live-uncommitted**: `mockColonyStore.ts:70-78` carries `punchLog` as the single source with `grid` built by `projectPunches(...)`, `:161-165` records that the creation punch is minted straight into `punchLog` by the pure layer so "there is no separate seam to close here any more", and `findMintedPunch` ALREADY greps to 0 — so 8e's AC (1) is written as the CLOSING check on that diff (6a/11's baseline device), with the falsifiable half being "no second append path outside the pure layer", not the vacuous zero-hit grep. (b) **COLLISION FOUND, recorded on 8f as scope (e), not designed**: task 13 shipped `app/PunchSection.client.tsx`, whose ADD picker maps `PUNCH_LOCATION_OPTIONS` imported from `AddMouseDialog.client` (`:10`/`:62`/`:68`), and that list INCLUDES `'untagged'` (`AddMouseDialog.client.tsx:64`) — so today a user can hand-ADD a second `untagged` row, contradicting the owner's "it is not a choice" model through the very surface 13 just shipped. 8f's AC (10) requires the picker not offer it; whether the MUTATION layer also refuses one is left to the architect. Related trap recorded so the two edits are not made blind: deleting the dialog's select must not orphan the option list its sibling imports. 8f's `['"]untagged['"]` AC deliberately NAMES the surviving files instead of asserting "exactly ONE file" — today the literal sits in 3 source files + the fixture, so a one-file wording would have been unmeetable the day it was written, the SEVENTH-occurrence mistake on the task that records the SEVENTH occurrence. Also re-counted while touching it (DRIFT rule): `docs/phases/p0.7.tasks.md` is **1671** lines, not the "~1140" both docs claimed; the SPLIT-DEFERRED ruling itself is unchanged
2026-09-22 client+docs: the mock's punch store RENAMED to match the schema — `punchLog`→`punches`, `PunchHistoryEntry`→`PunchRow` (`punchHistory.ts`→`punchRow.ts`), `usePunchLog`→`usePunches`, `seedPunchLog`→`seedPunches`. Pure rename, no behaviour change; check-types 5/5, lint 0 errors. WHY: 8e had already made it the single source with one append path, but the name still read as a second thing beside a punches table when it IS the punches table — the owner asked "do we still use punchLog? we decided to use just the punches table", which is the confusion the name was causing. Mock vocabulary is now 1:1 with the DB: one `punches` collection holding every row with its `deletedAt`, two views onto it (the grid DTO's active projection, the drawer's full set) — the one-table-two-queries shape 0026's partial index exists for. `ColonyState.punches` and `MouseCell.punches` deliberately share a name: one is the table, the other is that mouse's active rows projected out of it. `PunchRef` (narrow grid DTO, no metaId/deletedAt) and `PunchRow` (stored row, carries both) both stay — only the second was misnamed, it was never "history". Live task text in `docs/phases/p0.7.tasks.md` + `TASKS.md` renamed with it; the five earlier STATUS lines above KEEP the old name, since they record what it was called when each decision was made (§4: never rewrite a line)
2026-09-22 client+docs: P0.7-b **6a, 7, 8d, 8e, 8f CLOSED `[x]`**, all reviewer PASS at HEAD `23474e7` — closing review drove the real compiled pure layer with 41 probes incl. 400 randomised add/remove ops: 6a all criteria (`grep deletedAt packages/types/src/grid.ts` = 0); 7 seed equality AS AN EQUALITY (44 active rows == summed fixture `punches.length`, zero seeded tombstones, no store-local punch counter, both wrappers one line) and it closes while its BLOCKED-BY step 6 is still `[~]` (L4 shape, stated); 8d PASS with TWO ACs **RETIRED by 8f and stated as retired, not passed** (the "`toe` chosen" clause and the "no location literal in `buildMouseCell`" clause are actively contradicted by the shipped design) and ONE AC not re-runnable — the migration's CHECK behaviour, FILE-READ because there was no live Postgres this session, recorded as file-read not as a claimed probe; 8e (`20e020d`) one append path, `findMintedPunch` gone, creation punch present via all four rails; 8f was ISSUE on AC (8) (`'untagged'` still in `PUNCH_LOCATION_OPTIONS`) and is FIXED in `23474e7` by MOVING the constant into `PunchSection` rather than filtering it — 8f had deleted the dialog's select, leaving the dialog defining a list it no longer used and exporting it to a sibling (the quality-gate smell the review flagged separately); one move resolves both, and AC (8) is closed by NAMING the surviving `untagged` files as it requires
2026-09-22 docs: P0.7-b **step 6 stays `[~]`, now BLOCKED-BY new 8g** — its clause "every other mouse is reference-equal" is UNMET, not retired: RETIRED is the device for CLOSED tasks (8b→11, 8d→8f) and 6 is open, so the clause keeps its checkbox. It HELD at 6's own commit `96f5a8d` (`addPunch` mapped `m.metaId === input.metaId ? {...m, punches} : m`) and was broken by 8e (`20e020d`), whose `projectPunches` spreads every line/cage/slot/mouse unconditionally — NOT a decision 8e made (its shape text says `grid.punches` "becomes a PROJECTION", never "rebuild every object"), so 8e is not un-ticked. NEW **8g** restores the clause as written; the guarantee is BOTTOM-UP and all five rules are required (mouse by reference when its projected `PunchRef[]` is value-equal; slot when every mouse was reused; cage when every slot; line when every cage; root reuse harmless because `commit` compares the `ColonyState` object), and the SEED-ALIASING COST is stated not hidden: with reuse the initial projection returns `SEED_COLONY` itself so the first snapshot ALIASES the fixture — safe today (immutable-by-construction mutations, `moveMouse` clones before splicing) but `mockColonyStore.ts` says "never mutate SEED_COLONY arrays", so it is an AC not an assumption. NEW **8h** — `mintPunch` owns the `{grid, punches}` re-derivation plus a DEV-GATED duplicate-`punchId` guard at `commit()`, same species as `addPunch`'s `untagged` refusal (a store-level rejection of a programmer error, NOT a test); REJECTED with its reason so it is not re-proposed: a branded `punchId` is genuinely structural but lands a mock-era-only invariant in `packages/types` and needs blessed casts at `seedPunches` and at the MVP2 deserialiser, where the server owns uniqueness and the brand degrades to a cast site; the guard has TWO HALVES with DIFFERENT LIFESPANS — uniqueness survives MVP2 and then validates server responses, `max(punchId) < nextPunchId` is mock-era only. NEW **BACKLOG, no date** — path-copy `moveMouse`: the obvious fix does NOT work, re-projecting after `structuredClone` CANNOT restore identity because the clone already produced fresh mice and an identity-preserving projection would faithfully reuse the clones, so routing `applyColonyMove` through `commit()` alone is a no-op and not worth a task; only a path-copy `moveMouse` does anything, deferred because drag-and-drop shipped. 8g/8h use temporary dev PROBES observed then removed (8d AC (2)'s device, §19 deliberate breakage) — tests stay SUSPENDED
2026-09-22 docs: RECORD-ONLY from the same review, no AC and no task — the `punches`/`punches` naming collision now sits inside task 7's AC text (`ColonyState.punches` vs `MouseCell.punches` deliberately share a name, so the AC reads "punches.length equals the sum of punches.length"; verified as intended at 44 == 44, text deliberately left as is); `colonySeed.maxPunchId`'s comment says the counter's source "moves to the log", a move that did not happen (its load-bearing half, ids-are-never-reused, is true); 8f's scope text names "the mint in `buildMouseCell`" when the mint lives in `addMouse` via `mintPunch` (`colonyMutations.ts:123-128`); 8d's "regenerate SCHEMA.md + ERD" scope item was not run and would have been a NO-OP DIFF anyway, since SCHEMA.md records indexes and not CHECK constraints; and MVP-ladder L3 — the mock fixture is isolated by FILENAME (`apis/getColonyGrid.mock.api.ts`) rather than by its own directory, with a header calling it "anonymised-but-realistic", the opposite of the ladder's "unmistakable / marked values" (same class as the `NewTaskDialog` L3 note, architect owns any ruling)
2026-09-22 client+docs: DEPLOY VERDICT recorded so the state is legible to whoever picks this up next — architect and reviewer AGREE that **nothing found in this closing pass blocks MVP1**: every finding is value-correct at HEAD `23474e7`, and the only observable is re-render count on a demo-sized colony. 8g and 8h are CONTRACT work, not incident response
2026-09-22 docs: landing hashes filled in on the four tasks that closed without one — 6a `c68dd5a`, 6 `96f5a8d` (still `[~]`), 7 `9748ab1`, 8d `7d94b06`. The pm pass that closed them flagged the gap rather than guessing: every other `[x]` in the phase file carries hash + PASS, and those four carried only "verified at HEAD `23474e7`". Main session had the hashes in context; they are the commits those tasks actually landed in, not the HEAD the reviewer verified against
2026-09-23 docs: P0.7-b's LAST FOUR tasks CLOSED `[x]`, all reviewer PASS — **9e** (`81f4abc`; both greps proved non-vacuous by deliberate breakage, `TS2305` on re-importing `nextLitterCode` and `MouseDetail`) with **ACs (3)+(4) RESTATED, not passed as written**: they demanded grep counts of 1 and 0 while the residual hits sat INSIDE the `x_`-soft-deleted files, so meeting them literally required `rm` or gutting those files — both forbidden by `rules/core.md`, which outranks task text; restated with `x_*` excluded (precedent: task 11's AC (5)), and the files have since been removed by the OWNER (`b4d5e19`), so the restatement is about what the AC should have said, not today's counts. **8g** (`0a95e34`) — reuse verified at ALL FOUR levels, not just the mouse: exactly one mouse, slot, cage and line differ after `addPunch`, asserted on COUNTS so a "mouse reused but slot rebuilt" defect could not hide; seed aliasing confirmed real and SAFE (init returns `SEED_COLONY` itself, fixture deep-equal unchanged after adds, removes, four rail creations and a move); its scope is `addPunch`/`removePunch` only — the four add mutations still spread lines and cages broadly, recorded not opened since no criterion claimed them. **8h** (`0a95e34`, corrected in `517d8a0`) — the guard throws on a duplicate `punchId` and on `max(punchId) >= nextPunchId`, at `commit()` and at init, inert under `NODE_ENV=production`; `517d8a0` also stopped the counter advancing on a REJECTED write, and two overstating sentences are fixed: the single-writer property is **ENFORCED, not structural** (a hand-built `[...state.punches,row]` with a unique id and advanced counter still commits). AC (1) corrected to name `deriveColonyState`, not `mintPunch`. **Step 6 CLOSED** (`96f5a8d`) — its one open clause was "every other mouse is reference-equal", restored by 8g, every other criterion re-verified PASS in the same run; nothing carried forward (§7). RECORD-ONLY, no task: `commit()` is bypassed by `updateMouse` and `applyColonyMove` (two unguarded paths, neither able to corrupt the log), and the uniqueness half's promised MVP2 server-response validation holds in DEV BUILDS ONLY, which its comment does not say
2026-09-23 docs: **P0.7-b ARCHIVED — the split this repo deferred twice is done** (owner: "when tasks done, and steps can be closed. then we can archive tasks and steps."). `docs/phases/p0.7.tasks.md` **1890 → 267 lines**; the completed detail (steps 1, 2, 3, 4, 5, 6, 6a, 7, 8, 8b, 8c, 8d, 8e, 8f, 8g, 8h, 9a, 9b, 9c, 9d, 9e, 11, 13, the superseded-not-shipped inline `sex` editor, the 10/12 merge notes, the deploy verdict, the closing review's record-only findings, the spent SPLIT-DEFERRED note and the P0.7-b preamble) moved to `_archive/tasks.p0.7-b.md`. §2 MECHANICS HONOURED: destination written and read back BEFORE any cut, and the cut was done as chunked Edits whose `old_string` was the same text already in the destination — an Edit that succeeds PROVES that block was byte-exact in both files (no `diff` available in this agent's tools). Nothing deleted, nothing `rm`ed. STAYS LIVE, per §2's "never archive a set while any of its tasks is open": the six base P0-b tasks, the BACKLOG path-copy `moveMouse`, the `NewTaskDialog`/`SEED_COLONY` note (architect's), the `x_`-inert-inside-`packages/` note (owner's, its instance `x_mouseDetail.ts` annotated as removed by `b4d5e19` while the CONVENTION GAP stays open — DRIFT rule), plus three record-only findings about CURRENT CODE (8h's two `commit()` bypasses, 8g's mutation-scope limit, the MVP-ladder L3 fixture-isolation note). **RULING — the AC-defect PATTERN block STAYS LIVE, not archived:** it is guidance for writing the NEXT AC, not a record of finished work, `rules/docs.md` §7 cites three of its instances generically, and today's closes (9e, 11) lean on it as precedent; its instances now point at the archive. Its natural home is cross-cutting (`TASKS.md` or docs.md §7), but `TASKS.md` is itself past the hard line — that move is the owner's call, not this pass's. `TASKS.md`'s P0.7 stub was ~300 lines of history wrong on state in a dozen places; REPLACED with a §1 state stub and the old text preserved VERBATIM in the archive under "TASKS.md P0.7 stub as it stood before the archive"
2026-09-23 docs: TWO CORRECTIONS to the archive line above, appended not struck (§4 — the line is otherwise true). (a) **The verification claim was overstated:** a succeeding Edit proves `old_string == SOURCE`, i.e. the cut was byte-exact on the source side; it does NOT prove `archive == source`, which rests on the same text being re-typed in two calls of one session plus a heading grep of the destination (24 checkbox items, every section present). The pre-cut file is in git at `517d8a0` — whoever has Bash should diff `git show 517d8a0:docs/phases/p0.7.tasks.md` against the archive's moved blocks to close the gap properly. (b) **That line called `TASKS.md` "past the hard line" while the same pass was making it false:** the stub replacement took `TASKS.md` 535 → ~264 lines, UNDER the 300 soft line, so relocating the PATTERN block there is feasible whenever the owner wants it. The phase file's PATTERN header is corrected in place with the re-counted number — a number inherited instead of re-grepped is that block's own defect shape. Also RECORDED, not actioned, pre-existing DRIFT: `PLAN.md:173-174` still says the four-mutation add-flow split is "NOT built — TASKS P0.7-b step 8", but step 8 shipped and is now archived
2026-09-23 docs: **NEW P0.7-c — genotype as GENE ROWS**, two shipped commits recorded `[x]` with their criteria checked against SOURCE this pass — `ca216c2` stored `MouseCell.genotype` → `genes: GeneRef[]` composed at read by `lib/genotype.ts` (grid.ts:155; sole composer, 10 call sites) + migration `0029` adding NULLABLE `mice_genes.allele_pat`/`allele_mat` with no DEFAULT/CHECK so NULL/NULL ("not recorded", `PlpCre`/`WT`) and `'+'/'+'` ("recorded wild-type pair") stay DIFFERENT facts; and `3932157` widening `LITTER_CODE_RE` to `/^([A-Z]{3,5}|WT)$/` — `WT` is a real litter code on 10 of the 22 seed mice, so the dropdown and the validation gate were dropping them, while two-letter typos still fail because the fix is an ALTERNATION not a length change. Both ticked WITHOUT a reviewer PASS line (none supplied) and their "22 mice, 0 mismatches" equality is COMMIT-REPORTED, not re-run — this agent had no shell; the caveat is written on the tasks, not hidden. §7 carry-forward: NEW `[ ]` for `InGridParentCell.genotypeColor`, which is STILL A STORED COPY of a value derived from the genotype that same interface no longer stores (grid.ts:98-101, rendered `ColonyGridView:1785`) — AC scoped to that interface because `MouseCell`/`OutsideParentCell`/`LineRow` colour keys legitimately survive (the FIFTH-occurrence trap). RECORDED NOT SCHEDULED, each with its reason: `G_WT` is one mutable array shared by 12 fixture mice (same class 8g ruled SAFE; `readonly GeneRef[]` is the cheap hardening), `getGenes()` is exported+uncalled exactly like `getColonyGrid()`/`getTasks()` (the named mock→real seam, verified), and the TWO gene lists are deliberate — `GENE_CODES` (4, no WT, "genes to check") vs `GENE_CATALOG_CODES` (5, with WT, "assign a genotype") — with the drift hazard named once: `GENE_CODES` is hand-written, not derived
2026-09-23 docs: **P0.7-c task 4 opened `[ ]` — `genes.sort_key` + migration `0030`, OWNER-APPROVED, NOT STARTED, written-not-run.** `0007_breeding_and_records.sql:12-14`'s own header was VERIFIED before being used as the rationale (`order_index` is "the TOTAL ORDER that makes rendering deterministic" and "NOT a biological claim about marker precedence" — the job `sort_key` takes over), as was `mouse_meta.raw_genotype` (0002:297, SCHEMA.md:294) which makes `order_index`'s second role redundant, and the live index `mice_genes_order_key (mouse_id, order_index)` (SCHEMA.md:264) that `(mouse_id, gene_id) WHERE deleted_at IS NULL` replaces, closing `[Nf1, Nf1]`. THREE scope items the dispatch did not name, found by grep: `mintGeneRefs` sets `orderIndex` from PICK ORDER, so `Gene` + the `GENES` mock must gain `sortKey` or the sort has no source; `scenarios/flow-weekly-cycle.sql:37,41` + `scenarios/README.md:22` INSERT/ORDER BY/document `order_index` and go stale on landing (same scenario line 36 still inserts the pre-0029 baked code `'Nf1 f/+'` — recorded separately); and NO migration seeds `genes` at all (`0014` defers it to a data migration that does not exist), so the gap seeding must be INSERT-or-UPDATE by code. Exit bar is `ca216c2`'s — byte-identical genotype for all 22 fixture mice — and it is falsifiable rather than vacuous because both multi-gene fixture sets put PlpCre first, which `sort_key` 10 reproduces. ALSO FOUND: `SCHEMA.md:251-266` shows `mice_genes` WITHOUT `0029`'s columns, so either 0029 is unapplied to `colony_dev` or SCHEMA.md was not regenerated — settle which before 0030 runs anywhere
2026-09-23 plan: **§5 Q47 + Q48 added** — Q47 the gene/genotype COLOUR model, DECIDED IN PRINCIPLE by the owner (*"each gene has a color and +/+ will influence color but not this time"*) and DELIBERATELY NOT IMPLEMENTED: palette keyed by the WHOLE composed genotype label, so a zygosity difference is a different key and a different colour (`Ai14` ≠ `Ai14 +/+`), order normalised by `sort_key` so input order cannot split one mouse into two colours, `WT` and `?` get NO colour (preserving the property `getColonyGrid.mock.api.ts:27-29` already states — only mutant genotypes carry a hue so they pop), unseen genotype assigned a colour and stored. Its three OPEN parts are recorded as open, not answered: palette-as-STATE in the mock era (today `GENO` is a hand-written constant, nothing assigns at runtime), colour EXHAUSTION at the 13% alpha the grid actually renders (`${color}22`, ColonyGridView:1975/:1785 — ~12–20 distinguishable hues against far more label combinations; reuse or fall back to no-fill?), and that this is an ALTERNATIVE to the per-gene-hue-with-zygosity-modulation idea, never both. Q48 asks whether `mice_genes` should validate gene COMBINATIONS at all (`[Nf1, WT]` is accepted and unvalidated; `0029` added no CHECK on purpose, and task 4's new index closes only the DUPLICATE case) — professor's, not an architect's. PLAN.md §5 stub updated in the same edit (docs.md §1). NUMBERING COLLISION HANDLED, not renumbered: `docs/phases/p0.3.tasks.md:62` cited a `plan §5 Q47` that did not exist (recorded 2026-09-22) and now resolves to the WRONG question — annotated AT THE CITATION SITE as dangling, and the stub says so too
2026-09-23 docs: **RECORDED OPEN FOR THE OWNER — ALLELE ORDER CONFLICT, shipped code vs a standing owner decision, NOT resolved here** (`rules/core.md`: conflicts STOP and go to the owner). `STATUS.md` 2026-09-17 records the owner deciding the pair is ORDERED = PARENT OF ORIGIN, "first MATERNAL, second PATERNAL", with the columns named `allele_maternal`/`allele_paternal` precisely because "a generic name is how someone fills an ordered pair backwards"; `docs/phases/p1.genotyping.plan.md:13-18,24-30` carries the same as P1 design. SHIPPED `0029:15-16` says the opposite — "the paternal and maternal allele, in that order", rendered `code allele_pat/allele_mat`, and `grid.ts:62` + `genotype.ts:31` follow it. Under the 09-17 reading every heterozygote in the fixture is displayed with its parents swapped. Cannot be told apart from here: either the owner reversed it on 09-23 or `0029` inverted a standing decision. The 09-17 line is NOT struck (§4 — its content may still be live); the conflict is recorded at both sites (P0.7-c and `p1.genotyping.plan.md`) and escalated. RELATED DRIFT annotated in the same pass: `PLAN.md`'s P1 bullet and `p1.genotyping.plan.md` both still described a flat `MouseCell.genotype` + a `GeneCall` DTO that `ca216c2` made impossible, and the P1 file's "Keep `order_index`" line is retired by task 4 — annotated in place, stub and detail together, neither rewritten
2026-09-23 docs: CORRECTION to the P0.7-c line above, appended not struck (§4 — the rest of it stands) — **the in-grid parent `genotypeColor` drift is a MEMO, not a `[ ]`. OWNER RULED** (*"whats the problem? leave a memo."*), so the `[ ]` that line announced was never opened: no date, no target, nobody picking it up. Recorded with the reviewer's reproduction against the real `updateMouse` — add a gene to mouse 101 (father of 103) and 101's OWN cell correctly goes no-fill because `lib/updateMouse.ts` nulls its own `genotypeColor`, but nothing syncs `parents[].genotypeColor`, so row 103's father sub-cell keeps painting the old teal `#0d9488` behind the NEW text `Nf1 f/+;WT` — one mouse in two colours on one screen, the tint claiming a genotype that no longer exists. NOT URGENT (mock-era only; the fixture supplies the colour today and the server resolves it later; data is value-correct, only the tint is stale) but WORTH KEEPING: it is the same stored-copy-of-a-derivable defect class this whole arc removed, and the cell is now HALF-migrated — string fixed, colour not. Filed alongside the deferred colour model (`plan §5 Q47`), which cross-links back, since both sit in one design area. **§7 BENT DELIBERATELY AND NAMED**: an unmet criterion of a ticked task would normally become its own `[ ]`; the owner outranks the rule (`rules/core.md` authority order), and the bend is written on task 1 itself so it cannot be read as an oversight. §7 still applies unchanged to everything else in this pass
2026-09-23 docs: **P0.7-c SPLIT to `docs/phases/p0.7-c.tasks.md` the day it opened** (docs.md §1) — adding it inline took `docs/phases/p0.7.tasks.md` to ~409 lines, past the 400 HARD line, and the "artificial seam" escape hatch does not apply: P0.7-c shares no private context with the six base P0-b tasks or the AC-defect PATTERN block it was sitting between. §2 mechanics honoured — destination written and READ BACK before any cut, cut performed as chunked Edits whose `old_string` was the text being removed — that proves the SOURCE cut, NOT destination equality (the line-78 overstatement, avoided here): the destination was AUTHORED, not copied, and its deltas are listed below. Destination and cut text are NOT byte-identical ON PURPOSE, stated in the new file's header: three inherited numbers/symbols were re-grepped and corrected during the move — the `genotypeOf` call-site count in `ColonyGridView.client.tsx` is **8** (`:195 :234 :314 :393 :567 :903 :1986 :1990`), not the "6" first written; a `byMetaId` symbol that was never grepped is no longer named; and "the 8 `ParentCell` fixture entries" became "every in-grid entry", with the two outside parents GREPPED (`metaId: null` at `getColonyGrid.mock.api.ts:282,:288`) — three instances of the PATTERN block's own first defect shape, caught in the file that hosts it. Two task-4 ACs also RESTATED away from bare-identifier greps (`orderIndex:` KEY DECLARATION and `.orderIndex` PROPERTY ACCESS, 9d's restated form) because a comment saying "sort_key replaced order_index" legitimately re-introduces the word. `p0.7.tasks.md` ~409 → ~290 lines, leaving a state stub; `TASKS.md`'s P0.7 stub carries the same state and the new `Detail:` link, updated in the same edit
2026-09-23 plan: **ALLELE ORDER RESOLVED (owner) — MATERNAL FIRST; the 2026-09-17 decision STANDS**, *"어머니 먼저 — 9/17 결정이 살아있다"*. `0029` had INVERTED a standing decision; the owner did NOT reverse it on 09-23. Fixed in `11d0322` and VERIFIED against source this pass: `0029` corrected IN PLACE rather than compensated for by `0030` (it had never touched a database, so no archaeology) — columns now declared `allele_mat, allele_pat` (`:45-47`) with a header stating the left allele is MATERNAL and citing 09-17 (`:15-24`); `renderGene` emits `mat/pat` (`genotype.ts:37-38`); `GeneRef` declares `alleleMat` first (`grid.ts:62-74`); fixture rows were **SWAPPED, not reordered** (`getColonyGrid.mock.api.ts:54-55,70-90`) so the rendered string is unchanged and only the MEANING is corrected; and the rule now lives at the migration header AND at `renderGene`, not only in a phase doc, because it had been inverted once already. HISTORY KEPT, not erased — that `0029` shipped inverted and was caught by a REVIEWER comparing code against `p1.genotyping.plan.md:59` is the useful part. GENERAL TRAP kept with it: task 4's byte-identical exit bar would have CERTIFIED the paternal-first strings as correct had the inversion not been caught first — an equality bar preserves whatever it is anchored to, defects included, which is exactly why the fixture was swapped rather than reordered. The three escalation sites (P0.7-c, `PLAN.md` P1 bullet, `p1.genotyping.plan.md`) are converted from OPEN to RESOLVED in this pass
2026-09-23 docs: **P0.7-c task 4 CLOSED `[x]` (`11d0322`)** — developer evidence, NO independent reviewer pass, recorded as such. Every criterion re-checked against source: file is `0030_gene_sort_key.sql` (SINGULAR `gene` — the task predicted `0030_genes_sort_key.sql`, corrected rather than inherited), seeds PlpCre 10 / Nf1 20 / Ai14 30 / ccEGFP 40 / WT 50 with gaps of 10 and `NOT NULL` no-default so a surviving 0014-era code like `'Nf1 f/+'` fails LOUDLY; `GeneRef` declares `sortKey` with no `orderIndex:` left in `packages/types/src`; `mintGeneRefs` copies `sortKey` FROM THE CATALOGUE (`geneSortKey(code)`), not from its map index, and `inOrder` sorts on it; `0030:64` drops `mice_genes.order_index` and `:75-77` creates `mice_genes_mouse_gene_key (mouse_id, gene_id) WHERE deleted_at IS NULL` (FILE-READ, not a live probe — no Postgres this session, recorded the way 8d's migration AC was); exit bar met at 22 rows / 0 mismatches across BOTH the sort_key change and the allele swap. Two things this task's scope PREDICTED and the original dispatch did not name both shipped: `Gene` + the `GENES` mock gained `sortKey`, and `GENE_CODES` is now DERIVED (`GENE_CATALOG_CODES.filter(...)`, `taskTypes.ts:14`) after a reviewer found the standalone literal had silently drifted — so this file's "hand-written drift hazard" note is CLOSED, while the two-list DESIGN stands. §7 CARRY-FORWARD, AC (6) unmet → NEW **task 5**: the three `order_index` references are now stale AND `scenarios/flow-weekly-cycle.sql:37,39,41` will FAIL against any DB that has run `0030` (it still INSERTs the dropped column and aggregates `ORDER BY mg.order_index`); `scenarios/README.md:22` and `docs/phases/p0.4.tasks.md:19` carry the same dead contract, and the same scenario's line 36 still seeds the pre-0029 code `'Nf1 f/+'` — the exact row `0030`'s `SET NOT NULL` is built to reject. NEW **task 6**, owner-visible NOTE: nothing in `migrations/` seeds `genes` (`0030`'s own header says so), so the five `sort_key` values live ONLY in the mock catalogue and `0030`'s `CASE` — WHERE the catalogue seed belongs (seed migration / P0.5 generator / import) is the owner's call, closing condition named
2026-09-23 docs: reviewer verdict for `ca216c2` RECORDED — **PASS**, and it did not trust the commit: it parsed `ca216c2^`'s fixture and ran `genotypeOf` over `ca216c2`'s to re-derive the equality (`mice=22 genotype mismatches=0 color mismatches=0 distinct forms=8`), then FORCED UNCACHED gates after noticing the first run replayed from Turbo cache (`turbo run --force`: check-types 5/5, lint 0, build 5/5); all 8 claims PASS, verdict durable in the session scratchpad. `3932157` and `11d0322` still have measured DEVELOPER evidence and no independent pass — written that way at every site, not upgraded by association. Two reviewer findings recorded with the resolution: `geneRefsEqual` compared by ARRAY POSITION while `genotypeOf` sorted (a server returning the same rows in another order would have read as a change and forced a spurious write) — FIXED in `11d0322`, now sorting both sides through the same `inOrder()` and deliberately NOT comparing `sortKey`, a catalogue fact rather than part of the picked set; and `CodeBadgeSelect` has no exclusivity logic, so deselect+reselect reorders a mouse's picks — now MOOT for everything checkable in source (display sorts by `sortKey`, change detection is order-insensitive, `mintGeneRefs` takes `sortKey` from the catalogue), NOT moot for what the picker itself shows, which is Q49. UNCHANGED and still open: `SCHEMA.md` is stale — `0029` is unapplied to `colony_dev`, and the reviewer adds it was last regenerated through `0027`, so `0028` landed without a regen and `0030` makes three
2026-09-23 plan: **§5 Q49 ADDED** — should the gene PICKERS follow `sort_key` too? Since `11d0322` the GRID composes in catalogue `sort_key` order (PlpCre first) while the badge pickers still list CATALOGUE ARRAY order (Nf1 first, `getGenes.mock.api.ts:25-34`), so a user picks in one order and reads the result in another. Options named, none chosen (sort the picker by `sort_key`; leave the array order, which may have its own most-used-first logic; or make `sort_key` the array order) — owner's call, nothing assumed and nothing changed. PLAN.md §5 stub updated in the same edit, 48 → 49 questions
2026-09-23 docs: **CORRECTION — THERE IS NO DATABASE (owner: *"no db yet"*), which invalidates a premise three readers shared today.** `colony_dev` does not exist and never did; migrations `0001`–`0030` are PAPER CONTRACTS — nothing to apply, nothing to regenerate, and no code↔DB drift, because there is no DB side. So the lines above saying "`0029` is unapplied", "`0030` makes three" and "needs Postgres" describe a database that is not there; corrected at every site (P0.7-c body, both stubs, `TASKS.md`'s schema pointer, `PLAN.md`'s R11–R25 pointer) rather than struck, since the rest of those lines stands (§4). **ROOT CAUSE, and the finding that outlives the corrections: `packages/db/SCHEMA.md:3` asserts "GENERATED by `packages/db/scripts/schema-doc.sh` from the live `colony_dev`" — VERIFIED false this pass, it carries `pup_number_offsets` (`0027`, `:357`) and ZERO `untagged` (`0028`), so it stopped at `0027` and is a snapshot of a database that is gone, claiming to reflect a live one.** A GENERATED file whose PROVENANCE LINE OUTLIVES ITS SOURCE becomes an authoritative-looking lie: it misled THREE readers in ONE session about the single thing it exists to settle — a reviewer read it as "0029 unapplied", this pass read it as "either 0029 unapplied or SCHEMA.md not regenerated" (honest uncertainty, wrong premise in both branches), and the main session collapsed that disjunction into the wrong branch and told the owner 0029 had not been applied. Note the shape: expressing the uncertainty honestly did NOT stop the wrong conclusion downstream, because every branch presupposed a database. DRIFT class (true when written, false once its referent went) at WHOLE-FILE scale. **`SCHEMA.md` NOT EDITED** — the owner is deciding whether to correct the banner or keep it and record the hazard; recorded as an open item either way
2026-09-23 docs: P0.7-c tasks 5 and 6 RE-AIMED by the same correction — they are **BEFORE-THE-FIRST-`createdb` PREREQUISITES**, not maintenance on a running system. 5: `scenarios/flow-weekly-cycle.sql` breaks nothing today because there is nothing to run it against, which is precisely why it will be found by the person LEAST able to diagnose it — whoever builds the schema first — since it INSERTs the dropped `order_index` (`:37`), aggregates `ORDER BY mg.order_index` (`:39,:41`) and seeds `'Nf1 f/+'` (`:36`), the exact 0014-era row `0030`'s `SET NOT NULL` is built to reject; `scenarios/README.md:22` and `docs/phases/p0.4.tasks.md:19` carry the same dead contract. Its AC is FILE-LEVEL on purpose — a "run it and see" criterion would be unmeetable until the very `createdb` the task protects. 6: "nothing seeds `genes`" is likewise part of what must exist before a DB can stand up at all, since a catalogue with no rows makes `genes.sort_key` a column nobody can use, and the missing seed is what would let task 5's `'Nf1 f/+'` through. **DECIDED, and said out loud rather than left implicit: they stay TWO checkboxes** — one trigger, two deliverables (5 is mechanical with a falsifiable AC and no decision in it; 6 is an OWNER decision about where the catalogue seed lives, which no file edit can close), and merging them would put one checkbox over two deliverables, the defect this phase's own history keeps recording. Task 4's scope (g) and AC (7) are re-marked INAPPLICABLE rather than unmet — both were about applying/regenerating against that non-existent DB — with the wrong premise kept visible in place; task 4's close is unaffected, every criterion it rested on was a file read
2026-09-23 docs: `docs/phases/p0.7-c.tasks.md` is now ~412 lines, past docs.md §1's 400 HARD line, and is **NOT split — the reason is written in the file, as §1 requires**: what remains is ONE sub-phase (three closed tasks, the two prerequisites their closure produced, and the allele-order resolution + `SCHEMA.md` finding that every task cites), so any cut separates a task from the evidence that closed it — shared private context, the artificial seam §1's escape hatch names. The real remedy is §2: when 5 and 6 close, tasks 1/2/4 + the resolution record archive to `_archive/tasks.p0.7-c.md` and the file collapses to open items, as P0.7-b's did; not before, since §2 forbids archiving a set with open work
