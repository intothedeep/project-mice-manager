# living docs + their split siblings — owned by architect/pm agents (rules/docs.md).
# Reformatting them turns a format-only commit into a non-surgical doc edit.
PLAN.md
TASKS.md
STATUS.md
# root CLAUDE.md carries the owner's own words — agents never rewrite it
CLAUDE.md
docs/**/*.md
*.plan.md
*.tasks.md

# cold storage — never rewritten (rules/docs.md §3)
_archive/

# shared harness submodule — reformatting it ships churn to every repo that
# pulls intothedeep/.claude. `pnpm format` globs **/*.md and WILL reach in here.
.claude/

# soft-deleted, awaiting the owner's manual removal (rules/core.md)
x_*

# generated files — a format pass here is churn the generator reverts
apps/*/next-env.d.ts
packages/db/SCHEMA.md

# build artifacts
.next/
dist/
node_modules/
