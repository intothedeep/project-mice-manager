# status archive — client mock-UI era (2026-09-07 → 09-09)

> Cold storage (docs.md §5 roll-off). History-only. Moved from `01.status.md`
> 2026-09-12; append-only, nothing changed. The 2026-09-12 active cycle stays
> live in the log.

2026-09-07 client: frontend-first Cage Grid prototype (mock, read-only) shipped for professor demo — colony_client_web home renders Excel colour semantics (done/instruction/plan/flag/dead) from a typed fixture; DTOs in @repo/types, seam apis/getLineGrid.mock.api.ts (mock->real = 1 file); fixture seeds M4+10AZZ/U3BCX/F10BEVe/M4BCW.2 open questions; plain-CSS demo deviation noted (Tailwind at real UI); lint+check-types green; boots on :3000
2026-09-08 client: grid extended to full 4-level hierarchy (colony>line>cage>slot>mice, slot=own level); fetcher getColonyGrid (old getLineGrid x_-soft-deleted); DTOs ColonyGrid/GridLine/GridCage/GridSlot; Hybrid explore (nav+sticky filter bar search/sex/signal, dim non-match) chosen over nav/filter/group-by; Move = cascade menu line>cage>slot (chosen over drag-drop), same-line default + new-slot + confirm breadcrumb; pure lib/gridMove+gridFilter verified 9/9 (incl append-only immutability, empty-slot cleanup, cross-line); cross-line = WARN-not-block (OPEN, ask professor); lint+check-types green
2026-09-08 client: adopted shadcn + Tailwind v4 (house convention per nexjts.md; supersedes plain-CSS demo deviation) + desktop line|cage|mouse 3-pane (Miller columns, mice grouped by slot); shadcn atoms hand-added components/ui (button/card/input/badge/label/dialog/radio-group), cn in lib/utils, components.json, Inter+JetBrains Mono via next/font (codes=mono); Move menu on Dialog+RadioGroup; chroma reserved to 5 signals only (lib/signal.ts single source), chrome neutral; verified by real browser clicks (puppeteer-core throwaway): filter-plan dim+counts, Move cascade w/ same/cross-line + breadcrumb; lint+check-types green; deps +51 (tailwind/@radix/lucide/cva/clsx/tw-merge)
2026-09-08 client: Move verified END-TO-END in browser (puppeteer 8/8) — M4BCW picked from cage 2413 → 2414/A8, asserted gone from 2413 (count 3→2) and present in 2414 (3→4); confirms Dialog→Number()→applyMove→setColony→re-render+recount plumbing, not just dialog populate
2026-09-08 client: interaction model → show-all + linked highlighting (replaces drill that hid children); all lines/cages/mice visible, active line highlights child cages, active cage highlights its mice; TWO ORTHOGONAL AXES — active line/cage = persistent structural markers, filter = strong/dim (match strong, non-match dim); filter drives mouse opacity when on else active-cage membership does, active markers persist under both; verified in-browser (line switch moves cage highlight; filter=plan dims non-match while active cage stays marked); goal reaffirmed: keep professor's mouse-mgmt SIMPLE + automatable
2026-09-08 client: Task bin prototype (mock) at /tasks + nav (Cages|Tasks) — Open/Done/Verified board, cards w/ subject chip + direction detail + due(overdue red) + actor trail; role-gated transitions via pure lib/taskFlow canTransition/availableActions (staff open→done; professor done→verified|reopen|cancel + open→cancel; admin any→any) w/ staff/professor/admin role switch; TaskCard/TaskStatus/Role DTOs in @repo/types; taskFlow in app for now (unbuilt-@repo/domain-dist trap), moves to @repo/domain when service enforces; verified 8/8 scratch + in-browser (staff Mark-done Open→Done, Professor shows Verify/Reopen/Cancel); lint+check-types green; serves the "automate" pitch (weekly loop replaces hand-versioned Excel)
2026-09-08 client: Mouse-detail drawer (mock) — click mouse id → shadcn Sheet (Radix) w/ Identity, Genotype (per-marker badges), Parents, and APPEND-ONLY History timeline (dot+date+actor, newest first); MouseDetail/GeneCall/ParentRef/HistoryEvent DTOs in @repo/types; seam apis/getMouseDetail.mock.api.ts keyed by mouse_meta_id; verified in-browser 6/6 (M4+10AZZ opens, shows history + pooled-+10 entry + parent + PlpCre marker); nav now Cages|Tasks|Upcoming
2026-09-08 client: Upcoming/due view DISPATCHED to bg developer agent (parallel, per user) — /upcoming route w/ auto date calc (plug/deliv/wean/genotype) + overdue grouping, pure lib/dueDates.ts; agent owns new files, main session added nav link; integrate on completion
2026-09-08 client: Upcoming/due view INTEGRATED (bg agent built, main session wired nav+verified) — /upcoming auto-computes plug(+10)/delivery(+20)/wean(+21)/genotype(+21) from mating/dob, groups Overdue/Today/ThisWeek/Later via pure lib/dueDates (dueStatus/daysUntil, UTC-parsed); cards abs+relative date + kind + subject; overdue=red today/soon=amber+flag later=muted; DTO local to getUpcoming.mock.api (clean parallel, no shared-file edit); whole-client check-types+lint 0 errors, /upcoming 200, drawer no regression
2026-09-08 client: New-task create UI — "+ New task" on Task bin (professor/admin gated); data-driven form lib/taskTypes.ts (task_type → type-specific fields → direction payload) for Mate/Plug/Birth/Wean/Genes-to-check/Genotyping/Move/Food/Sac w/ mouse-cage-litter pickers + gene multiselect; MATE AUTO-CASCADE → plug(+10)/delivery(+20) into Upcoming via lib/dueDates; cross-screen shared client store lib/mockStore.ts (useSyncExternalStore, seeds from SEED_*, addTask/setTaskStatus) so created tasks+cascade persist across SPA nav (mock only; real=react-query later); TaskCard+direction, TaskSubjectKind+mate; verified in-browser 7/7 (Professor creates Mate M4BCW×F5AYL → Open card; nav Upcoming shows both follow-ups auto-created); check-types+lint 0 errors
2026-09-08 client: New-task creation opened to ALL roles (staff+professor+admin) per user — removed professor/admin gating on "+ New task"; role still gates ADVANCE actions (done/verify) not creation; verified staff opens dialog
2026-09-08 client: Color-by toggle (Off/Line/Genotype, lib/colors.ts) — line=categorical hue dot+active-selection colour (avoids signal families), genotype=deterministic swatch (same genotype→same colour); signal colours stay reserved for state, only one identity channel on at a time (user chose toggle over always-on); + multi-select mice (checkbox → "N selected" bar → NewTaskDialog batch mode presetMice: subject=mice, types restricted to mouse-subject, direction.mice); verified in-browser 9/9 (line+genotype colouring, select 2→batch dialog→create→card in Task bin); check-types+lint 0 errors. Register mouse/litter wizard DESIGN PLANNED (born/external, ties P0-b) — not built
2026-09-08 client(ui): line/cage sections made recognizable — each line (pane 2) and cage (pane 3) is now a bordered rounded card with a bold header bar + left accent, active=violet-tinted header+primary border, gap between groups; theme changed to High-contrast mono (user pick) — white bg / ink #0a0a0a / violet primary #6d28d9, signal colours unchanged; retuned line/genotype categorical hues to avoid the violet primary + signal families; check-types+lint 0 errors
2026-09-08 client(ui): palette re-derived from a user reference screenshot (extracted colours w/ pillow: bg #f6f6f7 soft gray, accents pink~324/blue-violet~232/teal~156) — theme now soft-gray canvas + white cards + bright blue-violet primary #6366f1 (was stark-white + dark violet); categorical line hues → teal/pink/orange/green/cyan/slate; signals unchanged; verified rendered page surfaces #f6f6f7 + indigo present; check-types+lint 0 errors
2026-09-09 client(ui): adopted the sibling project's (project_tradelunch) LIGHT neutral shadcn schema — white bg / near-black neutral primary / light-gray (0.97) accents, radius 0.625; LIGHT ONLY (user: no green/dark for this project); mono font switched to IBM Plex Mono to match sibling; active line/cage now marked via primary/10 tint + near-black left border (accent==secondary in neutral so contrast moved to border+tint); signals + categorical line/genotype hues kept; verified rendered surfaces #ffffff/#f7f7f7 neutral; check-types+lint 0 errors
2026-09-09 client(ui): square + dark-line + all-mono per user — radius tokens all 0 (no rounded edges), border/input darkened oklch 0.922→0.55 (~#717171 crisp line) + ring 0.45; explicit pills squared (filter/gene chips, count badges, selection bar, genotype swatch); active line/cage sections use crisp border-primary (dropped shadow-sm); whole UI now IBM Plex Mono (font-sans→mono var, body font-mono, Inter removed); verified compiled CSS --radius:0rem / --border:#717171 / body font-mono; check-types+lint 0 errors
2026-09-09 docs: created docs/rendering-performance.md — analysis of client render perf at 300 cages/6000 mice (current renders ALL mice, ~50k DOM nodes, full re-render per state change) + options (virtualization / server-scoped fetch / lazy cages / memo+debounce / server filter+pagination); ANALYSIS ONLY, not implemented
2026-09-09 client: tasks gained signal (instruction/plan/note) + createdAt (=DB created_at) — TaskSignal DTO, 7 mock tasks tagged, create dialog has a Signal picker (default instruction); cards coloured by signal via light BACKGROUND tint (instruction red/8, plan blue/8, note muted) + coloured signal label (left thick border removed per user, bg carries it); footer shows "created <date>"; verified served /tasks: 4 instruction/2 plan/1 note bg tints, 0 border-l-4, createdAt present; check-types+lint 0 errors
