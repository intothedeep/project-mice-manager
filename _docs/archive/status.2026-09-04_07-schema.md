# status archive — 2026-09-04 → 2026-09-07 (DB schema + ETL scope era)

> Rolled off from 01.status.md per docs.md §5 (periodic roll-off). Cold
> storage — history only. Migrations 0001-0020 + R1–R25 schema fold shipped.

2026-09-04 docs: 00.plan/00.tasks/01.status authored from architect+ml briefs
2026-09-04 pm: re-scoped per gpt_01 — P0 split P0-a/b/c (Breeders-only import, cage/mouse/move/task MVP); litter+mouse_move in; genotyping-normalization+xlsx-export deferred; 5 new open questions (gpt_01)
2026-09-04 pm: folded R1/R2/R3 — role-gated task transitions + append-only task_status_history (P0-a); pending `transfer` table + drag-drop viz (P0-c, mouse_move untouched); `snapshot` versioning + publish tx (P1, P0-b pull-forward option); open questions 14–18 added
2026-09-04 pm: folded schema refinements R1–R6 — slot+mouse_line entities, mouse_id/cage_number unique-alone, litter-code {3,5}+litter_seq sort key, open tag-suffix, genotype_label; genotyping DEFERRED→P1 (testing tickets+per-marker entry); Excel-familiar grid + mouse-history timeline → P0-a; pedigree tree + version replay → P1; OQ 2/4/5/11/12 updated, 19–22 added
2026-09-04 pm: folded append-only+surrogate identity (R7) — mouse=birth facts, mutable→mouse_attr_log(+view); natural key (litter_seq,pup_number) partial-unique, label non-unique; mouse_id_sequence dropped; sex+U; slot UNIQUE(label); mouse_move slot cols; ZZZ→AAAA; Q19–22 closed, Q2/Q4/Q11 residual
2026-09-04 pm: identity naming fold — mouse PK renamed mouse_seq_id (mouse_id label unchanged); litter gains mouse_letter_id+litter_seq (sort via JOIN litter); natural key → (litter_id,pup_number); new mouse.reclip_add (+N re-clip, Q23 residual); FK refs renamed in mouse_attr_log/mouse_move/transfer/genotyping
2026-09-04 pm: folded R8/R9 — tables PLURAL (task_status_history→task_status_transitions); litter_seq column dropped, litter order derived (length-first + COLLATE "C", CHECK ^[A-Z]{1,5}$); litter_code_counter single-row FOR UPDATE; base-26 codec stays pure-domain for generation; PK id/FK mouse_id (mouse_seq_id,mouse_ref retired); reclip_tag pipe-joined; client→domain dep added; parser tests Vitest not pytest
2026-09-04 repo: git init + baseline commit (docs+.claude rules only; _assets real xlsx gitignored) (68acc3f)
2026-09-04 plan: Q11 RESOLVED — pup_number = distinguishing number within a litter only (no birth-order/tag semantics, no CHECK); auto-assign 1..N unblocked for new litters, imported numbers preserved
2026-09-04 scaffold: pnpm+turbo monorepo; deps client→{types,domain}, server→{types,db,domain}, domain→types (ec32373)
2026-09-04 db: postgresql@17 local; Pool singleton + forward-only checksum migration runner; edited-after-apply fails loudly, exit 1 (db00dea)
2026-09-04 db: P0.2 schema 0001-0006 applied from empty; R9 length-first order proven on live PG (ZZZ<AAAA); append-only triggers + enum-cast validation verified by deliberate breakage (3c9527c)
2026-09-04 plan: Q24/Q25/Q26 added — mouse_status unassigned, attention none/flag, cages.status TEXT are judgement calls pending professor
2026-09-04 db: architect fixes — mice.raw_mouse_id + mice_pooled_raw_key (pooled rows had NO uniqueness guard); TRUNCATE guards on all 4 append-only tables; to_cage_id NOT NULL; users.role CHECK; slots/mouse_lines provenance; severity CHECK; attr value NOT NULL
2026-09-04 db: runner detects deleted applied migration (exit 1); ROLLBACK no longer masks original error; @repo/db stops exporting raw pool, adds Executor type
2026-09-04 decision: role vocabulary unified on professor (P1 genotyping said director); migrations 0001-0006 amended in place — last chance, zero data; forward-only from first seed
2026-09-04 correction: status line above re parser tests is stale — runner is Jest + ts-jest, NOT Vitest (CLAUDE.md §19)
2026-09-04 plan: R10 folded (workbook-driven redesign) — 6 Breeders columns had no schema home (TISSUE 97%, GENOTYPING 98%); new matings/mouse_genotypes/mouse_events/notes; mice 19→17; enum count 5→6
2026-09-04 db: Q19 FALSIFIED by real file — slot F5 exists in two cages; slots.label global UNIQUE → partial UNIQUE (cage_id,label). Same soft-delete UNIQUE bug found on cages.cage_number, mouse_lines.name, litters.mouse_letter_id — all four → partial
2026-09-04 plan: matings kept as a FACT table, not folded into tasks — proven by a NEGATIVE mating→delivery gap (-5d), so the sheet describes two different cycles and overwrites all prior ones
2026-09-04 decision: tasks+notes append-only via SELF-REFERENCING origin id (first row = itself, id drawn from a sequence, no post-insert UPDATE) — supersedes thread_id UUID; keeps a real self-FK, retires the task_identities side-table idea
~~2026-09-04 decision: FKs STAY ON pre-production; hand-edits use SET session_replication_role=replica (adding FKs later fails on orphans)~~ <!-- SUPERSEDED 2026-09-05 -->
2026-09-05 decision: SUPERSEDES the previous FK line — no bypass at all. FKs enforced ALWAYS, relations used at every stage; session_replication_role removed from the plan and must not be used. Circular refs resolved by CREATE-then-ALTER ordering, not deferred enforcement
2026-09-05 schema: matings.cage_id REMOVED — workbook has no "cage where mating happened" (col D is current cage); derivable from mouse_moves at mated_on. litter_id KEPT: parent back-lookup cannot disambiguate repeat cycles
2026-09-05 schema: cage/slot divergence hole found by deliberate breakage (mouse in cage 1 with a slot of cage 2 inserted cleanly) — fixed with slots UNIQUE (id,cage_id) + composite FKs on mice and mouse_moves; MATCH SIMPLE keeps slot-less rows legal
2026-09-05 schema: append-only ABANDONED (reject_mutation + triggers dropped); all tables mutable with created_at/updated_at/deleted_at + shared set_updated_at trigger; origin_id versioning KEPT for tasks/notes with current_* views
2026-09-05 db: 0007_breeding_and_records + 0008_task_mating_link added; all 8 migrations apply clean on a dropped/recreated colony_dev
2026-09-05 schema: mice.cage_id KEPT — verified against the workbook, the Experimental sheet has NO Slot column (117 of 143 caged mice have no slot), so cage cannot be derived from slot_id; composite FK guards the overlap instead
2026-09-05 schema: current_tasks/current_notes views REMOVED (user) — callers write DISTINCT ON (origin_*_id) ORDER BY origin_*_id, id DESC; origin-id versioning unchanged, tasks_origin_idx/notes_origin_idx still serve it
2026-09-05 schema: matings.litter_id RENAMED baby_litter_id (user) — the litter this cycle PRODUCED, distinct from mice.litter_id which is the mouse's own BIRTH litter; index matings_baby_litter_key
2026-09-05 schema: tasks subject CHECK proposed then WITHDRAWN — falsified by workbook (Experimental r25-27 'CHECK FOOD' room-level tasks have no mouse; 'move X to cage Y' names both). Left unconstrained by user decision, recorded as Q31; rationale pinned in 0005 so it is not re-added
2026-09-05 docs: ERD (graphviz, core + full) and SCHEMA.md generated FROM the live schema via packages/db/scripts/{erd,schema-doc}.sh — cannot drift from migrations
2026-09-05 analysis: Q2 '+N' grammar measured against the workbook — 446 '+' forms all have ONE genotype/DOB/cage/slot (not aggregates); postnatal-timepoint reading killed (DOB->tissue gap 7-15d regardless of N); sum reading makes litter numbering continuous past 10 (YO 1-10 then 11-16, QE 8-10 then 11-19), 17/446 collisions some of which are the same mouse written both ways
2026-09-05 decision: is_pooled default KEPT for this version (user) despite the evidence above — recorded in Q2 as a known risk, NOT to be silently changed; cost is 446 mice excluded from mice_litter_pup_natural_key and from R9 litter ordering
2026-09-05 decision: PROTOTYPE '+N' rule (user) — parse only sex + LEADING pup_number + litter code (M4+10AZZ -> M/4/AZZ); +N not parsed, is_pooled NOT set, original kept in raw_mouse_id AND a notes row; reclip_tag stays unused; exceptions deferred to after the prototype
2026-09-05 verify: leading-number rule gives 0 collisions across 175 (litter,pup) pairs on Breeders (the only P0 import target); other sheets show 146/3528 (4%) of shape M1+10A vs M1A and are out of P0 scope
2026-09-05 schema R11: litters + litter_code_counter REPLACED by mouse_ids (code UNIQUE + base-26 seq); code is a BIRTH COHORT id, measured 37/54 Breeders codes shared by 2-8 mice all with identical DOB (37/37)
2026-09-05 schema R11: litters MERGED into matings (were already 1:1 via matings_baby_litter_key, duplicated the parent pair) — matings now carries mother/father + mated_on + birth_date + pup_count + code_id; mice.litter_id -> mice.birth_mating_id, natural key (birth_mating_id, pup_number); tasks.litter_id dropped
2026-09-05 schema R11: label 'M4BCW' no longer stored anywhere — composed from sex + pup_number + code; R9 length-first index retired in favour of mouse_ids.seq; tables 21->20, FKs 53->48; verified by breakage (dup code rejected, 5 mice share one BCW, dup pup rejected, label round-trips)
2026-09-05 schema R12 (supersedes R11): mouse_ids -> mouse_meta (birth-given, immutable, mouse_id UNIQUE per mouse); mice becomes APPEND state rows (cage/slot/status) keyed by mouse_meta_id, current = DISTINCT ON head query; all other tables repointed to mouse_meta
2026-09-05 schema R12: matings SPLIT into mates (couple: mother+father, created at pairing) and litters (one cycle of that couple: litter_no, mated_on, birth_date, pup_count); pups (litter_id, pup_number) added; mouse_meta.pup_id links grown mouse to its pup slot
2026-09-05 verify R12: dup couple rejected, one couple with 2 litters (BCW/BGX) distinct dates, dup (litter,pup) rejected, dup mouse_id rejected, cage move appended 3rd mice row with head query returning 1, cage/slot composite FK still enforced
2026-09-05 schema R13: litters.litter_no REMOVED (litter = integer id only); professor's code moves to mouse_meta.code, NOT unique alone since littermates share it; label = sex||pup_number||code is the unique triple (173/173 distinct on Breeders)
2026-09-05 schema R13: mouse has TWO ids by design — mouse_meta.id (ours) + mouse_meta.code (professor's) — to reconcile old and new systems; pups table FOLDED into mouse_meta (held only litter_id+pup_number, and splitting them put label uniqueness beyond any UNIQUE)
2026-09-05 schema R13: outside mice admitted with code/litter_id/pup_number all NULL; CHECK mouse_meta_pup_needs_litter rejects pup_number without litter_id (names nothing) but allows litter-known/number-unknown; verified by breakage
2026-09-05 schema R14 (corrects R13): letter code moves BACK to litters.litter_code (UNIQUE) + seq from litter_code_seq; mouse_meta.code and .seq REMOVED, mouse_code_seq dropped — a mouse has ONE stored id, ours
2026-09-05 schema R14: professor's label is composed sex||pup_number||litters.litter_code, never stored; label uniqueness now free from litters_litter_code_key + mouse_meta_litter_pup_key, so the R13 triple index is dropped and the littermate-code-divergence risk is structurally gone
2026-09-05 verify R14: dup litter_code rejected, BCW cohort of 5 composes to F1/F2/F3/M4/F5BCW by join with exactly 1 distinct code, outside mouse admitted with litter/pup NULL
2026-09-05 schema R15: mouse_meta.litter_code added as a DENORMALIZED copy so the professor label needs no join; pinned by composite FK (litters UNIQUE (id, litter_code) + mouse_meta FK ON UPDATE CASCADE), MATCH SIMPLE lets outside mice through
2026-09-05 verify R15: wrong code for the litter rejected, nonexistent code rejected, litter rename BCW->BCX cascaded to all 5 mice in one UPDATE, outside mouse admitted with both NULL
2026-09-05 scenarios: ran the professor/staff weekly cycle as SQL (packages/db/scenarios/) — 7 gaps found, 2 outright failures (litter-level notes rejected, tasks had no assignee)
2026-09-05 schema R16: notes redesigned (subject_mouse_id NULLABLE, note_type + meta JSONB, signal_id FK); cell_signal enum -> signals TABLE (id,type,color) seeded from the workbook colours
2026-09-05 schema R16: groups + group_members + tasks.assigned_user_id/assigned_group_id (CHECK at most one) + tasks.direction JSONB + assignables view for the picker
2026-09-05 schema R16: mouse_attr_logs DROPPED (2nd source of truth) -> mice.is_alive + death_reason; mouse_moves DROPPED (mice version rows ARE the move history) -> mice.reason + idempotency_key + in_transit
2026-09-05 schema R16: outside mice get a litter (litters.is_from_outside) so mouse_meta.litter_id/pup_number are NOT NULL and the re-import key covers every mouse; sex moved mouse_meta -> mice so corrections keep history
2026-09-05 schema R17: a GROUP IS A USER (users.type 'user'|'group'); groups holds only group meta pinned by composite FK (user_id,user_type) so a person cannot get group metadata; tasks.assigned_to is ONE FK, retiring assigned_user_id/assigned_group_id + CHECK + assignables view
2026-09-05 schema R17: mice.in_transit -> transit_status with waiting/issued/moved/verified, DEFAULT 'verified' (resting state); verified by walking all four steps and rejecting an unknown value
2026-09-05 decision R18: group eligibility (only a member may work a group-assigned task) is BACKEND business logic, not a DB constraint — a cross-row trigger would reject silently where a service check can explain itself
2026-09-05 schema R18: users.role now NULLABLE with users_role_matches_type — a person must have a role, a group must not; authorization always reads the ACTING PERSON'S role, never the group's; verified both rejections
2026-09-05 schema R19: mouse_lines DROPPED — a subcolony IS a mouse line (one workbook = one colony, its column-B lines are subcolonies); measured 62/62 Breeders cages belong to exactly one line so cages.subcolony_id was already right
2026-09-05 schema R19: line_id -> subcolony_id on litters and mouse_meta, pinned by composite FK so a mouse cannot claim a different programme from its litter; subcolonies UNIQUE (colony_id, name) not global
2026-09-05 schema R19 amend: mouse_meta<->litters subcolony composite guard REMOVED (user) — a mouse may be moved into a different programme than its birth litter; plain FKs to subcolonies remain on both, litter_code stays pinned. Verified: divergent subcolony accepted, nonexistent subcolony rejected, litter_code guard still rejects
2026-09-05 schema R20: mates gains subcolony_id + cage_id — the app creates pairings and a cage IS chosen then (the earlier "workbook has no mating cage" argument applied to import only); litters.subcolony_id REMOVED as reachable via mate_id
2026-09-05 schema R20: accepted loss recorded — a litter with mate_id NULL (parents unknown, or is_from_outside shell) has no programme of its own; its mice still carry mouse_meta.subcolony_id. Verified all four cases
2026-09-05 schema R21 (reverses R20): mates.subcolony_id + cage_id REMOVED — programme derives from either parent (measured 39/39 pairs within one line, 0 crossing), cage-at-pairing derives from the parent's mice version current at that time
2026-09-05 BUG FOUND + FIXED: created_at DEFAULT now() is TRANSACTION-START time, so all rows written in one transaction share it — and the ETL imports in one transaction, which would have made historical state unreconstructable. Added mice.effective_at (when the state became true) + mice_effective_idx; verified time travel inside a single transaction
2026-09-05 schema R22: mates.status added — pending/cohoused/awaiting/pregnant/delivered (합사전/합사/임신준비/임신확인/출산) + mates_status_idx; status is the CURRENT cycle's state, past cycles stay as their own litters rows
2026-09-05 R22: every mates transition logged to audit_logs (before_json/after_json) so no status-history table is needed; co-housing VERIFIED by query not constraint (cross-row over mice version rows) — new scenario packages/db/scenarios/flow-mating.sql walks all five states and both co-housing checks
2026-09-05 schema R22 amend: mates gains cohoused_at/awaiting_at/pregnant_at/delivered_at, and status becomes a GENERATED STORED column derived from them — a writable status alongside four timestamps could have claimed 'pregnant' with delivered_at set; now they cannot disagree
2026-09-05 verify: full mating flow re-run — status follows the timestamps, direct writes to status REJECTED by Postgres, clearing delivered_at walks status back to pregnant
2026-09-05 schema R22 (rewritten): mates is ONE ROW PER CYCLE not per couple — mates_couple_key REMOVED (it made re-mating the same pair impossible, verified by breakage); mates:litters now 1:1 via litters_mate_key; litters.mated_on dropped, expected_delivery_on moved to mates
2026-09-05 schema R22: mates.status + 4 stage timestamps REPLACED by append-only mate_status_logs (mate_id, status, occurred_at, is_occurred_at_approx, actor_id, note) — timestamps could not record who, could not survive a stage entered twice, and could not hold a reason
2026-09-05 verify R22: 7 transitions including a reversal pregnant->awaiting->pregnant with actors and notes preserved; current status = newest row; invalid status rejected; same couple re-mated into a second mates+litters while cycle 1 kept its history
2026-09-05 schema R22: litters.birth_date REMOVED — the 'delivered' row in mate_status_logs already carries that date in occurred_at with its actor and '~' approx flag; litters now holds outcome only (litter_code, seq, pup_count, is_from_outside). Verified both cycles read their birth date from the log
2026-09-05 data: mate status vocabulary re-derived from the workbook by UNIQUE (row,value) — preg? 16, pups? 8, sac-if-not 6, check-ps 6, no pups 1, miscarriage 1; col K 'PLUG' holds only 2 values and both are QUESTIONS ('Is it pregnant?', 'Are there new pups?'), not plug data
2026-09-05 CORRECTION: the earlier claim "'no pups' appears 1023 times, the commonest note" was WRONG — that counted CELLS; it is ONE note in row 489 replicated across ~1023 columns. Fixed in plan R16 and scenarios README; the P1 nullable-subject change still stands on room-level notes
2026-09-05 schema: mate_status_logs gains 'no_pups' terminal status — without it a failed cycle sits at 'awaiting' forever with nothing to close it; miscarriage is recorded as no_pups + note
2026-09-05 schema R22 (final): mate_status_logs table REMOVED — mates itself is versioned-append with origin_mate_id, first row self-referencing, same pattern as tasks/notes; current state = DISTINCT ON (origin_mate_id) newest row
2026-09-05 schema R22: litters.mate_id pinned to ORIGIN rows by composite FK (mate_id, mate_id) -> mates (id, origin_mate_id) — Postgres allows a repeated column in the key, so only rows where id=origin_mate_id qualify; verified a mid-history row is rejected
2026-09-05 schema: 0002 REORDERED (user) — mouse_meta and mice now created BEFORE mates and litters; only mouse_meta->litters stays deferred, mates/litters get their FKs inline
2026-09-05 RISK FOUND: append writes that insert a PARTIAL row silently destroy omitted state — recording a death without carrying cage_id/sex leaves them NULL and the mouse vanishes from its cage. No constraint can catch it (NULL cage is also the legitimate "unplaced" value). Recorded as a P0.6 task; scenarios rewritten to demonstrate wrong vs right
2026-09-05 RISK confirmed on ALL FOUR versioned tables (not just mice): a partial append INSERT silently destroys omitted columns — tasks lost assigned_to+subject_cage on 'done', mates lost both parents on 'pregnant', notes lost litter_id on edit. Verified in flow-probes PROBE 1/6/7/8 with wrong and right forms side by side
2026-09-05 decision R23 (user): handled in BUSINESS LOGIC via an appendVersion(table, logicalId, changes, actor) repository helper that copies the head row forward — no schema constraint can distinguish an omitted NULL from an intended one (unplaced mouse, unassigned task)
2026-09-05 schema R24: subcolonies RENAMED mouse_lines, subcolony_id -> line_id (cages, mouse_meta) — naming only, the entity was already the mouse line per R19; dead UNIQUE (id, colony_id) dropped with it (its composite FK was removed earlier)
2026-09-07 plan R25 (user): schema review pass folded — 9 changes + 2 tensions resolved; Fable research backed tasks/notes/audit. Migrations 0009+ pending, NOT yet verified
2026-09-07 R25 decisions: slots.label -> global unique (records 'F5' relabel blocker); sex -> sex ENUM staying on mice; line_id moved mouse_meta -> mice (mutable); import provenance + machinery removed (import out of scope)
2026-09-07 R25 finding (architect): litters.line_id/mates.line_id NEVER existed — the 0002 bred-under comment was aspirational; left derived/unhomed, plan R19 amended in place
2026-09-07 R25 decisions: genes table NEW (code=whole marker incl zygosity); mouse_genotypes -> mice_genes (gene_id FK, order_index kept); mouse_events DROPPED -> dates land as done tasks rows (losses: ~approx, raw_value, dedup)
2026-09-07 R25 decisions: prev_id CAS + UNIQUE(origin,prev_id) on all 4 versioned tables (no version int, head id is the token, 409 on conflict); notes gains colony/cage/line/slot FKs with NO exactly-one CHECK (room-level=0, mouse+litter=subject+context)
2026-09-07 R25 decision: audit_logs -> append-only immutable (drop updated_at/deleted_at+trigger, REVOKE UPDATE/DELETE, add request_id/on_behalf_of) = SANCTIONED soft-delete exception (§22.4); service-layer same-tx writes; version-row pointer not dup diff for the 4 versioned tables
2026-09-07 R25 DONE (migrations): 0009-0019 written + scratch-verified from empty, 3 deliberate-breakage probes pass (mice CAS collision rejected, audit_logs UPDATE rejected, zero-target + mouse+litter notes both insert); lint+check-types green; grep clean (no server/domain code refs dropped objects). DEV DB NOT migrated (apply is destructive — user's call); SCHEMA.md/ERD regen + scenario fixes + 0019 REVOKE FROM <app_role> still pending
2026-09-07 R25 SHIPPED to dev DB: 0001-0020 applied from empty (dev has no data yet); committed f972349 (migrations+docs), 865e68b (scenarios+R25 probes), 2006f87 (0020 finish mice_genes rename — seq/pk/fk, verified 0 mouse_genotypes* objects remain)
2026-09-07 R25 follow-ups recorded in 00.tasks P0.2-R25: audit_logs real immutability needs a NON-OWNER app role (owner bypasses REVOKE — PROBE 11); slots.label global-unique 'F5' relabel MEMO before import; Q31 (task subject rule + tasks-gain-colony/line/slot) still for Dr. Lopez-Juarez
