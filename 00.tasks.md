# 00.tasks — Lopez-Juarez Mouse-Colony Automation

> Completed items live in `_docs/archive/` (cold storage — do not read unless
> investigating history). This doc holds active + planned work only.

Task legend: `[ ]` TODO · `[~]` IN PROGRESS · `[x]` DONE.
Each task lists AC = deterministic acceptance criteria.
> Schema-evolution fold history (P0 re-scope + R1–R15) rolled off → _docs/archive/tasks.schema-fold-preamble.md  <!-- ARCHIVE: history-only -->
> Current schema is authoritative in packages/db/SCHEMA.md (generated from live DB), NOT restated here.

## P0 — Prototype (LOCAL ONLY)

### P0.1 Scaffold — P0-a

- [x] Monorepo scaffold: pnpm + turbo; `apps/colony_client_web` (Next.js 16),
      `apps/colony_server` (Express), `packages/{db,types,domain}` + reused config
      packages.
      AC: `pnpm install && pnpm turbo build` succeeds; dep graph is
      client→{types,domain}, server→{types,db,domain}, domain→types only;
      packages/domain stays pure (no I/O, no node-only imports) so it is
      client-bundlable; no circular deps.
- [x] Local Postgres setup + `packages/db` migration runner (numbered .sql
      migrations, raw SQL). **DECIDED 2026-09-04 — Homebrew `postgresql@17`,
      NO Docker, NO Supabase CLI in P0** (the sibling's `db:start` symlinks a
      colima socket with sudo — inherited fragility for zero benefit, since P0
      may not use hosted Supabase). Runner = `packages/db/src/migrate.ts`
      (~90 lines) + `src/reset.ts`; only new dep is `dotenv` (`pg` already
      present for the Pool).
      - Filenames: `NNNN_description.sql`, zero-padded 4 digits, in
        `packages/db/migrations/` — matches the sibling (which uses `0001_*`,
        NOT Supabase timestamps) and stays copyable into `supabase/migrations/`
        at P2, since the CLI applies lexicographically.
      - Connection: `DATABASE_URL`; precedence env > root `.env` > default
        `postgres://localhost:5432/colony_dev`. Runner derives the maintenance
        URL to `CREATE DATABASE` when missing. One-time prerequisite, stated not
        hidden: `brew install postgresql@17 && brew services start postgresql@17`.
      - **Forward-only — NO down migrations** (YAGNI: solo dev, local DB, no P0
        rollback scenario). Recovery path is `db:reset`, which the P0.5 seed loop
        needs regardless.
      - Tracking: `schema_migrations(filename TEXT PK, checksum TEXT NOT NULL,
        applied_at timestamptz)`. Each file runs in ITS OWN transaction together
        with its `schema_migrations` INSERT, so a failure leaves no partial record.
      AC: one command creates DB and applies all migrations from empty; re-run is
      a no-op (recorded + checksum matches → skip). A migration EDITED after being
      applied must FAIL LOUDLY (checksum mismatch → write a new migration
      instead) — never silently re-applied or ignored. An unapplied file that
      sorts BEFORE an already-applied one must also fail (out-of-order insertion).

### P0.2 Schema (`packages/db`) — P0-a — DONE, then SUPERSEDED (2026-09-05)

> Schema shipped, then REDESIGNED by plan §4 R11-R15 (mouse_meta/mice split,
> mates + litters, denormalized litter_code). The original P0.2 and P0.2-R
> acceptance criteria describe tables that no longer exist and are history only.
> <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.p0.2-schema.md](./_docs/archive/tasks.p0.2-schema.md)
>
> **CURRENT SCHEMA IS NOT DESCRIBED IN THIS DOC.** It is generated from the live
> database into [packages/db/SCHEMA.md](./packages/db/SCHEMA.md) (with
> [ERD.core.png](./packages/db/ERD.core.png)) by
> `packages/db/scripts/{schema-doc,erd}.sh`. Regenerate after every migration
> change rather than restating structure here — restating it is what produced
> the drift this stub replaces.

### P0.2-R25 Schema review pass (`packages/db` migrations 0009+) — P0-a

> Plan §4 R25. New checksum-guarded migration files only (never edit 0001-0008).
> Verify each by DELIBERATE BREAKAGE against a rebuilt DB (§19), then regenerate
> `packages/db/SCHEMA.md` + ERD. Tests stay SUSPENDED.

- [x] `0009` drop import provenance columns (import_batch_id/source_sheet/source_row)
      off cages, slots, mouse_meta, mice, mates, litters, mouse_genotypes, mouse_events.
- [x] `0010` mothball import machinery: DROP raw_sheet_rows, import_errors,
      color_maps, then import_batches (FK order). 0006 stays on disk, inert.
- [x] `0011` `mice.sex` TEXT CHECK → `sex` ENUM.
- [x] `0012` move `line_id` mouse_meta → mice; appendVersion helper must carry it
      forward. Correct the stale 0002 bred-under comment in-migration.
- [x] `0013` `slots.label` → global partial unique (drop cage-scoped). Record the
      'F5' relabel blocker for the future import.
- [x] `0014` CREATE `genes` (id, code, label, description) + partial-unique on code.
- [x] `0015` rename `mouse_genotypes` → `mice_genes`; drop marker_text, add
      `gene_id → genes(id)`; keep (mouse_id, order_index) unique.
- [x] `0016` DROP `mouse_events` + `mouse_event_kind`. AC: doc records date landing
      as done tasks rows + accepted losses.
- [x] `0017` prev_id CAS on tasks/notes/mates/mice: ADD prev_id +
      `UNIQUE (origin_*_id, prev_id) WHERE deleted_at IS NULL`. AC: two concurrent
      edits off the same head — second INSERT rejected (deliberate breakage).
- [x] `0018` notes reach: ADD nullable FKs colony_id/cage_id/line_id/slot_id. NO
      exactly-one CHECK. AC: room-level (zero-target) and mouse+litter notes both insert.
- [x] `0019` audit_logs immutable: DROP trigger + updated_at/deleted_at, recreate
      the two indexes without the deleted_at predicate, ADD request_id/on_behalf_of_id,
      REVOKE UPDATE,DELETE. AC: an UPDATE on audit_logs is rejected.
- [x] Regenerate SCHEMA.md + ERD (from live DB); fix scenarios (flow-*.sql) to R25
      + add CAS/notes-reach/audit probes (commits 865e68b, 2006f87).
- [ ] NOTE — audit_logs REAL immutability: 0019's `REVOKE UPDATE,DELETE FROM PUBLIC`
      is bypassed by the table OWNER (scenarios PROBE 11 proved it). For true
      append-only, the app must connect as a NON-OWNER role granted INSERT/SELECT
      only, then `REVOKE UPDATE, DELETE ON audit_logs FROM <app_role>`. Belongs with
      the P2 Clerk-auth / RLS role work (DB role separation does not exist yet).
- [ ] MEMO (revisit BEFORE any import) — slots.label is now GLOBALLY unique (0013),
      but the real workbook has label 'F5' in cages '2413' and '4'. The deferred
      import CANNOT load that data without a relabel step. Decide the relabel scheme
      (e.g. cage-number-qualified labels, as the scenarios now use) before import.
- [ ] OPEN Q31 (Dr. Lopez-Juarez) — the real task-subject rule: per task type, which
      subjects are valid, and may a task have zero/multiple? And should `tasks` gain
      colony/line/slot FKs to mirror the R25 notes reach? Subject columns stay
      unconstrained until she answers.

### P0.3 Domain parsers (`packages/domain` — pure, no I/O) — P0-a

- [ ] Tiered mouse-ID parser (rev. 2026-09-04 R3): canonical
      `[MF]\d+[A-Z]{1,5}(tag-suffix)?(\.seq)?` — parser accepts litter codes
      `^[A-Z]{1,5}$` (generator emits 3–5); tag-suffix = OPEN set: TEXT +
      known-mapping lookup (empty→foot/leg, e→ear, (toe)→toe, others
      possible), unknown suffix → warn NOT reject; `.seq` = tag
      reissue/re-clip; bare no-sex NEWBORN-PUP variant (`1BIZ`) → NEW
      `unsexed_pup` disposition (non-warn, round-trips); other bare no-sex
      IDs (`1BIS`) → sex=U + warn, never guess; **`+N` variant — PROTOTYPE
      RULE (user, 2026-09-05): parse ONLY sex + leading pup_number + litter
      code.** `M4+10AZZ` → sex=M, pup_number=4, litter_code=AZZ. Do NOT parse `+N`
      into `reclip_tag`, do NOT set `is_pooled` — these are ordinary single
      mice for the prototype and DO enter the natural key. The original string
      is preserved twice: byte-exact in `mouse_meta.raw_mouse_id`, and as a
      `notes` row on the mouse. `reclip_tag` stays unused until Q2/Q23 are answered;
      junk `=>` → UNPARSED→error.
      AC: 66/66 real-ID fixtures parse to the documented shape or route to a
      disposition listed in the FROZEN disposition table at
      packages/domain/fixtures/mouse-id.dispositions.md; zero silent
      fallbacks; `+N` fixtures (`M4+10AZZ`, `F10+1earAYY`, multi `F1+6+8CF`)
      all yield the LEADING pup_number with the raw label byte-preserved and a
      note carrying the original.
      VERIFIED 2026-09-05: the leading-number rule yields 0 collisions across
      175 (litter, pup) pairs on Breeders, the only P0 import target. Other
      sheets show 146/3528 (4%) collisions of the form `M1+10A` vs `M1A` —
      they are out of P0 scope and need Q2 answered before import.
      BLOCKED-BY: professor confirmation of the amended disposition set (plan
      Q4) — the fixture table must be frozen before this task starts.
- [ ] litter-code codec (pure, R9): encodeLitterCode(n)/decodeLitterCode(code) —
      bijective base-26 (A=1..Z=26). Used ONLY to GENERATE the next code
      across AZZ→BAA and ZZZ→AAAA rollovers (P0-b). CHANGED BY R14: the
      ordinal IS now persisted, as `litters.seq`, so issue order is a plain
      ORDER BY seq and the R9 length-first index is retired. The codec computes
      `seq` for IMPORTED codes; newly issued codes take it from
      `litter_code_seq`.
      AC: encode/decode round-trips for all `^[A-Z]{1,5}$` fixtures;
      seq(AZZ)+1 == seq(BAA); seq(ZZZ) < seq(AAAA); for the mixed-length
      fixture set {ZZZ, AAAA, AZZ, BAA, A, AB}, ORDER BY decodeLitterCode(code)
      differs from a plain lexicographic sort (which places 'AAAA' before
      'ZZZ') — that difference is exactly why the ordinal is stored.
- [ ] YYMMDD codec: 260405→2026-04-05; pin `20` century; validate MM/DD;
      invalid→raw-store + warn.
      AC: valid/invalid fixture table passes; invalid input never throws, returns
      warn disposition.
- [ ] Forward-fill helper: fill mouse-line/cage/slot down blocks (Breeders cols
      B/D/E inheritance — B resolves to the mouse_line entity, E to slot.label
      per plan §4); section-label rows ("To Breed", "Ps Breed.") →
      section_marker disposition (not mouse, not error).
      AC: fixture reproducing Breeders R22–R25 fills correctly; section rows emit
      section_marker.
- [ ] canTransition(role, from, to) pure fn: role→allowed-transition matrix
      (staff: open→done; professor: open→done, done→verified, done→open,
      open→cancelled, done→cancelled; admin: any→any; nobody exits
      verified/cancelled except admin) — placeholder matrix TBD-with-professor
      (plan Open Question 18). Fixture tests are SUSPENDED (CLAUDE.md §19) —
      build the pure fn now, write its tests in the end-of-development pass.
      AC: matrix defined in exactly one place; deterministic (pure, no I/O);
      every (role, from, to) combination is REPRESENTABLE in the matrix
      structure (the allowed/disallowed fixture table comes with the test pass).
- [ ] (OPTIONAL v0.1) Note-date extractor: `\b2[0-9]\d{4}\b` → note_event; keep
      original text, don't fix typos. raw_notes is stored verbatim regardless of
      whether this ships.
      AC: fixtures based on real notes (75/185 contain dates) pass; original text
      byte-identical in output.
- [ ] Parser test suite — **SUSPENDED until development is done (user directive
      2026-09-04; CLAUDE.md §19). Do NOT write this yet.** Deferred to a single
      end-of-development pass, not deleted: the fixture inventory below is the
      spec for that pass. Until then, parsers are validated by deliberate
      breakage against real inputs, not by a suite.
      (**Jest + ts-jest** via the reused `@repo/jest-config`
      — matches CLAUDE.md §19 and the sibling project; the ml brief's pytest
      reference is superseded, plan §2 discrepancy CLOSED): mouse_id (66 real fixtures
      incl. unsexed_pup), litter_code codec, yymmdd,
      forward_fill (+ note_date_extractor if built); mirror src; core parsers
      only, no I/O/SQL.
      AC: full suite green in one command; no test touches DB or filesystem beyond
      fixtures.

### P0.4 ETL (`apps/colony_server` helpers) — P0-a

- [ ] Import helper (synchronous exceljs, one tx, isolated interface): **Breeders
      only normalized** — header row 2, data 3+, cols A:P (1–16); never crawl
      max_col. Column map per plan §4 + R10 (REVISED 2026-09-04 — cols I–N are
      NO LONGER raw-only): B/D/E forward-fill — B→line_id via mouse_line
      entity; D→cage resolved FIRST, THEN E→slot.label under that cage (slot
      resolution is CAGE-SCOPED — slots unique per (cage_id, label), R10);
      F→ID parser; G→raw_genotype plain + `mouse_genotypes` rows (split on
      ';', tolerate ':' typo, order_index preserved); H→YYMMDD;
      I→mates father (nullable FK + mate_raw_label + import_errors warn on
      unresolved); J→litters.mated_on, L→expected_delivery_on ('~' → approx
      flags, raw preserved); K PLUG → notes-only, no column (plan Q28);
      M/N→`mouse_events` rows (one row per date); O→`notes` rows
      (mice.raw_notes DROPPED, R10) — note-date extraction into tasks is
      PROMOTED in importance (the '260824 preg?' pattern, R10); P→raw_parents.
      AC: real workbook imports with 0 silently dropped rows — every non-imported
      row exists in import_error with provenance; whole import is one transaction;
      cage is resolved BEFORE slot for every row.
- [ ] Idempotent upsert by natural key (rev. 2026-09-04 surrogate identity —
      SUPERSEDES the old label-alone key): mouse matches on **(litter_id, pup_number)**
      via the partial unique index — import resolves the litter DIRECTLY by
      its letter code via UNIQUE(litters.litter_code) → litter row;
      line_id for disambiguation ONLY, not part of uniqueness; pooled
      (`is_pooled=true`) and unparseable rows match by raw label (raw label
      kept alongside the parsed `reclip_tag`); cage = (cage_number). On
      conflict/match, birth facts are NEVER updated — only un-tombstone
      (deleted_at=NULL) + refresh provenance cols; import seeds initial
      `mice` version rows (sex, is_alive, cage/slot — 'notes' dropped from
      the field set R10, note content goes to the `notes` table — actor =
      importing user); label/sex/state diffs on a MATCHED mouse → APPEND
      `mice` version rows (actor = import user), NEVER tombstone+reinsert;
      rows absent in new version → deleted_at tombstone.
      AC: importing the identical file (same sha256) is a no-op; importing a
      file with one removed row tombstones exactly that row; ID-mutation
      fixture (FLIPPED 2026-09-04 from documented mismatch to assertion):
      unsexed `1BIZ` → sexed `M1BIZ` re-import MATCHES the same `mice.id`
      and appends exactly one sex row + one mouse_label row to
      `mice` version rows — no tombstone, no re-insert; a Breeders row whose
      letter code has no litters row creates exactly one litters row and
      reuses it for every sibling row in the same import (no duplicate
      litters for one code — enforced by UNIQUE(litters.litter_code)).
- [ ] raw_sheet_rows archival of ALL 9 sheets (lossless cells JSONB);
      Dplct'd breed+exp NEVER promoted to source of truth.
      AC: every row of every sheet present in raw_sheet_rows; no normalized rows
      originate from Dplct'd breed+exp.
- [ ] Import-error report: endpoint + UI surface listing import_errors rows
      (severity error/warn) with provenance and resolved_at; surfaced in ticket bin.
      AC: after a real-workbook import, report row count equals import_errors
      count; each row shows sheet/row/col/raw_value/rule_violated.

### P0.5 Seed data — P0-a

- [ ] Mock/seed generator: realistic anonymized seed derived from the real xlsx
      (structure, ID grammar, colors preserved; identifying content scrambled).
      AC: `seed` command populates an empty local DB; prototype + full test suite
      run green without the live file present.

### P0.6 Server API + UI — P0-a

- [x] **Frontend-first Cage Grid prototype (mock, read-only) — professor demo
      (2026-09-07).** `colony_client_web` home = the grid; renders Excel colour
      semantics back (black=done, red font=instruction, blue font=plan, yellow
      fill=flag, gray fill+strike=dead) so the professor recognises the room at
      a glance. View DTOs (`LineGrid/GridCage/MouseCell/SignalColor`) live in
      `@repo/types`; the seam is `apis/getLineGrid.mock.api.ts` (named as the
      future real fetcher → mock→real swap is one file). Fixture deliberately
      seeds the open questions (`M4+10AZZ` pooled, `U3BCX` unsexed, `F10BEVe`
      ear-tag, `M4BCW.2` reclip) so the demo doubles as an ambiguity checklist.
      DEVIATION (SUPERSEDED 2026-09-08): the demo started on plain CSS for boot
      reliability; the shadcn/Tailwind migration below now replaces it.
      NEXT (not built): Task-bin, Mouse-detail drawer, move mutation (+CAS 409).
- [x] **Full hierarchy + Hybrid explore + Move menu (mock, 2026-09-08).**
      Extended to the real 4-level nesting **colony › line › cage › slot › mice**
      (slot is its own level, holds many mice); fetcher renamed
      `getColonyGrid.mock.api.ts` (old `getLineGrid` soft-deleted `x_`), DTOs now
      `ColonyGrid/GridLine/GridCage/GridSlot/MouseCell`.
      Explore model = **Hybrid (nav + filter)** chosen by user over nav-first /
      filter-first / group-by: hierarchy stays the page spine, a sticky filter
      bar (search + sex + signal chips) dims non-matching cages/slots/mice.
      Move = **cascade menu** (user chose menu over drag-drop) line › cage ›
      slot, same-line default, `＋ new slot`, confirm breadcrumb; pure core
      `lib/gridMove.ts` mirrors the append-only move (new head row, empty slots
      dropped) — verified by scratch run (9/9: filter, move, source-preserved,
      original-immutable, new-slot cleanup, cross-line). `lib/gridFilter.ts` is
      the pure predicate. Interactive parts are `*.client.tsx`.
      OPEN DECISION: cross-line move is currently **warn, not block** — a mouse
      can be moved into another line's cage with a ⚠ banner. Confirm with the
      professor whether it should be hard-blocked (line = strain identity) or
      allowed with the warning.
- [x] **shadcn/Tailwind v4 + `line | cage | mouse` 3-pane desktop view — funding
      pitch polish (2026-09-08).**
      **3-PANE LAYOUT SUPERSEDED 2026-09-12** by the single unified nested-column
      body (line|cage|slot|mice rails + header jump dropdowns). shadcn/Tailwind
      adoption + Move-menu parts still stand.
      User chose shadcn (already the `rules/nexjts.md`
      house convention) and a desktop **Miller-columns** layout: Line ▸ Cage ▸
      Mice(grouped by slot). Tailwind v4 (`@tailwindcss/postcss`) + shadcn atoms
      hand-added under `components/ui/` (button/card/input/badge/label/dialog/
      radio-group), `cn` in `lib/utils`, `components.json` for future CLI adds,
      Inter + JetBrains Mono via `next/font` (codes in mono). Move menu rebuilt on
      shadcn Dialog + RadioGroup. Design: **chroma reserved for the 5 workflow
      signals only** (`lib/signal.ts` is the single colour source); UI chrome is
      neutral graphite so signals read as meaning, not decoration.
      Verified by REAL browser clicks (puppeteer-core, throwaway): filter
      signal=plan → line/cage match counts + dim; Move dialog cascade populates
      line›cage›slot with same/cross-line + here/current hints + breadcrumb;
      Move applied END-TO-END (M4BCW 2413→2414/A8, counts 3→2 / 3→4).
      lint + check-types green.
- [x] **Interaction model → show-all + linked highlighting (2026-09-08).**
      **SUPERSEDED 2026-09-12**: cross-pane "active line/cage" highlight replaced by
      in-body SCOPE HIGHLIGHT (click a node → its ancestor path + subtree light up,
      `lib/gridSelection`). Show-all + orthogonal filter-dim principle still holds.
      Replaced the drill (which HID non-selected children) with: all lines, all
      cages, all mice always visible; activating a LINE highlights its child
      cages, activating a CAGE highlights its mice. TWO ORTHOGONAL AXES per user:
      (1) active line/cage = persistent structural markers (never dropped),
      (2) filter = emphasis/dim (match strong, non-match dimmed). Priority: when
      a filter is on it drives mouse opacity; otherwise active-cage membership
      does; the active markers persist under both. Verified in-browser: line
      switch moves the cage highlight; filter=plan dims non-matches while cage
      2501 stays actively marked.
- [x] **Task bin prototype (mock) — the "automate" pitch (2026-09-08).** `/tasks`
      route + nav (Cages | Tasks). Board = Open / Done / Verified columns; card
      shows type, subject chip (mouse/cage/litter/room), direction detail, due
      (overdue in red), and actor trail (assignee / done-by / verified-by).
      **Role-gated transitions** via pure `lib/taskFlow.ts` `canTransition` +
      `availableActions` (staff: open→done; professor: done→verified|reopen|
      cancel, open→cancel; admin: any→any) — a role switch (staff/professor/
      admin) flips which buttons appear. `TaskCard`/`TaskStatus`/`Role` DTOs in
      `@repo/types`. NOTE: `taskFlow` lives in the app for now (avoids the
      unbuilt-`@repo/domain`-dist runtime trap); MOVES to `@repo/domain` when the
      service also enforces it — same function shape. Default matrix is still
      TBD-with-professor (see the assignment bullet below).
      Verified: `canTransition`/`availableActions` 8/8 scratch run; in-browser —
      staff Mark-done moves Open→Done, switching to Professor reveals Verify/
      Reopen/Cancel and hides Mark-done. lint + check-types green.
- [x] **Mouse-detail drawer (mock) — "simple management" depth (2026-09-08).**
      Click a mouse id in the grid → right sheet (shadcn Sheet on Radix Dialog).
      Sections: Identity (genotype/dob/litter), Genotype (per-marker badges from
      `mice_genes→genes`), Parents (mother/father + genotype), and an APPEND-ONLY
      **History timeline** (one entry per version row, newest first, dot + date +
      actor). DTO `MouseDetail`/`GeneCall`/`ParentRef`/`HistoryEvent` in
      `@repo/types`; seam `apis/getMouseDetail.mock.api.ts` (keyed by
      mouse_meta_id, rich entries for the ambiguity cases + a fallback). Verified
      in-browser 6/6 (drawer opens on M4+10AZZ; shows History, the pooled-'+10'
      entry, a parent, PlpCre marker). check-types green.
- [x] **Upcoming/due view (mock) — the date-automation pitch (2026-09-08).**
      `/upcoming` route (built by a parallel bg developer agent, integrated by
      main session). Auto-computes plug-check(+10d) / delivery(+20d) / wean(+21d)
      / genotype(+21d) from mating occurred_at and pup dob, then groups into
      Overdue / Today / This week / Later via pure `lib/dueDates.ts` (`dueStatus`,
      `daysUntil`, UTC-parsed to avoid midnight shift). Cards show absolute +
      relative date, kind badge, subject chip; overdue=red(instruction),
      today/soon=amber+flag-fill, later=muted. DTO kept local to
      `apis/getUpcoming.mock.api.ts` (no shared-file edit → clean parallelism).
      Verified: agent scratch-tested the UTC date math; main session — whole
      client check-types+lint 0 errors, `/upcoming` serves 200, screenshot shows
      2 overdue / 2 today / 3 soon / 1 later.
- [x] **New-task create UI + type-driven form + mate auto-cascade (mock,
      2026-09-08).** "+ New task" on the Task bin — ANY role can create a task
      (staff + professor + admin; user directive 2026-09-08). Role still gates
      the ADVANCE actions (done/verify), not creation. `lib/taskTypes.ts` =
      data-driven form schema:
      picking `task_type` reveals type-specific fields, which land in the task's
      `direction` payload (mirrors tasks.task_type + direction jsonb). Types:
      Mate, Plug check, Birth/delivery, Wean, Genes-to-check, Genotyping, Move,
      Check food, Sac. Fields: mouse/cage/litter pickers (from the colony
      fixture), gene multi-select, date, number, text.
      **Auto-cascade**: creating a Mate enqueues plug-check(+10d) + delivery
      (+20d) into Upcoming via `lib/dueDates`. Cross-screen state lives in a
      mock-era shared client store `lib/mockStore.ts` (`useSyncExternalStore`;
      seeds from the SEED_* exports; `addTask`/`setTaskStatus`) so created tasks
      + cascades persist across SPA route nav (replaced by react-query + real
      endpoints later; a full reload reseeds — mock only). `TaskCard` gained an
      optional `direction`; `TaskSubjectKind` gained `'mate'`.
      Verified in-browser 7/7: as Professor, create Mate (M4BCW × F5AYL) → Open
      card added; client-nav to Upcoming shows both auto-created follow-ups.
      check-types + lint 0 errors.
- [x] **Color-by toggle + multi-select → batch task (mock, 2026-09-08).**
      **COLOR-BY TOGGLE SUPERSEDED 2026-09-12** by the always-on multi-channel
      colour system below (professor wants every colour at once, no toggle). The
      multi-select → batch-task half of this item STILL STANDS.
      Grid gains a `Color by: Off | Line | Genotype` toggle (`lib/colors.ts`).
      **Line**: each line a categorical hue (indigo/teal/… — avoids the signal
      families) shown as a dot + the active-selection colour; the active line's
      cages highlight in its hue. **Genotype**: a deterministic swatch per mouse
      (same genotype → same colour). DISCIPLINE: signal colours stay reserved
      for state (text/fill); identity/grouping hues are dots/swatches/borders
      only, and only ONE identity channel is on at a time (user chose the
      toggle over always-on). **Multi-select**: a checkbox per mouse → a
      selection bar ("N selected · Create task · Clear") → opens NewTaskDialog in
      BATCH mode (`presetMice`): subject = the mice, types restricted to
      mouse-subject (Genes-to-check/Genotyping/Move/Sac), `direction.mice=[ids]`.
      Verified in-browser 9/9 (line + genotype colouring; select 2 → batch
      dialog → create → card lands in the Task bin). check-types + lint 0 errors.
- [x] **Multi-channel identity colours — always-on Excel colour-cell row (mock,
      2026-09-12).** Supersedes the single-select Color-by toggle: the professor
      reads state by colour, so ALL channels render at once. The mouse row is now
      a fixed-column grid (id · genotype · DOB · mate · actions) — each channel
      scans down its own column. Channels: **sex** (♂ sky / ♀ pink id-cell bg,
      rule), **age** (DOB cell amber when ♂>1y / ♀>10mo — `isOldMouse(dob,sex)`
      computed at read, never stored), **genotype** (own `genotypeColor` cell
      tint; WT/`'?'` = null = default bg so only mutants pop; a mouse differing
      from its line's nominal colour is a visible transfer), **mate** (one filled
      chip per father-fanout group — `mateColors` is a LIST, a mouse can be in
      several). **Signal (state)** moved to the row left-spine (`signalSpineClass`)
      + id-text colour, off the identity cells. `lib/colors.ts` reduced to the
      rule channels (`SEX_TINT`, `isOldMouse`); the genotype hash + `ColorBy`
      toggle removed. DTO: `MouseCell += dob, genotypeColor, mateColors`;
      `GridLine += lineColor, nominalGenotypeColor` (see plan §2). Mock
      (`getColonyGrid.mock.api.ts`) seeds resolved hex via a `GENO` map (same
      genotype → same hex) + `MATE` group colours — the mock of the future
      `color_palette`/`color_assignments` tables.
      Verified: check-types 0 errors; served page shows the DOB dates + amber
      age class hot-reloaded. Also folded this session: line/cage panes stacked
      multi-row + narrowed for a wider mice column; dynamic breadcrumb + Legend
      on one row; per-mouse active-task colour tags (filled squares); pane-header
      totals.
      DEFERRED to real `colony_server`: the `color_palette`/`color_assignments`
      DB tables + write-time colour allocation + canonical genotype key from the
      ETL parser (colour code does no normalizing). No colour-picker/admin UI
      (YAGNI).
- [ ] **Register mouse/litter wizard — DESIGN PLANNED (2026-09-08), ties P0-b.**
      "Add mice to the colony", origin = born-in-colony (parents=mate →
      auto litter_code → N unsexed pups `1BIZ…` + auto wean/genotype tasks) OR
      external (`litters.is_from_outside`, no parents, known sex/genotype/source).
      Entry: `+ Register` on Cages + a "Register litter" action on a done
      Birth/delivery task (closes the automate loop). 3-step wizard
      (Origin → Details → Review); Review previews the mice + auto-tasks before
      confirm. Pure core `nextLitterCode()` + `planRegistration(input) →
      {mice[],tasks[]}`; real path = `POST /litters` one tx. NOT yet built.
- [ ] **Rendering perf at scale (300 cages / 6000 mice) — ANALYSIS ONLY,
      not implemented (2026-09-09).** Grid currently renders ALL mice at once
      (~50k DOM nodes) + full re-render on every state change → jank at scale.
      Options + recommended combo (server-scoped fetch + virtualization + memo/
      debounce; server filter+keyset at real-server) written up in
      `docs/rendering-performance.md`. Adopt at real-server integration.
- [ ] Task assignment + group eligibility (R17/R18, 2026-09-05).
      `tasks.assigned_to` is ONE FK to `users`; a group is a users row with
      `type='group'`. Picker = `SELECT id, display_name, type FROM users WHERE
      deleted_at IS NULL`. "My tasks" = assigned directly to me OR to a group I
      belong to via `group_members`.
      **Group eligibility is a SERVICE rule, not a constraint:** when
      `assigned_to` names a group, reject a transition whose actor is not in
      `group_members` for that group, with an explanatory error — the DB does
      not enforce this.
      **Authorization reads the ACTING PERSON'S role**, never the group's;
      `users.role` is NULL for groups by CHECK, so there is nothing to read.
      AC: a member of the assigned group can move the task open→done; a
      non-member is refused with a message naming the group; a task assigned
      directly to a person is unaffected; canTransition is called with the
      actor's role in both cases.
- [ ] **`appendVersion` repository helper — copy the head row, then override.**
      DECIDED 2026-09-05 (user): this is BUSINESS LOGIC in the backend, not a
      database constraint.
      Applies to ALL FOUR versioned-append tables, not just `mice`:
      | table | logical key | fields silently lost by a partial insert |
      |---|---|---|
      | `mice` | `mouse_meta_id` | cage, slot, sex, is_alive, attention, transit_status |
      | `mates` | `origin_mate_id` | mother, father, expected_delivery_on |
      | `tasks` | `origin_task_id` | subject_*, litter_id, due_date, **assigned_to**, direction |
      | `notes` | `origin_note_id` | subject_mouse_id, litter_id, meta |
      An append INSERT that omits a column gets its DEFAULT (usually NULL), and
      since the newest row IS the current state, the omitted value is destroyed
      silently — no error, no log. Measured (`flow-probes.sql` PROBEs 1, 6-8):
      marking a task `done` without carrying the row forward erased its
      assignee and subject cage; advancing a pairing erased both parents.
      NO SCHEMA CONSTRAINT CAN CATCH THIS: a NULL `cage_id` is also the
      legitimate "unplaced" state (P5) and a NULL `assigned_to` the legitimate
      "unassigned" state, so a constraint cannot tell an omission from an
      intention.
      Shape: `appendVersion(table, logicalId, changes, actor)` reads the current
      head row, copies EVERY column forward, applies `changes`, and inserts.
      Nothing may write these tables directly.
      AC: all four tables go through the helper; no `INSERT INTO mice|mates|
      tasks|notes` outside it; `flow-probes.sql` keeps the wrong form and the
      right form side by side for each table as a regression check.
- [ ] Unplaced mice must stay visible (scenario P5).
      The create-mouse service appends the first `mice` row in the SAME
      transaction with `cage_id`/`slot_id` NULL, and location views LEFT JOIN
      from `cages` so a mouse with no cage renders as "unplaced" instead of
      vanishing. No schema constraint can force this — a child row cannot be
      made mandatory.
      AC: a newly created mouse appears in the mouse list immediately with an
      "unplaced" location; the cage grid still shows empty cages.

- [ ] Server endpoints (controllers→services(tx)→repositories(executor)→@repo/db
      pool, sendOk/sendError envelope, IDs as strings): mice, cages, moves,
      tasks, audit, import.
      AC: every endpoint returns the envelope; every mutation writes an audit_log
      row with actor_id = stub user; zod-validated DTOs from @repo/types; no
      repository function opens or commits a transaction; every repository fn
      accepts an executor param.
- [ ] MOVE transaction (service helper): read mouse.cage_id (cache) → from;
      INSERT mouse_move (TRUTH); UPDATE the mouse.cage_id/slot_id derived
      cache (rev. 2026-09-04 — cache write, truth = mouse_move); INSERT
      audit_log(action='move'). Rules: last-write-wins; idempotency_key guards
      double-submit; reject moves to soft-deleted/nonexistent cages; enforce
      R1 invariant — if slot_id set, slot.cage_id must equal mouse.cage_id
      (clear or reassign slot_id on cross-cage move).
      AC: one move = exactly one mouse_move row + one audit row + updated
      cache, atomically; cache equals the latest mouse_move row; replayed
      idempotency_key is a no-op; move to a tombstoned cage returns an error
      and writes nothing; post-move a set slot_id always belongs to the
      current cage.
- [ ] Dashboard UI: colony counts (total/breeders/experimental) + upcoming tasks
      (today / this week).
      AC: counts match SQL counts on seed data; upcoming-task buckets match
      due_date arithmetic exactly.
- [ ] Excel-familiar grid dashboard (added 2026-09-04): shared status view laid
      out like the current Breeders Excel grid (Genotyping grid when that
      subsystem lands, P1); normalized enums rendered back as the workbook
      color cues — black=done, red=instruction, blue=plan, gray=sac/dead,
      yellow=flag (plan §1). Read-only; internal data stays normalized enums;
      mutable mouse fields read from the mouse_current_attrs VIEW, not raw
      logs (R7); Excel remains import-only — this grid is the on-screen
      substitute.
      AC: every non-deleted seed mouse row renders in the grid; each color cue
      is derived ONLY from enum/status fields (no stored colors read); view
      contains no mutation controls.
- [ ] Cage-grid view: cages grouped by location; mice listed per cage; select
      mouse → detail (sex, dob, raw_genotype, status, attention, notes, move
      history — mutable fields via mouse_current_attrs).
      AC: every non-deleted seed cage and mouse visible; detail fields match
      the mouse row + mouse_current_attrs view values; move history ordered
      moved_at then PK.
- [ ] Mouse-detail history timeline (added 2026-09-04): merged
      history-from-logs view over audit_logs + `mice` version rows +
      task_status_transitions WHERE entity = the selected mouse, ordered by time
      then PK; read-only.
      AC: for a seed mouse with ≥1 move, ≥1 task transition and ≥1 field edit,
      all three event types appear exactly once each in correct order; no
      mutation controls.
- [ ] Search/filter: by composed label (sex||pup_number||litter_code, via
      mouse_current_attrs — labels repeat, result may be multi-row),
      mouse line (via line_id FK → mouse_lines, R19), cage_number, sex,
      status, attention.
      AC: each filter returns exactly the matching seed rows; line filter
      matches on mouse_line.name through the FK; a repeated label returns all
      its mice; empty result is an explicit empty state, not an error.
- [ ] Move-mouse UI flow: select mouse → select destination cage → confirm →
      history recorded (uses the MOVE transaction).
      AC: confirmed move visible in cage view + mouse history + audit log;
      double-click on confirm produces exactly one move.
- [ ] Ticket/task bin UI: create (professor drop) → mark done (staff) → verify /
      cancel (professor); import-error tickets appear here. Transitions are
      **role-gated**: service guards with canTransition (P0.3) against
      users.role and, in ONE tx, UPDATEs task.status + actor/timestamp columns,
      INSERTs task_status_transitions, INSERTs
      audit_log(action='task_status_change'); client hides disallowed buttons
      via the same canTransition fn; server re-enforces. Includes read-only
      **upcoming task-list / print handoff view** (Excel-substitute — plan Open
      Question 9).
      AC: full lifecycle open→done→verified exercisable in UI; each transition
      atomically writes exactly one task_status_transitions row (with
      transition-time actor_role) + one audit row + stamps the correct
      actor/timestamp column; a disallowed (role, from, to) forced via API is
      rejected server-side and writes nothing; handoff view is read-only and
      printable (no mutation controls).
- [ ] Audit-log viewer (read-only, filter by entity/actor/date).
      AC: every mutation performed in the UI appears; log is not editable from UI
      or API.
- [ ] Local-run setup: one documented command path (env `.env`, local pg, seed,
      dev servers).
      AC: from clean clone, documented steps reach a working UI with seed data;
      no cloud dependency.

### P0.7 Litter recording + auto tasks — P0-b (MVP v0.2)

- [ ] Litter-code generator (rev. 2026-09-04 R3/R9): GLOBAL persistent counter,
      emits `^[A-Z]{3,5}$` (starts 3 letters, grows via rollover), seeded from
      file max **BIY** (header O1 "BIZ" = professor's next); roll AZZ→BAA and
      ZZZ→AAAA (confirmed 2026-09-04, plan Q20 resolved); NEVER emits `+`
      pooled forms; uses the P0.3 base-26 codec ONLY to compute the next code
      (rollover-safe); persists litter_code + seq on the litters row — no
      base-26 value is stored (R9).
      AC: first generated code after seed is BIZ; AZZ→BAA and ZZZ→AAAA
      rollover fixtures pass; 1000 sequential codes all match `^[A-Z]{3,5}$`,
      are pairwise distinct, and are already in ascending order under
      ORDER BY length(code), code COLLATE "C" (i.e. generation order ==
      derived sort order); no litter_seq value is written to any table.
- [ ] Pup-ID generator: bare unsexed form (`1BIZ`, `2BIZ`…); accepts
      operator-entered pup numbers (tag position vs birth order — plan Open
      Question 11; pup_number also feeds the R9 derived sort AND the
      (litter_id, pup_number) natural re-import key).
      AC: round-trip parse(generate(x)) == x for the full fixture set; generated
      IDs get the `unsexed_pup` disposition from the P0.3 parser.
- [ ] Parents parser: Breeders col P raw_parents → mother/father mouse links.
      AC: fixture set from real col-P values parses or routes to warn; no silent
      guesses.
- [ ] task_offset_rule config table + auto future-task generation on litter
      create. Placeholder defaults ALL TBD-with-professor: weaning DOB+21d,
      genotyping/tissue DOB+~12–15d; NOTHING hardcoded.
      AC: no offset constants in code; generated tasks appear in the task bin as
      `open`; editing a rule row changes generated dates without a code change;
      unconfirmed rules have confirmed_at NULL.
- [ ] Record Litter UI + CREATE-LITTER transaction: INSERT litters (line_id,
      litter_code); allocate the litter code via the GLOBAL
      counter (per-line mouse_id_sequence DROPPED — plan Q22 resolved;
      pup_number AUTO-ASSIGNED 1..N for new litters — unblocked by Q11
      RESOLVED 2026-09-04, any distinct values are valid; operator may
      override, and IMPORTED litters keep their original numbers, never
      renumbered); INSERT mice per pup
      (birth facts) + seed the initial `mice` version row (cage/slot,
      sex='U'); INSERT future tasks; ONE audit_logs row
      (action='create_litter').
      AC: one confirm creates litter + N mouse_meta rows (with their initial mice
      rows) + future tasks atomically with exactly one audit_logs row; any
      failure rolls back everything.
- [ ] Surface Breeders raw cols I–N (mate/mating/PLUG/DELIV/tissue/genotyping
      dates) read-only in mouse detail, from raw_sheet_rows. NOTE (R10,
      2026-09-04): I–N are now NORMALIZED at P0.4 import (mates + litters /
      mouse_events) — this task renders THOSE tables in mouse detail;
      raw_sheet_rows remains the provenance fallback only.
      AC: for a mouse whose source row has I–N data, raw values render read-only
      with sheet/row provenance.
      NOTE (RATIFIED 2026-09-12, plan §4): breeding-cycle dates (mated/
      last-mating = `mates.occurred_at`, ~DELIV = `expected_delivery_on`) ride
      the VERSIONED `mates` row, never `mice`/`mouse_meta`; edit = appendVersion,
      but a NEW cycle of the same couple = a NEW `origin_mate_id` (re-mating),
      NOT a version row. PLUG stays notes-only (Q28). Tissue/genotyping dates
      (old cols M/N) are now `tasks` rows (task_type Tissue/Genotyping), not a
      surfaced raw column — see the P1 Genotyping subsystem note.

### P0.8 Visual cage management — P0-c (MVP v0.3)

- [ ] Visual cage-management view (location/room layout of cage grid); cages
      render their slots (R1); mouse chips render within their slot when
      slot_id is set, else at cage level.
      AC: every non-deleted cage renders under its location with its
      non-deleted slots; mouse chips reflect current mouse.cage_id and, when
      set, mouse.slot_id.
- [ ] transfers table — MUTABLE (`deleted_at`): mouse_id FK mice,
      from_cage_id, to_cage_id, status TEXT default 'pending'
      (pending|committed|cancelled), idempotency_key UUID UNIQUE,
      initiated_by/at, resolved_by/at, mouse_move_id FK (set on commit). NO
      status column is added to the `mice` version rows beyond transit_status —
      schema-unchanged.
      AC: duplicate idempotency_key insert rejected; `mice` schema
      byte-identical to P0-a; transfers rows soft-delete via deleted_at.
- [ ] Pending-transfer transitions (service): INITIATE = INSERT
      transfer(pending) + audit(action='transfer_initiate') — NO mouse_move,
      NO cage_id change (mouse.cage_id stays = origin until confirmed).
      COMMIT (one tx) = validate from_cage_id = mouse.cage_id; INSERT
      mouse_move; UPDATE mouse.cage_id=to; UPDATE transfer SET committed,
      mouse_move_id, resolved_* WHERE id=$ AND status='pending'; INSERT
      audit(action='transfer_commit'). CANCEL = UPDATE transfer SET cancelled,
      resolved_* WHERE id=$ AND status='pending' + audit; no mouse_move,
      cage_id untouched.
      AC: commit produces exactly the P0-a move writes (mouse_move + cage_id +
      audit) plus the resolved transfer row, atomically; commit/cancel replay
      against a non-pending transfer is a no-op; stale pending (mouse moved
      since initiate) fails validation and writes nothing (plan Open
      Question 17).
- [ ] Pending-transfer visualization (from `transfers WHERE status='pending' AND
      deleted_at IS NULL`): ORIGIN cage shows solid mouse chip (via
      mouse.cage_id) + "transfer tag" (from_cage_id=:cage); DESTINATION cage
      shows dimmed/ghost chip (to_cage_id=:cage).
      AC: while pending, the mouse renders solid at origin with tag and dimmed
      at destination; after commit or cancel both markers clear.
- [ ] Drag-and-drop move (reuses the P0-a MOVE transaction + idempotency_key);
      a drop can fire the instantaneous move OR open a pending transfer —
      recommendation: simple drop = instantaneous, explicit "start transfer" =
      pending (plan Open Question 16).
      AC: a drop produces exactly the same DB writes as the P0-a move flow
      (cage_id update + mouse_move + audit rows); double-drop guarded by
      idempotency_key.

## P1 — Beta

- [ ] In-app changelog engine: version history + diff over import_batches +
      audit_logs + `mice` version rows (NOT excel-vs-excel file
      diffing) by natural key → diff(added/changed/removed, field, old, new).
      AC: two known workbook import batches produce the exact expected diff set.
- [ ] Per-version changelog UI (who/what/when) + human confirmation of tombstone
      candidates (sac/move).
      AC: removed-row tombstones require explicit confirm before final.
- [ ] snapshots table — MUTABLE (`deleted_at`): version_no INT UNIQUE (monotonic
      v0,v1…), label, state JSONB (materialized dashboard payload),
      state_schema_ver INT, high-water marks last_audit_log_id /
      last_mouse_move_id / last_mouse_attr_log_id (added 2026-09-04) /
      last_import_batch_id, published_by FK users, published_at. Substrate for
      the changelog engine — NOT a second change log (audit_logs + `mice`/`mates`/
      `tasks` version rows + import_batches remain THE change log). Optional
      pull-forward to P0-b (plan Open Question 14).
      AC: duplicate version_no rejected; high-water-mark columns FK/reference
      valid max ids of the four append-only sources at publish time.
- [ ] Publish transaction (service): read live dashboard payload → max(id) of
      each append high-water source (audit_logs, `mice`, `mates`, `tasks`,
      import_batches) → INSERT snapshots(version_no=last+1,
      state, marks) → INSERT audit_logs(action='publish_snapshot'). Boundary =
      professor explicitly publishes (weekend loop, plan Open Question 14).
      AC: one publish = exactly one snapshot row + one audit row, atomically;
      version_no increments by exactly 1 per publish.
- [ ] View-at-version: version picker list + single-row read
      (`SELECT state FROM snapshots WHERE version_no=$1`) — serve the JSONB, NO
      log-folding on read; live dashboard stays the existing P0-a query.
      AC: viewing any published version issues exactly one snapshot-row query;
      the API response body for that version equals the stored snapshots.state
      JSONB byte-for-byte.
- [ ] Version→version step-by-step replay (added 2026-09-04): fold the
      audit_logs / `mice`+`mates`+`tasks` version rows / import_batches between two
      snapshots' high-water marks into an ordered, steppable diff — extends
      the changelog engine above.
      AC: replay between v(n) and v(n+1) lists every log entry between the two
      mark sets exactly once, in (created_at, PK) order, and the entry count
      equals the sum of the four high-water-mark deltas. (State reconstruction
      from logs is explicitly NOT claimed — plan §4 defers fold-on-read.)
- [ ] Pedigree/generation tree (added 2026-09-04): family tree over
      litters.mother_mouse_id/father_mouse_id + mice.litter_id — depends on
      the P0-b parents parser.
      AC: for a seeded 3-generation family, the tree renders exactly the
      parent→litter→pup edges present in DB; mice with no litter render as
      roots; no cycle crashes the view (cycle → explicit error state).
- [ ] Scheduling offset refinement: professor-confirmed task_offset_rules values
      replace placeholders.
      AC: every task_offset_rules row has confirmed_by/confirmed_at set; fixture
      events yield the professor-confirmed offsets.
- [ ] Normalize Experimental sheet (982 rows, parallel entity, import only).
      AC: 0 silently dropped rows; rejects in import_error.
- [ ] Normalize New litters sheet (21 rows, feeds Breeders, import only).
      AC: litters link to Breeders mice by natural key; rejects in import_error.

### P1 Genotyping subsystem — PROMOTED from DEFERRED 2026-09-04

Built ON ticket/role/task_status_transitions + markers/line_marker_panels/
genotyping_runs/genotyping_results. The moved view/edit-UI task below is the
read grid; the new ticket/entry tasks own the write path.

NOTE (RATIFIED 2026-09-12, plan §4): the request→done→verify LIFECYCLE of a
tissue/genotyping check is a `tasks` row (task_type Tissue/Genotyping), version
rows via `origin_task_id` = status transitions of ONE request; "how many
requested" = COUNT(DISTINCT origin_task_id). `tasks` IS the request ledger —
this ratifies migration 0016 dropping `mouse_events` (dates land as tasks; see
P0.2-R25 `0016`). Structured RESULTS live in genotyping_results, NOT
`tasks.direction`. Tissue with only a collection date needs no dedicated table
(a task + optional note suffices); add one only if structured sample data must
be queried.

- [ ] Genotyping tables: markers, line_marker_panels, genotyping_runs,
      genotyping_results (prep_stage >p15 vs Pup, allele, signal, raw_value).
      AC: genotyping_results FKs to run + mouse + marker AND to the originating
      `tasks` row (provenance — the request the result answers); unique
      (mouse_id, marker, pcr_date) natural key enforced (mouse_id = FK to the
      mouse record PK `mice.id` — mouse_seq_id retired 2026-09-04).
- [ ] Genotype tokenizer: split on `;`, token→(marker, allele); alleles
      +/+, +/-, -/-, f/+, f/f, G/+, G/G, (hmo), (-); marker lookup seeded from
      GENOTYPING header; unknown marker→warn not reject; marker alone→allele=present.
      AC: fixture set covers every allele form + unknown-marker + bare-marker cases;
      all pass.
- [ ] GENOTYPING sheet normalization (A:O, legend rows 1–4, header 5, data 6+) +
      genotype upsert by natural key (mouse_id, marker, pcr_date).
      AC: 0 silently dropped rows; rejects in import_errors.
- [ ] Genotyping view + edit UI (runs, per-mouse marker/allele grid, signal colors).
      AC: each seed genotyping_results row visible; edit persists + audit-logged.
- [ ] Genotyping testing ticket (role-gated, added 2026-09-04): director picks
      mouse + markers → creates a genotyping_runs row + tasks in the ticket bin
      (reuses canTransition role gates).
      AC: only director/admin can create a testing ticket (staff attempt via
      API rejected, writes nothing); ticket lists the selected markers; staff
      see the ticket in the task bin.
- [ ] Per-marker result entry (added 2026-09-04): staff fill discovered allele
      per marker — incl. (-), ?, dnw — across prep-stages (>p15 vs Pup) and
      dates.
      AC: each entry persists one genotyping_results row FK'd to
      run+mouse+marker with prep_stage and date; (-)/?/dnw values round-trip;
      every entry audit-logged.
- [ ] genotype_label writeback (R6, rev. 2026-09-04): on run completion,
      recompute genotype_label = deterministic `;`-join of per-marker
      genotyping_results in line_marker_panels order and APPEND a
      `mouse_genotypes` rows (current via
      mouse_current_attrs — NO mouse column update); NEVER overwrite
      raw_genotype.
      AC: same inputs → byte-identical label on recompute; raw_genotype
      byte-unchanged after writeback; each recompute appends exactly one
      attr-log row; label order matches line_marker_panels order for a
      shuffled-input fixture.
- [ ] Add-marker endpoint (R5, added 2026-09-04): role-gated (director/admin)
      INSERT into markers — no schema change per marker.
      AC: staff request rejected server-side and writes nothing; a
      director-added marker is audit-logged and selectable in the next testing
      ticket.

## P2 — NOT MVP

- [ ] Offline PWA: read-only offline + queued mutations replayed online (pending
      Open Question 7 decision).
- [ ] Clerk auth: user mapping via clerk_user_id; roles admin / professor
      (read+write+verify) / staff (read+write-status). Identity resolution only
      — the role-gated transition matrix is already service-enforced from P0-a.
- [ ] RLS policies enforcing the role matrix at DB level (hardens the same
      canTransition matrix).
- [ ] Revisit ETL placement: promote import helper to queued worker if needed.
- [ ] xlsx export helper — see DEFERRED below (ordering: P2 or a dedicated
      later phase, per plan §3).

## DEFERRED (post-MVP — moved here in the 2026-09-04 re-scope, NOT deleted)

Superseded-for-MVP tasks preserved verbatim-in-substance for when the
export/color phase is scheduled. NOTE 2026-09-04: the genotyping-named tasks
that lived here were PROMOTED to P1 (see "P1 Genotyping subsystem" above — a
MOVE, nothing dropped); the items below are decoupled from it and stay
deferred:

- [ ] Colour mapper: font RGB → `signals.type` (R16 made signals a TABLE with its
      own colour column); fill theme+tint → color_maps
      lookup; unmapped combo → flag_for_review, never ignore.
      AC: known font colors (black/FF0000/FF0432FF) map; any unmapped fill returns
      flag_for_review, never a default enum.
- [ ] Color-map calibration pass WITH professor: populate color_maps (scope
      font/fill, argb/theme+tint → target_enum); confirm gray dead/sac semantics.
      AC: color_maps covers 100% of fills present in the real workbook OR
      remaining combos have flag_for_review rows; sign-off noted in status log.
- [ ] xlsx export helper: faithful export of Breeders + Genotyping from DB, up to
      full-format parity (colors, layout, legend rows).
      AC: export → re-import of the exported file yields zero domain diffs;
      professor accepts exported file format.
- [ ] Lifecycle tables: lifecycle_events (+ sample_type / lifecycle_event enums).
      NOTE (R11-R15, 2026-09-05): breeding is NO LONGER deferred and `matings`
      no longer exists — it shipped, then split into `mates` (the couple) and
      `litters` (each cycle, carrying its own dates and outcome). Only
      `lifecycle_events` (+ its enums) stays deferred here.
      AC: lifecycle_events.event uses the lifecycle_event enum; FKs to
      mouse_meta/cages (mouse_meta, not mice — `mice` holds version rows).
